# Changelog

## v0.0.00128 - 2026-08-06

■更新履歴（Readme / ChangeLog 用）

- 追加: ユーザー操作（メニュー・ボタン・セグメント切替・ポップアップ選択）の実行記録をデバッグログへ追加した。不具合報告時に「どの操作をどの順で行ったか」をログから追跡できる。

■更新履歴

- `NSApplication` サブクラス `MosaicApplication` を新設し、`sendAction(_:to:from:)` の
  オーバーライドで全コントロール・メニューのaction送出を一元フック（category `Action`）。
- 記録形式: `操作: メニュー ファイル > 画像を開く…` / `操作: ボタン 候補生成` /
  `操作: ボタン 人物 → ON` / `操作: 切替 範囲選択モード` / `操作: 選択 ノイズ` 等、UI上の表示名のみ。
- スライダーのドラッグ（連続送出でログが溢れる）とテキスト入力（内容がプライバシーに関わる）は
  記録しない。画像内容・ファイルパスも記録しない。
- `Mosaic/DEBUG_LOG_INVENTORY.md` に `Action` カテゴリの節を追加。

## v0.0.00127 - 2026-08-06

■更新履歴（Readme / ChangeLog 用）

- 改善: 「デバッグログ…」メニューをヘルプメニュー直下へ移動した（旧: ヘルプ＞デバッグ＞デバッグログの3階層 → ヘルプ＞デバッグログの2階層）。ログウィンドウを開きやすくするため。

■更新履歴

- 中間の「デバッグ」サブメニューを廃止し、`showDebugLogWindow` の項目をヘルプメニューへ直接追加。
- `Mosaic/ARCHITECTURE.md`・`Mosaic/DEBUG_LOG_INVENTORY.md` のメニュー経路表記を同期。

## v0.0.00126 - 2026-08-06

■更新履歴（Readme / ChangeLog 用）

- 修正: デバッグログの「消去…」を実行しても、今回起動分の過去ログが画面に残り続けていた問題を修正した。消去後は消去時刻より前のログが表示・保存の両方から除外され、画面上完全にクリアされる。

■更新履歴

- 従来の消去処理は保存済みファイルの削除＋退避位置の前進のみで、表示側が毎回
  `OSLogStore` から再取得する「今回起動分」（直近10分）には消去前のログが残っていた。
- 消去時刻を `debugLogClearedAt` に記録し、`fetchCurrentProcessLogText()` が
  それ以前のエントリを読み飛ばす方式へ変更。表示・退避の両方が同じ読み出しを
  経由するため、復活も残存も起きない。役目を終えた `markCurrentLogAsArchived()` は削除。
- 消去ダイアログの「アプリを再起動するまで表示に残ります」の注記を撤去。

## v0.0.00125 - 2026-08-06

■更新履歴（Readme / ChangeLog 用）

- 修正: ツールバーのモード切替（編集/範囲選択/マスク追加ペン/マスク消しゴム）と学習モードのアイコンが、他のツールバーアイコンよりも小さく表示され、詳細設定の「アイコンサイズ」変更にも追従しなかった問題を修正した。

■更新履歴

- `canvasModeControl`（`NSSegmentedControl`）は `toolbarIconButtons` の `NSButton` 経路を
  通らないため、アイコンサイズ設定（SymbolConfigurationのpointSize）が一切適用されて
  いなかった。新設の `applyIconSizeToCanvasModeControl()` でセグメント画像と
  セグメント幅（小=40/中=48/大=58）を設定へ追従させた。
- 学習モードボタンは登録済みだったが、`refreshLearningModeButton()` がON/OFF状態の
  記号（graduationcap/.fill）を SymbolConfiguration **なし**で上書きしていたため、
  常に既定サイズへ戻っていた（＝学習モードアイコンだけ小さいバグの直接原因）。
  共通ヘルパ `currentIconSymbolConfiguration()` を新設して全経路で共用し、
  `applyIconSizeToAllToolbarButtons()` からもセグメントと学習モードボタンへ再適用する。

## v0.0.00124 - 2026-08-04

■更新履歴（Readme / ChangeLog 用）

- 修正: なし（ユーザー向け挙動の変更はありません。運用文書の記載ずれのみ修正しました）。

■更新履歴

- 「残務（v0.0.00122・v0.0.00123の未レビュー分）の全コードレビュー」を実施。詳細は
  `Docs/QC/CodeReview/QC_CodeReview_v0.0.00124.md`。
- `Mosaic/ARCHITECTURE.md` の章番号重複を修正（§5.65が2つ存在していた。2つ目を§5.66へ改番）。
- `Mosaic/ARCHITECTURE.md` §5.58.3 が、v0.0.00119でツールバーを「マスク追加ペン」
  「マスク消しゴム」の2モードへ分割した後も旧仕様（単一の「マスク修正モード」・3セグメント）の
  まま残っていた（§5.61と矛盾）。現状に合わせて修正した。
- `Mosaic/QUALITY_STATS.md` のテスト件数表記をCHANGELOG記載の148件（v0.0.00123時点）に同期した。
- 情報共有として、`SAMSegmentEngine.minimumUsableCoverage`（v0.0.00123で0.02→0.40へ較正）が
  クラス共通の定数で乳首・乳輪ROIにも適用されるが、v0.0.00123の実測・回帰テストは性器ROIのみを
  対象としていたため、乳首・乳輪へSAMを使った場合の副作用は未確認である旨を`ARCHITECTURE.md`
  §5.66.3に記録した（対応は保留。次に該当ケースで問題が出た場合の参考情報）。
- コード（Swift）側の変更は無し。テスト148件PASS（既存のまま）、両ゲート通過。

## v0.0.00123 - 2026-08-03

■更新履歴（Readme / ChangeLog 用）

- 修正: 「対象形状（SAM）」で、性器の一部（特に亀頭部分）が完全に露出したままモザイクされない場合があった問題を修正した。

■更新履歴

- `SAMSegmentEngine.minimumUsableCoverage` を 0.02 → 0.40 へ引き上げた。
  - 実際に生成されたマスクを画像として書き出して目視確認したところ（一時診断コード。
    確認後は正式な回帰テストへ差し替え済み）:
    - `coverage=0.26`: 亀頭部分が完全に露出（検閲漏れ・失敗）
    - `coverage=0.58` / `0.71`: 対象を綺麗に覆えている（正常）
  - 従来の0.02は「完全に空」しか弾けず、この種の**部分的な検閲漏れ**を見逃していた。
  - 0.26（失敗）と0.58（正常）の間で、安全側に倍以上の余裕を見て0.40とした。
- 実測で確認: この変更で図形フォールバックへ切り替わったのは報告のあった1件のみ。
  サンプル5枚・性器ROI11件のうち、他の10件は引き続きSAMの精細な形状を使う
  （被覆率0.46〜0.71が保たれている）。
- テスト148件PASS（`lowCoverageSAMMaskFallsBackToShape` を追加）。

## v0.0.00122 - 2026-08-03

■更新履歴（Readme / ChangeLog 用）

- コードレビュー: 複数回のレビューでデグレ・整合性の問題5件を発見・修正した。詳細は `Docs/QC/CodeReview/QC_CodeReview_v0.0.00122.md`。

■更新履歴

- **P0**: インスペクタでROIの形状（矩形/楕円/多角形）またはカテゴリを手動変更しても、
  `maskShapeScale`（v0.0.00121）が再計算されない不具合を修正。矩形→楕円で対象の角が欠ける
  （検閲漏れ）、楕円→矩形で選択範囲より広い範囲が意図せずマスクされる、性器→非性器カテゴリ変更で
  倍率が残ったままになる、の3パターンが起きていた。
  `DetectedROIRefiner.recalculateMaskShapeScale` をコア層へ追加し、形状変更・カテゴリ変更の
  両ハンドラから呼ぶことで、現在の形状・カテゴリへ常に合わせ直すようにした。
- **P1**: `toggleMosaicPreview()`（モザイク表示チェックボックスの手動切替）が非表示レイヤ
  （v0.0.00118の `hiddenROIIDs`）を無視していた不具合を修正。`renderMosaicOutput(for:)` 経由へ統一。
- **P2**: マスク追加ペン（v0.0.00119）でレイヤ未選択のとき新規レイヤを作る操作が、
  「レイヤ追加」と「最初のストローク」で別々のUndo単位になっていた不具合を修正。
- **P3/P4**: ツールバーのツールチップとコード内コメントの表記漏れ（v0.0.00119の改名が
  一部反映されていなかった）を修正。
- テスト147件PASS（`recalculateMaskShapeScale` の検証1件を追加）。

## v0.0.00121 - 2026-08-03

■更新履歴（Readme / ChangeLog 用）

- 改善: 性器の認識範囲が実際より大きく表示される問題を解消した。画面に見える範囲が検出結果そのままになる。
  モザイクの掛かり方は今までと変わらない。

■更新履歴

- ROIの拡大方式を変更した。**表示・選択に使う `rect` は検出枠のままにし、
  マスクを切り取る形状だけを広げる**（`MosaicROI.maskShapeScale` / `maskShapeRect`）。
  - 従来（v0.0.00107〜120）は `rect` 自体を広げていたため、画面の選択範囲まで大きくなり
    「範囲が大きすぎる／位置がずれて見える」という報告が続いた（2026-08-02, 08-03）。
  - 形状を広げないとマスクが失われることは実測で確認済み（サンプル5枚・性器ROI11件で
    検出枠内の被覆率が1.2〜6.7%減る＝検閲漏れ）。そのため広げること自体はやめない。
  - **マスクの被覆率は変更前と完全に同一**であることを実測で確認した
    （`reportWhetherGenitalExpansionStillChangesTheMask`）。
- `analysisInsetScale`（倍率による逆算）を廃止。`analysisRect` は `rect` と同じになり、
  §5.62 で問題になった「クランプで逆算が壊れる」構造自体が無くなった。
- テスト146件PASS。

## v0.0.00120 - 2026-08-03

■更新履歴（Readme / ChangeLog 用）

- 追加: すべてのモザイクパターンでトーン（網点）のON/OFFができるようになった。
- 修正: サイドパネルの幅を変えても、ライブラリの検索欄が伸縮しなかった問題を修正。
- 変更: ノイズパターンの「細かさ」の初期値を4pxにした。
- 修正: 画像の端にある性器の範囲で、形状によってマスクが変わってしまう問題を修正。

■更新履歴

- `MosaicStyle.patternTone` を追加し、塗りつぶしレイヤの生成後に一律で網点化する（`CIDotScreen`）。
  ボーダー・雲・フラッシュは専用のトーン設定を持つため、そちらでは新しいチェックを出さない
  （1つのパターンにトーンのチェックが2つ並ぶのを避ける）。
  インスペクタの行は末尾（15行目）へ追加した（既存の行番号をずらさないため）。
  旧データは `decodeIfPresent` でOFF扱い（後方互換）。
- ライブラリの検索欄がパネル幅へ追従するようにした。フィルタ行の右端が
  `lessThanOrEqualTo` で止まっており、パネルを広げても行が縮んだままだった。
  `equalTo` へ変更し、検索欄側の hugging / compression resistance を下げて余白を吸収させる。
- 「細かさ」の既定値をパターンごとに持てるようにし、ノイズは4pxにした。
- **`expandGenitalROIsToCoverShape` のクランプを廃止**。画像の端で切り詰めると
  `analysisInsetScale` による逆算が元の検出枠へ戻らず、形状ごとに解析枠がずれていた。
  新しいサンプル画像で `samProducesTheSameMaskRegardlessOfROIShape` が検出（差 0.024）。
  クランプを外して全10件で差 0.000 に戻した。はみ出す分は描画・切り出しの各所で `clamped()` される。
- 実写の人物マスク被覆率を実測する `reportPhotoPersonMaskCoverage` を追加。
  サンプル5枚で Vision 0.20〜0.65 / SAM 0.49〜0.72。**優劣がつかず、被覆率では
  「マスク範囲がおかしい」の判定ができない**ことを確認した（イラスト経路の0.90判定は掛からない）。
- テスト145件PASS。

## v0.0.00119 - 2026-08-03

■更新履歴（Readme / ChangeLog 用）

- 変更: 「マスク修正モード」を「マスク追加ペン」に改名。レイヤ未選択のときは、モザイク対象へ新しいレイヤ（その他）を作って塗る。
- 追加: 「マスク消しゴム」をツールバーへ追加。Option(⌥)キーで追加ペンと消しゴムが一時的に入れ替わる。
- 修正: デバッグログの「消去」後にログが復活する問題を修正。

■更新履歴

- `InteractionMode` に `maskErase` を追加し、`editsMask` でペン系をまとめて判定する。
  - ツールバーは4セグメント（編集／範囲選択／マスク追加ペン／マスク消しゴム）。
  - Option(⌥)押下中は動作が反転する（追加ペン→消しゴム、消しゴム→追加ペン）。
    `(interactionMode == .maskErase) != inverted` で判定する。
- マスク追加ペンで対象レイヤが無い場合、クリック位置に新しいROI（カテゴリ「その他」、
  グループ名も「その他」、`source: "manual"`）を作ってから塗る（`onMaskStrokeNeedsNewLayer`）。
  枠の大きさは筆の太さから決める。消しゴムでは作らない（消す対象が無いため）。
- デバッグログ「消去」の不具合を修正。削除自体は成功していたが、消去時に退避位置を空へ戻していたため、
  30秒ごとの退避処理が今回起動分を丸ごと書き戻していた（GUI報告 2026-08-02）。
  `markCurrentLogAsArchived()` で退避位置を「今の最後の行」まで進めるようにした。
- テスト144件PASS。

## v0.0.00118 - 2026-08-02

■更新履歴（Readme / ChangeLog 用）

- 変更: 骨格から推定していた乳首・乳輪の候補を廃止した。検出モデルが見つけたものだけを出す。
- 追加: 自動検出の結果を候補カテゴリごとのグループへまとめるようにした。グループ名はカテゴリ名。
- 追加: レイヤ一覧のモザイク対象に、1件ごと・グループごとの表示ON/OFFチェックを追加した。
- 追加: デバッグログウィンドウに「消去…」を追加。保存済みログを世代ファイルごと削除できる。
- 改善: メインウィンドウを閉じると、デバッグログ・詳細設定・キー割当などの関連ウィンドウも閉じる。

■更新履歴

- **骨格由来の乳首・乳輪プライアを廃止**（`SensitiveROIGenerator.chestROIs` と
  `PoseDerivedROIFilter.dropChestPriors` を削除。ユーザー判断 2026-08-02）。
  肩関節からの幾何推定は、姿勢が斜め・背中向き・複数コマの漫画では位置も個数も当たらず、
  誤検知として3度報告された（§5.45 / §5.51 / §5.60）。信頼度でも分離できない
  （実測: 誤検知が 0.36 / 0.61 / 0.62 に散らばる）。
  取りこぼしはペンによるマスク修正（v0.0.00116）や手動ROI追加で補う。
  鼠径部のプライア（`pose-groin`）は誤検知報告が無く、検出器が性器を見つければ落ちるので残す。
- 自動検出の結果へ `roiGroupName = カテゴリの表示名` を設定し、カテゴリごとにまとめる。
  手描き（`source == "manual"`）と、既にグループ名があるROIは触らない。
- レイヤ一覧のモザイク対象へ表示ON/OFFチェックを追加。
  - 1件ごと・カテゴリグループごと（グループは全表示/全非表示/混在の3状態）。
  - **画面表示だけの設定で、画像出力には影響しない。** 既存の「表示: ROI/モザイク/人物/骨格」と
    同じ扱いにする（隠したまま出力して検閲漏れになるのを避けるため）。
- デバッグログウィンドウに「消去…」を追加。`RotatingLogFile.removeAll()` で世代ファイルごと削除する。
  確認ダイアログを出し、今回の起動分はシステム側に残る旨も伝える。
- 主ウィンドウの `windowWillClose` で補助ウィンドウ（動画プレビュー・画像出力・詳細設定・
  ショートカット一覧・テンキー割当・デバッグログ）をすべて閉じる。
- テスト144件PASS（骨格プライア廃止の検証1件を追加、旧フィルタのテスト2件を撤去）。

## v0.0.00117 - 2026-08-02

■更新履歴（Readme / ChangeLog 用）

- 修正: モザイクの設定を変えるたびにアプリが一時的に固まる問題を修正した。マスクを使い回すようにしたので、見た目の調整が即座に反映される。

■更新履歴

- `MosaicEngine` にマスクのキャッシュを追加した。
  - `applyMosaic` はモザイクの見た目を変えるたびに呼ばれるが、毎回 `createMasks` を実行していた。
    既定のマスク生成方式が「対象形状（SAM）」（v0.0.00104〜）なのでROIごとにONNX推論が走り、
    主スレッドで数百msかかっていた（GUI報告 2026-08-02「モザイクを編集する毎に一時ハングする」）。
  - **マスクはROIの形状・位置・生成方式・手描き補正だけで決まり、スタイル（パターン・色・
    ブロックサイズ等）には依存しない。** `MosaicROI.maskIdentity` を鍵にして使い回す。
  - 形状しきい値の変更とマスク生成方式の切替では `invalidateMaskCache()` を呼ぶ
    （キャッシュはエンジンの型名で識別しているため、同じ型で設定だけ変わる場合は明示的に捨てる）。
  - 上限256件。超えたら全て捨てる。
- テスト145件PASS（キャッシュの2件を追加）。スタイルだけ変えたときにマスクを作り直さないこと、
  ROIの形・位置・手描き補正を変えたときはそのROIだけ作り直すことを、生成回数を数えて検証している。

## v0.0.00116 - 2026-08-02

■更新履歴（Readme / ChangeLog 用）

- 追加: マスク形状をペンで塗って直せるようにした。ツールバーの筆アイコンで「マスク修正モード」へ切り替え、
  選択中のレイヤ上をドラッグすると塗れる。Option(⌥)キーを押しながらで消せる。太さはインスペクタで調整できる。

■更新履歴

- `ManualMaskStroke` / `ManualMaskPainter`（MosaicCore）を新設。
  - **ビットマップではなくストローク（点列）で保持する。** 保存データが小さく、座標がROIローカル（0〜1）
    なのでROIを移動・リサイズしても補正が追従し、1ストローク単位で取り消せる。
  - `MosaicROI.manualMaskStrokes` へ保持。既存データは空配列として読める（後方互換）。
  - 反映は `MosaicEngine.createMasks` の最後。どのマスク生成方式を選んでいても同じように効く。
    塗りは生成マスクとの明るい方を採り（`CIMaximumCompositing`）、消しは反転して掛ける。
- キャンバスに `InteractionMode.maskPaint` を追加。ツールバーの3つ目のセグメント（筆アイコン）で切り替える。
  - ドラッグ中の点列をROIローカル座標で貯め、離した時点で1ストロークとして確定する。
  - 1ストローク＝1回の「元に戻す」単位（塗るたびに全部消えるのを避ける）。
  - Option(⌥)キーを押しながらで消しになる。
  - 対象は選択中のレイヤ。選択が無ければクリック位置のレイヤを対象にする。
- インスペクタに「ペンの太さ」を追加（マスク修正モードのときだけ表示）。
- テスト143件PASS（手描き補正の3件を追加）。座標系は自前で検証せず、既存の
  `ShapeSegmentEngine.hardShapeMask` と同じ位置に出るかで確認している（上下反転を繰り返し起こしているため）。
  この検証で実装のバグ（1点ストロークの塗り色が黒のままで何も描かれない）を1件発見・修正した。

## v0.0.00115 - 2026-08-02

■更新履歴（Readme / ChangeLog 用）

- 修正: ウィンドウを閉じてアプリを終了しようとすると強制終了する問題を修正。

■更新履歴

- **主ウィンドウと動画書き出しシートに `isReleasedWhenClosed = false` を設定した。**
  v0.0.00113 で補助ウィンドウ5つは直したが、この2つを見落としていた。
  - 主ウィンドウは `AppDelegate.window` で強参照しているのに既定の `true` のままだった。
    閉じるとAppKitがreleaseして参照が解放済みメモリを指し、
    「最後のウィンドウを閉じた」→終了判定→`applicationShouldTerminate` の
    `if let window` で `objc_retain` して落ちていた（クラッシュ報告 2026-08-02、スタックと一致）。
- **再発防止として `scripts/ci/agent_governance_guard.sh` にチェックを追加した。**
  `Sources/NewMosaicApp/main.swift` の `NSWindow(` 生成箇所すべてについて、後ろ40行以内に
  `isReleasedWhenClosed` が無ければFAILにする。同じ原因で2度クラッシュ報告が出たため、
  目視ではなく機械的に検出する。故意に設定を外してFAILすることも確認済み。
- テスト140件PASS。

## v0.0.00114 - 2026-08-02

■更新履歴（Readme / ChangeLog 用）

- 修正: 楕円・多角形を選ぶと、矩形のときと違う範囲がマスクされる問題を修正。形状を変えても同じ対象が同じようにマスクされる。

■更新履歴

- ROIに「表示・切り取りに使う枠（`rect`）」と「解析に使う枠（`analysisRect`）」の区別を導入した。
  - v0.0.00111 で性器ROIを形状ごとに広げたが、`rect` はマスク生成エンジンが**対象を切り出す枠**
    （SAMのプロンプト枠・前景抽出のクロップ）としても使われていた。広げた枠をそのまま渡していたため、
    エンジンが別の範囲を切り出していた（GUI報告 2026-08-02）。
  - 実測（同じ男性器で形状だけ変えた場合のSAM被覆率）:

        矩形   枠0.178x0.126 → 0.55
        楕円   枠0.252x0.178 → 0.28  ← 対象を捉えられていない
        多角形 枠0.282x0.199 → 0.22

  - `MosaicROI.analysisInsetScale`（`rect` を元の検出枠へ戻す倍率）を追加し、
    `analysisRect` として参照する。倍率で持つのは、ユーザーがROIを移動・リサイズしても
    比率が保たれるようにするため。既存データは値が無ければ `rect` をそのまま使う（後方互換）。
  - `SAMSegmentEngine` のプロンプト枠と、`RegionForegroundSegmentEngine`（対象形状）のクロップを
    `analysisRect` 基準へ変更した。
- 実測で確認（`swift test --filter samProducesTheSameMaskRegardlessOfROIShape`）:
  サンプル3枚の性器ROI10件すべてで、矩形・楕円・多角形の被覆率が**完全に一致**（差 0.000）。
- テスト140件PASS（形状非依存の実測1件・解析枠の単体2件を追加）。

## v0.0.00113 - 2026-08-02

■更新履歴（Readme / ChangeLog 用）

- 修正: 詳細設定・テンキー割当などの設定画面を一度閉じると、再度開けなくなったりアプリが強制終了する問題を修正。

■更新履歴

- 補助ウィンドウ（画像出力・詳細設定・テンキー割当・ショートカット一覧・デバッグログ）に
  `isReleasedWhenClosed = false` を設定した。既定は `true` で、閉じるとAppKitがreleaseする。
  アプリ側は同じウィンドウを `var ...Window: NSWindow?` で強参照しているため、閉じた時点で
  **過剰解放になり参照が解放済みメモリを指していた**。
  次に開こうとすると「既に開いていれば前面へ」の経路が解放済みへ `makeKeyAndOrderFront:` を
  送り、開けない／落ちるという症状になっていた
  （クラッシュ報告 2026-08-02「キー割当後、再度キー割当設定画面が開けない」）。
- `windowWillClose` で該当する参照を `nil` にし、次回は作り直すようにした
  （`releaseAuxiliaryWindowReference`）。閉じたウィンドウを使い回すと内容が古いままになるため。
- 主ウィンドウと同じdelegateを共有するため、保存確認（`windowShouldClose`）とウィンドウ枠の保存
  （`saveWindowFrame`）は補助ウィンドウを除外する（`isAuxiliaryWindow`）。
- クラッシュレポートのシンボル名（`@objc VideoPreviewView.togglePlay()`）は誤帰属だった。
  動画プレビューを開いていない操作でも同じスタックが出ており、レジスタが示すセレクタは
  `makeKeyAndOrderFront:`、`x19` は `showAdvancedSettings()` を指していた。
  v0.0.00112 で入れた動画プレビューの後始末も、それ自体はライフサイクルの欠陥として妥当なので残す。

## v0.0.00112 - 2026-08-02

■更新履歴（Readme / ChangeLog 用）

- 改善: デバッグログをファイルへ保存するようにした。アプリを再起動しても前回のログが残る（1ファイル1MB・最大5世代）。
- 変更: ツールバーの「レイヤ削除」を「全レイヤ削除」に変更。
- 修正: 動画プレビューを閉じたあとの操作でアプリが異常終了することがある問題に対処した。

■更新履歴

- `RotatingLogFile`（MosaicCore）を新設。1ファイル1MB・最大5世代でローテーションする。
  保存先は `~/Library/Application Support/newMosaic/Logs/`。
  - 従来は `OSLogStore(scope: .currentProcessIdentifier)` から直近10分を読むだけで、
    プロセス内のログしか取れず**再起動で前回分が消えていた**。
  - 起動直後・30秒ごと・終了時にファイルへ退避する（`AppDelegate.startDebugLogArchiving`）。
    ファイルI/Oは主スレッドで行わない。
  - デバッグログ画面は「保存済み（前回起動分を含む）＋今回起動分」を連結して表示する。
- 「レイヤ削除」→「全レイヤ削除」へ改名（ツールバー・ショートカット一覧・関連コメント）。
- 動画プレビューの後始末を確実にした（クラッシュ報告 2026-08-02、
  `@objc VideoPreviewView.togglePlay()` 内の `objc_msgSend` で EXC_BAD_ACCESS）。
  - プレビューウィンドウに delegate が無く、閉じるボタンで閉じても `stop()` が呼ばれなかった。
    そのため再生・時刻オブザーバ・コントロールの `target`（＝自分自身）が残り続けていた。
  - `windowWillClose` で `stop()` を呼び、参照を解放する。
  - `stop()` は `playerLayer.player` とコントロールの `target`/`action` も解除する（冪等）。
  - 主ウィンドウと同じ delegate を共有するため、`windowShouldClose` と枠の保存は
    ウィンドウ判定で分岐する（プレビューでは保存確認も枠保存も行わない）。
- ローカルに残っていたONNXモデル8点を `Sources/MosaicCore/Resources/` から削除した
  （v0.0.00109 で Package の参照からは外したが実体が残り、SwiftPMが未処理ファイルとして警告していた）。
  実行時の参照先は `~/Library/Application Support/newMosaic/Models/`（`Docs/MODELS.md`）。
- テスト137件PASS（ログローテーションの2件を追加）。

## v0.0.00111 - 2026-07-31

■更新履歴（Readme / ChangeLog 用）

- 修正: 矩形の認識範囲が必要以上に大きくなる問題を修正。
- 修正: 楕円・多角形で認識範囲が性器より小さくなる問題を修正。範囲が対象を包み込む大きさになった。
- 修正: モザイクが選択範囲の外へはみ出す問題を修正。マスクは常に選択した形状の内側に収まる。

■更新履歴

- 性器ROIの拡大を**形状ごとの倍率**へ変更し、**形状が決まったあとに**適用するようにした
  （`DetectedROIRefiner.expandGenitalROIsToCoverShape`）。
  - 従来は検出直後に一律1.15倍していた。この時点では全ROIが既定の`.ellipse`のため、
    ユーザーが矩形を選んでいても拡大され「矩形が大きめ」になっていた（GUI報告 2026-07-31）。
  - 倍率は「その形状が元の検出枠を完全に含む」最小値を幾何から求めた。
    矩形 1.0（四隅を削らないので拡大不要）／楕円 √2 ≒ 1.414／多角形（既定の六角形）1.58。
- SAMのマスクを**常に選択範囲の形状で切る**ようにした（`SAMSegmentEngine`）。
  - 従来は「SAMが対象を分離できていれば矩形だけで切る」としていた（§5.46）。楕円で切ると
    対象が欠けるためだったが、その結果マスクが範囲の外へはみ出していた（GUI報告 2026-07-31）。
  - 上記の拡大により形状が対象を包み込むようになったため、形状で切っても欠けない。
    表示している範囲とマスクが一致する方が動作として正しい。
- テスト135件PASS（形状ごとの拡大倍率の検証を追加）。
- 不安定だったテスト `embeddingCacheAvoidsReencoding` を修正した。原因は2つ:
  1. 実行時間の比較で判定しており、機械の負荷で揺れていた → エンコーダの実行回数で判定する方式へ変更。
  2. 埋め込みキャッシュがプロセス共有のため、並列実行された他のテストが対象画像を入れ替え、
     2回目でも再エンコードが起きていた → `SAMSegmentEngine.withIsolatedCacheForTesting()` を追加し、
     このテストだけ専用キャッシュを使う。
  全体テストの5回連続完走を確認した。

## v0.0.00110 - 2026-07-31

■更新履歴（Readme / ChangeLog 用）

- 追加: 学習モードのON/OFF。ツールバーの学帽アイコンで切り替える。**既定はOFF**で、ONにしたときだけ学習する。
- 追加: ファイル＞設定＞初期化 で、初期化する項目を個別に選べるようにした（学習内容の初期化も可能）。
- 追加: ファイル＞設定＞バックアップ。各種設定・動作状態・学習内容を項目ごとに選んで一括バックアップできる。
- 修正: サイドパネルを縦に縮めたときにアイコンや行が重なる問題を修正。

■更新履歴

- 学習モード（`Learning.Enabled`、既定OFF）を追加。ツールバーへトグルボタンを置き、
  ONで塗りつぶしの学帽＋アクセント色、OFFで輪郭の学帽＋グレーで状態を示す。
  - OFFのときは**記録も利用も行わない**。利用だけ残すと、OFFにしても過去の学習由来の候補が
    出続けて止める手段が無くなるため。
  - 従来は常に学習しており、テスト用に描いたROIまで学習されて別の人物へ誤った候補が
    提案されていた（GUI報告 2026-07-31、§5.51）。
- `LearningEngine.removeAllLearningData()` / `storageSummary()` / `storageDirectory` を追加。
- ファイル＞設定＞初期化: 項目別のチェックボックス（アプリ設定／画面レイアウト・動作状態／
  モデルの学習内容／ライブラリ）を持つダイアログへ変更。既定チェックは前2者のみ
  （失うものが大きい学習内容・ライブラリは既定OFF）。
- ファイル＞設定＞バックアップ を初期化の下へ追加。選んだ項目を1フォルダへ書き出し、
  戻し方を記した `README.txt` を同梱する。ライブラリの元画像本体とAIモデルは含まない（容量のため）。
- サイドパネルの縦分割に最小高さの制約を追加（`NSSplitViewDelegate` の
  `constrainMinCoordinate` / `constrainMaxCoordinate` を縦分割にも適用）。
  最小高さは `fittingSize` から取り、上限280ptを設ける。
  ライブラリ一覧のスクロール領域にも最小高さ60ptを与えた。

## v0.0.00109 - 2026-07-31

■更新履歴（Readme / ChangeLog 用）

- 変更: AIモデル（計451MB）をアプリ同梱から**個別インストール運用**へ変更した。導入手順は `Docs/MODELS.md`。
- 注意: 別のMacでアプリを動かす場合、そのMacにもモデルの配置が必要になった。

■更新履歴

- ONNXモデル8点をGit管理外とし、`Package.swift` のリソース宣言からも外した。
  GitHubの1ファイル100MB上限（`anime_seg` 168MB / `anime_pose` 128MB）でpushできなくなったため。
- 配置先は `~/Library/Application Support/newMosaic/Models/`。実行時の解決は既存の
  `YOLOONNXModel.cachedModelURL(resourceName:)`（バンドル→上記フォルダ）がそのまま使える。
- `scripts/install_models.sh` を追加（導入と `--verify` による確認）。
- `Docs/MODELS.md` を追加（配置先・導入手順・モデル一覧・入手元・ライセンス・未導入時の挙動）。
- モデル未検出時のエラーメッセージに、配置先パスと導入手順の参照先を含めた。
- `scripts/package_macos_app.sh` はモデル未導入時に警告を出す（生成した `.app` は検出機能を使えないため）。
- テスト135件PASS（モデルはApplication Support経由で読めることを実測で確認）。

## v0.0.00108 - 2026-07-31

■更新履歴（Readme / ChangeLog 用）

- 修正: 引きのコマ（小さく描かれた人物）に、大きすぎる推定範囲が付く問題を修正。
- 修正: 自動検出が正しく見つけた部位の隣に、学習による推定範囲が重ねて出る問題を修正。
- 改善: 解析ログをより確実に残すようにした。

■更新履歴

- `LearningEngine` の学習提案（`source: "learned-prior"`）の2つの不具合を修正した。
  1. **大きさの単位の取り違え**: 位置は人物枠相対（`personGrid`）で決めるのに、大きさだけ
     画像相対の `meanWidth/meanHeight` を使っていた。アップのコマで学習した大きさが引きのコマの
     小さな人物にもそのまま適用され、人物より大きなROIが提案されていた
     （GUI報告「下段の性器（女性）の範囲が大きい」）。人物相対の平均
     `meanPersonRelativeWidth/Height` を追加し、人物ごとの提案では人物枠の大きさを掛ける。
     旧statsファイルにこの項目は無いためOptionalとし、無い場合は人物ごとの提案を行わない
     （次回の学習記録で再構築される）。
  2. **検出器が見つけたカテゴリにも提案していた**: 骨格由来プライア（`dropChestPriors` /
     `dropGroinPriors`）と同じ方針に揃え、検出器（`anime-censor` / `photo-censor`）が
     そのカテゴリを1件でも出していれば学習提案を足さない
     （GUI報告「下段の性器（女性）は誤検知」）。学習提案は取りこぼし時のフォールバックとする。
- 解析診断ログの出力レベルを `.info` → `.notice` へ（報告時に添付する前提のため取りこぼしを避ける）。
- テスト135件PASS（学習提案の2件を追加）。

## v0.0.00107 - 2026-07-31

■更新履歴（Readme / ChangeLog 用）

- 改善: 楕円マスクで性器の先端・根本が範囲から外れる問題を修正（範囲を15%広げた）。
- 追加: 自動検出のたびに、ソース画像のファイル名・MD5・検出設定・全ROIをデバッグログへ記録するようにした。不具合報告時にログを添付できる。
- 運用: コード修正・実装・ドキュメント更新のたびにコミット・プッシュする規則を追加。

■更新履歴

- `DetectedROIRefiner.expandGenitalEllipses`: 検出器が出した性器の**楕円**ROIを1.15倍へ広げる。
  楕円は外接矩形より約21%（1 - π/4）小さく四隅を削るため、検出枠が水平・垂直矩形で返る一方
  男性器が斜めに描かれていると、先端と根本が対角に来て楕円から外れていた（GUI報告 2026-07-31）。
  対象は検出器由来の楕円のみ。手描き（`source == "manual"`）・矩形・多角形・乳首は変更しない。
  画像外へはみ出す分は `clamped()` で切り詰める。
- `AnalysisDiagnostics`（MosaicCore）を新設し、解析1回ごとに次をデバッグログ（category `Detection`）へ出力する。
  - `analysis v=<版数> file=<ファイル名> md5=<MD5> size=<幅>x<高> domain=<画像種別> threshold=<しきい値> maskEngine=<方式> categories=[...]`
  - ROIごとに カテゴリ／生成元／位置／大きさ／回転／信頼度／形状／マスク生成設定（個別かinheritか）
  - プライバシー方針の例外としてファイル名とMD5を残す（同一ファイルかの照合のため）。フルパスと画像内容は残さない。
- `CLAUDE.md` §2.0（都度コミット・都度プッシュ）と §2.0.1（実装ごとの作業AIモデル切替と記録）を追加。
  `AGENTS.md` / `GEMINI.md` にも要約を記載。
- テスト133件PASS（性器楕円拡大の2件を追加）。

## v0.0.00106 - 2026-07-31

■更新履歴（Readme / ChangeLog 用）

- 改善: 使用メモリをさらに削減（画像切替用キャッシュの見直し）。

■更新履歴

- `PerImageEditState` から `renderedImage`（フル解像度の描画済み画像）を削除。切替時はROIから
  作り直す。表示していなくても画像出力・ライブラリ保存が参照するため、表示の有無にかかわらず
  再描画する（未描画のまま出力すると検閲漏れになる）。
- `imageEditStateLimit` を 8 → 3 へ。1枚あたり人物マスク（フル解像度グレー）を人数分×2系統
  保持するため、A4・300dpi・4人で約70MB。8枚では約560MBに達していた。
- 実測の内訳（`MemoryFootprintTests`）とアプリ側の保持量の対応を ARCHITECTURE §5.49 に追記。
- アプリと同じ合成経路（骨格プライア＋顔領域＋検出器＋各種除去）で最終ROIを出力する診断テスト
  `reportFinalROIsAsAssembledByApp` を追加。GUI報告の切り分けを推測ではなく実測で行うため。

## v0.0.00105 - 2026-07-31

■更新履歴（Readme / ChangeLog 用）

- 改善: 使用メモリを大幅に削減。編集を重ねるほどメモリが増え続ける問題を修正した。
- 注意: 「元に戻す」「やり直し」は結果を作り直す方式になったため、ROIが多い画像ではわずかに時間がかかる。

■更新履歴

- 「元に戻す」1ステップが**フル解像度の描画済み画像**を保持していた問題を修正（メモリ肥大の主因）。
  A4・300dpi（2480x3508）で1枚34.8MB、スタックに上限がなく、さらに画像切替用の
  `imageEditStates`（最大8枚）がそれぞれ独自のundo/redoスタックを持つため、
  8枚×20ステップ＝約5.6GBに達し得た。`EditorState` はROIのみを保持し、
  `applyEditorState` がROIから描画し直す方式へ変更した。
  - モザイク表示OFFでも `renderedImage` は画像出力・ライブラリ保存が参照するため、
    表示の有無にかかわらず再描画する（未描画のまま出力すると検閲漏れになる）。
  - `undoHistoryLimit = 100` を追加（1段が数KBになったため大きめ）。
- サムネイルキャッシュにLRU上限300件を追加（1枚約230KB。従来は無制限）。
- SAMの埋め込みキャッシュ上限を24→8へ（1件4MB。1画像あたりの小さいROIは通常10件未満）。
- ONNX Runtimeの推論に `memory.enable_memory_arena_shrinkage` を指定（`ORTMemory`）。
- メモリ実測テスト `MemoryFootprintTests` を追加。実測値（`swift test --filter MemoryFootprintTests`）:
  - 検出モデル +83MB / キャラクター分離 +207MB / 骨格 +135MB / SAM読込 +93MB
  - SAM初回推論 +540MB（2回目以降は+4〜6MBで頭打ち。MobileSAMのエンコーダ固有の作業領域で、
    実行スレッド数を1/2/4に変えても500〜540MBで変わらないことを確認済み）
  - モデル常駐の合計は約1.0〜1.5GB。これは現状の下限。

## v0.0.00104 - 2026-07-31

■更新履歴（Readme / ChangeLog 用）

- 改善: 小さい部位（乳首・小さく描かれた性器）のモザイク形状が、対象の輪郭に沿うようになった。
- 改善: 検出のしきい値を見直し、寝具の陰影などを性器と誤検出する例を減らした。真の検出は減っていない。
- 改善: 大きく描かれた性器が検出結果から消えることがあった問題を修正。
- 変更: マスク生成の初期設定を「対象形状（SAM）」に変更。
- 削除: マスク生成方式「対象形状（初期実装・比較用）」を削除。

■更新履歴

- `SAMSegmentEngine`: 小さいROIで周辺を切り出してから推論する経路を追加（`binaryMaskViaCrop`）。
  SAMは画像全体を長辺1024へ縮小し、デコーダのマスクは256×256で生成されるため、
  小さいROIは形状が潰れていた。実測（サンプル3枚・ROI13件、ROI矩形内の被覆率）:
  - 入力換算17〜59: 全体推論 0.89〜0.97（枠を塗り潰し＝分離できていない）→ 切り出し 0.72〜0.82
  - 入力換算130〜255: 0.36〜0.70 → 0.36〜0.66（差はノイズ程度）
  しきい値 `minimumBoxSideInInput = 96`。埋め込みキャッシュを (元画像, 切り出し窓) キーの多エントリ方式へ変更。
- `AnimeCensorDetector.defaultConfidenceThreshold = 0.4`（従来0.3）。実測で、0.30では寝具の陰影を
  性器と誤検出する例が1件（スコア0.33）あり、0.40でその1件だけが消え真の検出（最低0.44/0.47）は
  1件も失われなかった。
- `DetectedROIRefiner.dropOversizedGenitalROIs` を削除。人物枠を基準に「大きすぎる性器ROI」を落とす
  仕組みだったが、実測で**真の検出を消していた**（アップのコマでは性器が人物枠の相当割合を占めるため）。
  この規則は成立しない。誤検出はしきい値で扱う。
- `PersonSilhouetteProvider` を新設し、人物シルエットの経路（クロップ推論→SAM→全体画像推論）と
  「人物枠を塗り潰したマスクを弾く」判定をコアへ集約。従来の判定はクロップ（枠の1.08倍）に対する
  割合で見ていたため、人物枠に対して0.96でもクロップ比では0.82にしかならず**働いていなかった**。
  人物枠に対する割合で判定するよう修正（上限0.90）。実測で被覆率 0.96→0.79、0.97→0.68 へ改善。
- マスク生成の既定を `samShape` へ変更。`regionForegroundLegacy`（初期実装・比較用）と
  `LegacyRegionForegroundSegmentEngine` を削除。
- 実画像回帰テストの期待値 `*.expected.json` をGit管理下へ（座標のみで著作物を含まないため）。
  サンプル3枚で再現率1.00・誤検出0件。テスト計129件が通過。

## v0.0.00103 - 2026-07-29

■更新履歴（Readme / ChangeLog 用）

- 追加: 実画像を置くだけで検出・人物マスクの品質を自動測定できる回帰テストを追加。画像はGit管理外。
- 改善: 人物の輪郭抽出に失敗した場合、SAM（枠プロンプト）で人物を切り出すようにした。人物が重なる場面や寝具の多い場面で、背景や別人まで人物範囲として選択される問題の軽減を狙う。

■更新履歴

- `Tests/SampleImages/` を新設（`.gitignore`済み。README.mdのみ管理下）。`SampleImageRegressionTests` が同フォルダの画像を自動で読み込み、検出結果と人物マスク被覆率を測定する。画像が無ければ何も測定せず成功する（CIを壊さない）。
  - 期待値は `<画像名>.expected.json` で与える（カテゴリ＋正規化rect、`minimumRecall` / `maximumFalsePositives` / `maximumPersonMaskCoverage`）。同カテゴリ・IoU 0.3以上で対応付ける。
  - 期待値ファイルが無い画像は測定のみ行い合否判定しない。
- `SAMSegmentEngine.instanceMask(in:box:)` を公開。枠プロンプトから元画像サイズの二値マスクを返す。ROI用の推論経路と共通化（`binaryMask`）。
- 人物マスク生成の経路を「クロップ推論 → **SAM（枠プロンプト）** → 全体画像推論」の順へ変更。`anime_seg.onnx` はキャラクター/背景の二値分類のため人物の重なりや寝具の多い場面で分離できないが、SAMは枠で指定したインスタンスを取る。
- テスト追加（計132件）: SAMのインスタンスマスクが枠より小さく空でもないこと、サンプル画像回帰テストの空フォルダ動作。

## v0.0.00102 - 2026-07-29

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 「対象形状（SAM）」で、対象が選択範囲の楕円からはみ出してマスクされない（検閲漏れになる）問題を修正。
- バグ修正: 1ページに複数コマがある漫画で、小さいコマの対象部位が自動検出されない問題を修正。全体画像に加えて人物ごとのクロップでも検出するようにした（実写側と同じ方式に統一）。

■更新履歴

- **楕円からのはみ出し**: 楕円は外接矩形より約21%小さく四隅を削ぎ落とす。SAMが対象の輪郭を正しく取れていても、ROIの形状で切ると対象の一部がマスクから外れていた。SAMのマスクがROI矩形内で85%（`shapeConformCoverage`）を超えて広がっている場合＝対象を分離できていない場合のみROIの形状で切り、それ以外は矩形制限に留めて輪郭をそのまま残すよう変更。v0.0.00099で入れた「楕円ROIなのに四角いモザイク」の修正も維持される。
- **小さいコマの見逃し**: `AnimeCensorDetector`は全体画像1回のみの推論だった。モデル入力640x640に対し、1ページ複数コマの小さいコマでは対象部位が数ピクセルへ縮小され検出できない。実写側（`PhotoCensorDetector`, Build 39〜）にあった人物クロップごとの再推論をアニメ側にも適用した。
- `MultiScaleDetection`（`MosaicCore`）を新設し、全体＋人物クロップの推論・座標変換・重複統合を両検出器で共有する（実写側の重複実装を委譲へ置き換え）。
- SAMの診断ログへ`rectCoverage`と`clip=shape|rect`を追加。
- テスト追加（計129件）: クロップ座標の画像座標への変換、重複時に高信頼度を残すこと、代替タイルが全体を覆い隣接タイルが重複すること。

## v0.0.00101 - 2026-07-29

■更新履歴（Readme / ChangeLog 用）

- バグ修正: v0.0.00100で入れた性器の誤検出フィルタが効かず、誤検出が残っていた問題を修正。人物領域の選び方を「最も締まった枠」へ変更した。
- 追加: 検出後処理の判定値をデバッグログへ記録するようにした（ヘルプ＞デバッグ＞デバッグログ）。誤検出が残る場合の原因切り分けに使える。

■更新履歴

- **v0.0.00100のフィルタが効かなかった原因**: 体格基準の人物領域を「ROIとの重なり面積が最大のもの」で選んでいた。人物検出は複数人をまとめた過大な枠を返すことがあり（実測: 下段コマ全体を覆う人物枠）、その過大な枠が基準に選ばれて面積比が薄まり、しきい値12%を下回って除去されていなかった（実測: 締まった枠なら22%だが、過大な枠では10%）。
- `bodyScaleReference(for:persons:)` を新設。**ROIの6割以上を含む人物領域のうち面積が最小のもの**を基準に採る。端がわずかに重なるだけの枠を基準にしないよう下限（`minimumROICoverageByPerson` = 0.6）を設けた。
- `DetectedROIRefiner`へ診断ログ（category `Detection`、`genitalSize` 行）を追加。カテゴリ・検出スコア・ROI面積・基準面積・面積比・しきい値・人物数・判定結果を記録する。画像内容・ファイルパスは記録しない。
- テスト追加（計126件）: 過大な枠と締まった枠が併存する場合に締まった方を選ぶこと、端の重なりだけの枠を選ばないこと、画面実測の座標比で誤検出が除去され正しいROIが残ること。

## v0.0.00100 - 2026-07-29

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 人物に対して大きすぎる性器ROIを誤検出として除外するようにした。寝具の陰影などを性器と誤検出して大きな範囲がマスクされる問題を解消。
- 追加: 候補カテゴリに「**乳輪**」を追加。検出器の乳首枠は乳輪相当の大きさで返るため、「乳首」は枠を縮小した乳首相当の範囲、「乳輪」は枠そのままの範囲として別カテゴリへ分けた。
- 「乳首」は従来どおり既定ON、「乳輪」は既定OFF。乳輪まで覆いたい場合はチェックをONにする（両方ONにすると同じ場所へ大小2つのROIが並ぶ）。

■更新履歴

- `MosaicTargetCategory.areola`（乳輪）を追加。既存データの`allCases`索引がずれないよう列挙の**末尾**へ追加した（索引が学習データ書き出しのクラス番号に対応するため）。テストで索引を固定。
- `DetectedROIRefiner`（`MosaicCore`）を新設。
  - `splitNippleAndAreola`: 検出器の乳首ROIから乳輪ROI（枠そのまま）を派生させ、乳首ROI自体を0.55倍へ縮小する。面積比が0.3程度になるためIoUベースの重複除去（>0.5）では統合されず両方残る。手動追加ROIは対象外。
  - `dropOversizedGenitalROIs`: 性器ROIの面積が、それを最もよく含む人物領域の12%を超える場合に誤検出として除去する。実測では正しいROIが人物領域の約1.8%、寝具の陰影の誤検出が約17%だった。人物未検出・手動追加ROI・性器以外のカテゴリは対象外（部位のクローズアップ画像を壊さないため）。
- 候補カテゴリのグリッドを4行へ再構成（顔領域 / 胸部 / 性器 / その他）。
- テスト追加（計124件）: 乳首/乳輪の分割（縮小率・中心一致・IoU・ID重複）、手動ROIの非分割、過大性器ROIの除去と適用対象外の3条件、カテゴリ索引の固定。

## v0.0.00099 - 2026-07-29

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 性器を検出できた画像で、骨格から推定した**不要な「その他」ROI（人数分）**が生成されなくなった。3人検出時に「その他1〜3」が並び、うち1件がコマ全体を覆う問題を解消。
- バグ修正: 「対象形状（SAM）」で、楕円の選択範囲なのにモザイクが四角く出ることがある問題を修正。表示されている選択範囲の形状どおりに切り取るようにした。
- バグ修正: 設定の初期化後にインスペクタの幅が足りず、「性器（男性）」が「性」と表示されるなど項目名が省略され、モザイクパターンのタイルも横に切れていた問題を修正。
- バグ修正: 設定の初期化で「検出」「ワークフロー」「レイヤ表示」が既定値に戻らず、レイヤ表示が全OFFのままになる問題を修正。初期化後はROIとモザイクの表示がONで始まる。
- バグ修正: ライブラリ操作アイコンの右端が切れる問題を修正（4個ずつ2行へ折り返し）。
- 改善: 選択範囲・人物・骨格をすべて非表示にしている間は「詳細: 輪郭 / タグ」を無効表示にする。
- 改善: 選択範囲が0件のとき「モザイク対象」、画像未読込のとき「画像」をレイヤ一覧に出さない。
- 改善: 画像を開いていない間は候補生成・モザイク適用・レイヤ削除・ズームを無効表示にする。
- 改善: レイヤ一覧のスクロールバーを内容が収まっている時は隠す。塗りつぶし色の見本が横に伸びないよう固定サイズにした。キャンバスの背景色を純黒から標準の台紙色へ変更。

■更新履歴

- `PoseDerivedROIFilter`（`MosaicCore`）を新設し、骨格由来プライアの除去を純ロジックとして切り出してテスト可能にした。`dropGroinPriors`は検出器が性器を1件でも検出した場合に`source == "pose-groin"`を除去する（`.other`カテゴリのためIoUベースの重複除去では統合されず残っていた）。乳首側の`dropChestPriors`も同所へ移設。
- `ShapeSegmentEngine.hardShapeMask(for:extent:)` / `hardEllipseMask` を追加。`SAMSegmentEngine`の制限を矩形から**ROI形状の二値マスク**へ変更した。`ellipseMask`は放射グラデーションのため輪郭が縁で薄まる（§5.41）が、二値なら起きない。
- `inspectorPaneDefaultWidth`（360）と`defaultLeftPaneWidth(_:)`を追加し、`applyInitialLayoutIfNeeded`のハードコード280ptを置き換え。インスペクタの最小幅340ptを下回らないようにした。
- `libraryTwoColumnPaneWidth()`に下限340ptを追加（表示モード切替とズーム/更新アイコンが省略されない幅）。
- `refreshAllUIFromSettings`へ`loadWorkflowOptions` / `loadDetectionSettings` / `loadLayerVisibilityDefaults`を追加。`updateLayerDetailToggleAvailability()`を新設し表示トグルの変更時にも追従させた。
- `rootLayerItems()`をROI件数・画像有無でフィルタし、`numberOfChildrenOfItem`を同関数の件数へ統一（別計算による不整合を防止）。
- `updateImageActionAvailability()`を追加し、`updateUndoRedoAvailability()`から呼ぶ。
- テスト追加（計119件）: 二値形状マスクの楕円内部・角の値、鼠径部/胸部プライア除去の条件分岐。

## v0.0.00098 - 2026-07-29

■更新履歴（Readme / ChangeLog 用）

- 追加: マスク生成に「**対象形状（SAM）**」を追加。MobileSAM（Segment Anything の軽量版）を同梱し、検出した枠の内部から**対象の形状マスクを自動で直接取得**する。学習・アノテーション等の手作業は一切不要。
- 漫画風の合成線画での実測で、形状一致度（IoU）0.99（現状の枠塗り方式は0.56）。検出枠が1.4倍に大きい場合でも0.65（枠塗りは0.31）。
- 完全ローカル実行（画像の外部送信なし）。モデルはMobileSAM（Apache-2.0）のONNX変換版（MIT）を同梱。
- 既定のマスク生成方式は変更していない（GUI確認後、既定化はユーザー判断で実施）。

■更新履歴

- `SAMSegmentEngine` / `SegmentEngineKind.samShape` を追加。エンコーダで画像埋め込みを作り、ROI枠を「左上点(label=2)+右下点(label=3)」プロンプトとしてデコーダに与え、元画像サイズのマスクlogits（>0が対象）を得る。
- エンコーダ入力は`(H,W,3)` float32 RGB 0〜255。**リサイズはモデル外**のため長辺1024へ揃える（Python事前検証でリサイズ省略時に座標系がずれてIoU 0.31まで落ちることを実測し、原因を特定済み）。
- 画像埋め込みは直近1枚をキャッシュし、プレビュー再生成時はデコーダのみ実行（実測: 初回1.3秒→2回目0.6秒）。ORTセッションはプロセス内共有。
- ROI矩形（回転対応・二値）で制限し、被覆率0.02未満（ほぼ空）は図形へフォールバック（検閲漏れ防止）。回転ROIは回転後の外接矩形をプロンプトにする。
- 実モデルを通したend-to-endテストを追加（計114件）: 漫画風合成線画で検出誤差込みの枠からIoU>0.8、埋め込みキャッシュの短縮効果。
- `Package.swift`リソースへ`sam_encoder.onnx`（28MB）/`sam_decoder.onnx`（16MB）を追加、`THIRD_PARTY_NOTICES.md`へライセンス記載。

## v0.0.00097 - 2026-07-29

■更新履歴（Readme / ChangeLog 用）

- 追加: マスク生成に「**学習モデル形状**」を追加。部位の輪郭を学習したモデル（YOLOセグメンテーション）で、性器・乳首の形状に沿ったマスクを作れるようになった。
- 追加: ライブラリ＞「形状学習用データセットを書き出す」。多角形で輪郭をなぞった選択範囲が、そのまま形状モデルの教師データになる。
- 学習手順は `Docs/FINETUNE_GUIDE.md`「部位セグメンテーション（形状）モデルの学習」を参照。モデルは同梱せず、各自で作成して導入する。
- 既存のマスク生成方式・既定値は変更していない。モデル未導入時は従来どおり動作する。

■更新履歴

- `YOLOSegDecoder` を新設。YOLOv8/YOLO11-seg の2本出力（`output0`=枠+マスク係数、`output1`=マスクプロトタイプ）をデコードし、`sigmoid(Σ 係数×プロトタイプ)` でマスクを合成する。枠の外は0にする（ultralyticsの`crop_mask`相当）。
- `PartSegmentationDetector` を新設。`Application Support/newMosaic/Models/part_seg.onnx` を読み、部位ごとの形状マスクを返す。レターボックスのパディングを除去して元画像サイズへ戻す。出力が1本（検出専用モデル）の場合は明示的なエラーにする。
- `LearnedShapeSegmentEngine` と `SegmentEngineKind.learnedShape` を追加。ROIとのIoU 0.2以上で対応付け、同カテゴリを優先。ROI矩形（回転対応・二値）で制限する。モデル未導入・推論失敗・被覆率0.02未満のいずれも `ShapeSegmentEngine` へフォールバックし、検閲漏れを作らない。
- `YOLOSegDatasetExporter` を新設。1行 `class x1 y1 x2 y2 ...` の輪郭形式で書き出す。既定では手描き多角形ROIのみ（楕円・矩形は形状学習に有害なため除外）。回転は画素空間で適用する。
- クラス順は `PartSegmentationDetector.classCategories`（nipple / maleGenital / femaleGenital）で書き出し側と実行側を統一し、テストで固定。
- テスト追加（計112件）: マスク係数のオフセット、枠外の非塗り、sigmoidしきい値、検出専用モデルを渡した場合の空返却、レターボックス除去、モデル未導入時のフォールバック、輪郭書き出しの座標・回転・クラス順。

## v0.0.00096 - 2026-07-29

■更新履歴（Readme / ChangeLog 用）

- 調査: 「対象形状」の性器マスク品質について、初期実装との差分を実測で検証した。アルゴリズムは変更していない。

■更新履歴

- 被覆率の測定色空間としきい値の対応関係を実測テストで固定（`CoverageCalibrationTests`, 計99件）。実面積比0.80/0.83がsRGB測定では0.906/0.922と読め、初期実装のしきい値0.85を超えることを数値で確認した。
- 調査結果を `Mosaic/ARCHITECTURE.md` §5.41 へ記録。要点は以下。
  - 性器ROIのジオメトリは初期実装から不変（検出器の`detection.rect`そのまま・`rotation`なし・拡大なし）。自動回転が`rotation`を付けるのは`pose-chest`と`pose-groin`のみで、性器ROIには影響しない。`LearningEngine.refineCandidates`は`confidence`のみ変更し`rect`は変えない。
  - v0.0.00091の「初期実装・比較用」エンジンは当時の忠実な再現だった（sRGB測定・しきい値0.85・顕著領域マスク・二値矩形クリップがすべて一致）。それでも品質が改善しなかったため、**戻すべきアルゴリズムの差分は存在しない**。
  - 初期実装は性器クロップに対し前景マスクを実質的に使っておらず、常に顕著領域ヒートマップ（ソフトなグラデーション）へ切り替わっていた。形状制限の乗算も無かった。
  - v0.0.00093のガンマ修正で被覆率をリニア化した際、しきい値0.85を据え置いたため、切替が起きる実面積比が0.69→0.85へ変化した（比較用エンジンがv0.0.00091と異なる挙動になっている）。

## v0.0.00095 - 2026-07-29

■更新履歴（Readme / ChangeLog 用）

- 追加: 「検出」の先頭に「個別」チェックを追加。ONのとき、マスク生成方式・形状しきい値の変更は**選択中のレイヤにだけ**適用され、他レイヤのマスクとモザイク状態は変化しない（既定ON）。
- 改善: マスク生成の値を変えても、他レイヤのモザイクが作り直されなくなった。レイヤごとに別々のマスク生成方式を持てる。
- 改善: 既定値を変更。レイヤの「モザイク」表示ON、ワークフローの「自動候補生成」「自動保存」ON。

■更新履歴

- `MosaicROI`へ`maskEngine`（`SegmentEngineKind`のrawValue）と`maskThreshold`を追加。nilは全体設定を継承する。旧ライブラリJSONは当該キー欠落でnil（継承）として読み込む。
- `PerROISegmentEngine`を新設。個別設定を持つROIは同じ方式・しきい値ごとにまとめて専用エンジンへ、持たないROIは全体設定のエンジンへ振り分ける（ROIごとに推論を呼ばないためのグルーピング）。
- 「個別」ON時、マスク生成方式・形状しきい値の変更を選択中ROI（複数選択時はその全件）へ書き込み、Undo対象として再生成する。全体設定（AppSettings）は書き換えない。
- レイヤ選択時に、そのROIの個別設定をインスペクタ表示へ反映する（表示と書き込み値の不一致を防ぐ）。
- `Layer.mosaicVisibleDefault`（既定true）を追加し、画像読込直後からモザイク表示で開始する。ユーザーのトグル操作を既定値として記憶する。
- `Workflow.autoGenerate` / `Workflow.autoSave` の既定値をfalse→trueへ変更。
- テスト追加（計97件）: ROI個別マスク設定の保存・復元と旧JSONの継承読込、`SegmentEngineKind`のrawValue往復。

## v0.0.00094 - 2026-07-28

■更新履歴（Readme / ChangeLog 用）

- 改善: 「対象形状」に**輪郭線で囲まれた領域を塗る方式**を追加し、最優先で使うようにした。漫画は対象部位が輪郭線で囲まれて描かれるため、選択範囲の中心から線を越えない範囲へ広げることで、描かれた形そのものをマスクにする。
- これにより、前版の「描き込み密度」方式で起きていた「周囲の陰影・しずくなど線の多い別領域まで巻き込む」問題を解消する。輪郭を越えて外へ漏れた場合や、線が無い（写真等の）場合は自動的に従来方式へ切り替わる。

■更新履歴

- `inkBoundedRegionMask`を新設: クロップをグレースケール化し、明暗レンジに対する相対しきい値（レンジの45%）で線を判定、ROI中心から4近傍で領域拡張（塗りつぶし）する。囲っていた輪郭線もモザイク対象へ含めるため結果を1画素太らせる。明暗差が24未満（線が無い）の場合はnilを返す。
- 候補の評価順を「輪郭線領域 → 前景抽出 → 描き込み密度 → 顕著領域」の優先順とし、有効帯（0.03〜0.64）に入る最初のものを採用する方式へ変更（前版の「被覆率最小を採る」から変更。線画では輪郭線領域が意味的に最も正しいため）。
- 診断ログへ`boundedCoverage`を追加。
- テスト追加（計95件）: 閉じた円の内側だけが塗られ外側の無関係な線を巻き込まないこと、平坦な画像では有効帯で弾かれること。

## v0.0.00093 - 2026-07-28

■更新履歴（Readme / ChangeLog 用）

- バグ修正（重大）: 小さい選択範囲で**モザイクが一切掛からない**ことがある不具合を修正した。輪郭抽出が何も返せなかった場合に空のマスクをそのまま使っていたため。以後は必ず図形ベースへフォールバックし、選択範囲が塗られないまま出力されることはなくなった。
- バグ修正: マスクの「被覆率」の測定値が実際の面積より大きく出ていた（白60%のマスクが0.80と測定される）不具合を修正した。この値でマスク採否を判定していたため、判定が意図どおりに働いていなかった。
- 改善: 「対象形状」の輪郭抽出を作り直した。漫画・イラスト向けの**描き込み密度による抽出**を追加し、従来の前景抽出・顕著領域と合わせて3方式を試したうえで、**最も対象を絞り込めているマスクを自動採用**する。選択範囲を丸ごと塗るだけの結果（＝輪郭が取れていない）は採用しない。
- 背景: 実機のデバッグログで、Visionの前景抽出が選択範囲のほぼ全面を「対象」と返していること（＝体の内部にある部位は背景が無いため分離できない）が判明したため。

■更新履歴

- `coverageRatio`のレンダリングをリニア色空間へ変更（sRGB読み出しではガンマ補正で面積比が過大に出ていた。白60%が0.80と測定されていた）。
- `inkDensityMask`を新設: グレースケール→`CIEdges`→`CIMorphologyMaximum`＋ブラー（短辺/20の半径）で線を面へ広げ→`CIColorThreshold`で密度の高い塊を抽出する。漫画では対象部位が周囲の平坦な肌より線・網点が密という性質を利用する。
- マスク採用ロジックを「前景→（全面なら）顕著領域」の先着順から、3候補を作って**有効帯（`minimumUsableCoverage`=0.03〜`maximumUsableCoverage`=0.64）に入るもののうち被覆率最小**を採る方式へ変更。上限0.64は「マスクがROI面積の85%以下」に相当する。
- 診断ログを `fgCoverage`／`inkCoverage`／`salCoverage`／`selected` の形式へ変更し、どの方式が採用されたか分かるようにした。
- テスト追加（計93件）: 合成線画で描き込みの密な領域だけがマスクされること、空マスク・全面マスクが有効帯で弾かれること。

## v0.0.00092 - 2026-07-28

■更新履歴（Readme / ChangeLog 用）

- 追加（調査用）: マスク生成「対象形状」がどう判断したかをデバッグログへ記録するようにした。ヘルプ＞デバッグ＞デバッグログで、選択範囲ごとに「切り出しサイズ／回転角／前景抽出できたか／被覆率／顕著領域へ切り替えたか／図形へフォールバックしたか」が確認できる（画像内容は一切記録しない）。
- これにより「輪郭が取れずに選択範囲を丸ごと塗っている」のか「輪郭は取れているが制限方法で欠けている」のかを、推測ではなく実際の動作で切り分けられる。

■更新履歴

- `SegmentEngine.swift`へ`segmentLogger`（subsystem `com.yoshikawa.newMosaic` / category `SegmentMask`）を追加。現行・初期実装の両エンジンで、ROIごとに `crop`／`rotation`／`foreground`／`fgCoverage`／`saliency`／`finalCoverage` と、図形フォールバック時の理由（`noMask`／`cropTooSmall`）を記録する。
- 記録内容は数値・分岐名のみ。画像・ROI座標・ファイル名は含めない（DEBUG_LOG_INVENTORYのプライバシー方針を踏襲）。

## v0.0.00091 - 2026-07-28

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 「対象形状（初期実装・比較用）」で、傾けた選択範囲に対してマスクが軸並行の四角いブロックになる不具合を修正した。初期実装は選択範囲の回転機能が無かった時代のコードで、回転を一切考慮していなかったのが原因。傾いた選択範囲でも正しく追従するようにした。
- ご指摘のとおり、範囲の回転対応がマスク品質に影響していた。回転ROIでは (1) 切り出し範囲が実際の（傾いた）選択範囲から外れて対象物がはみ出す (2) 最終の切り取りも無回転の矩形で行われる、の2点で品質が落ちていた。

■更新履歴

- `LegacyRegionForegroundSegmentEngine`が`roi.rotation`を考慮するよう修正。クロップは回転後の外接矩形（`rotatedBoundingBox`）基準へ、最終制限は`ShapeSegmentEngine.rectangleMask(rect:extent:rotation:)`（**二値**の回転矩形マスク）による乗算へ変更。
  二値マスクのため、現行の楕円マスク（放射グラデーション）と違い縁でマスクが薄くならず、初期実装の「対象物の輪郭がそのまま残る」性質は維持される。
- 回帰テスト追加: 45度回転した矩形マスクが、無回転では範囲外だった斜め方向を含み、無回転では範囲内だった横方向を除外すること、かつ中間調を持たない二値であることを実測（`rotatedRectangleMaskFollowsROIRotation`。計91件）。

## v0.0.00090 - 2026-07-28

■更新履歴（Readme / ChangeLog 用）

- 追加（検証用）: マスク生成の選択肢へ「対象形状（初期実装・比較用）」を追加した。リリース初期（Build 41相当）の処理をそのまま再現しており、現在の「対象形状」と切り替えて同じ画像で見比べられる。
- 調査: 「対象形状の精度が初期より悪い」件について、初期実装との差分が**最終段の制限方法だけ**であることを特定した。初期実装はROIの矩形でマスクを切るのに対し、現在はROIの形状（楕円は中心から縁へ向かって薄くなる放射グラデーション）を掛けているため、**楕円ROIでは対象物の輪郭が縁に近いほど欠ける**。検出器が作るROIは既定が楕円のため、性器・乳首など縁いっぱいまで対象物が写るケースで影響が出やすい。差が実際に出ることを実測テストで恒久化した。

■更新履歴

- `SegmentEngineKind.regionForegroundLegacy`と`LegacyRegionForegroundSegmentEngine`を追加。クロップ範囲・前景抽出・顕著領域の処理は現行と共通（v0.0.00083で当時と同一へ復元済み）で、最終段のみ当時の`cropped(to: roiRect)`（矩形クロップ）を再現する。
- 回帰テスト追加: 楕円ROIの隅で現行方式（`ShapeSegmentEngine.restrict`）は輝度100未満まで減衰し、矩形クロップ方式は200超を維持することを実測（`ellipseShapeRestrictionAttenuatesMaskNearROICorners`）。初期実装エンジンの契約テストも追加（計90件）。

## v0.0.00089 - 2026-07-27（動画対応 V4: 書き出しUI・音声対応）

■更新履歴（Readme / ChangeLog 用）

- 新機能: モザイクを適用した動画を書き出せるようにした（動画メニュー＞動画を書き出す／⇧⌘E）。保存先を選ぶと、キーフレームのROIを起点に**その間のフレームは自動追随**しながら全編へモザイクが適用される。
- 新機能: 書き出し中は進捗シート（進捗バー・%表示）が出て、いつでもキャンセルできる。キャンセルすると中途半端な出力ファイルは残らない。
- 新機能: **音声を保持して書き出せるようになった**（V1〜V3の「音声なし」制約を解消）。音声は再エンコードせずそのまま複製するため、音質が劣化しない。
- これで動画対応（V1エンジン基盤→V2ライブラリ→V3編集→V4書き出し）が一通り揃った。

■更新履歴

- `VideoMosaicExporter.export`へ`includeAudio`引数（既定true）を追加し、`AudioPassthrough`ヘルパーで音声トラックを`outputSettings: nil`（無変換）で読み書きする。待機は全てタイムアウト付き（無期限待機禁止の原則を踏襲）。
- `exportVideoWithMosaic`（⇧⌘E）を追加。`roiProvider`内でキーフレーム区間が変わったら`VideoROITracker.start`し直し、区間内は`track`で追随させる（キーフレーム＝追跡の起点）。
- 進捗シート（`makeVideoExportProgressSheet`）・キャンセル（`CancellationFlag`）・完了処理（`finishVideoExport`）を追加。キャンセルはエラー扱いにせずステータス表示のみ。
- 音声トラックの有無に関わらず書き出しが壊れないこと、`includeAudio: false`でも書き出せることの回帰テストを追加（計88件）。

## v0.0.00088 - 2026-07-27（動画対応 V3: キーフレーム編集UI）

■更新履歴（Readme / ChangeLog 用）

- 新機能: ライブラリの動画を開くと、その時刻のフレームがキャンバスへ表示され、**静止画とまったく同じ操作**（自動候補生成・手動追加・モザイク設定）でROIを編集できるようになった。
- 新機能: キャンバス下部にタイムラインを追加した（動画編集中のみ表示）。シークバーで任意の時刻へ移動でき、現在時刻・尺・キーフレーム件数が確認できる。
- 新機能: キーフレーム操作を追加した。「キーフレーム追加」（⌘K）で現在のフレームのROIを確定保存し、「キーフレーム削除」「前/次のキーフレームへ」で行き来できる。
- 新機能: 「追跡を確認」（⌘T）で、直前のキーフレームから現在の時刻までROIが自動追随した結果を表示する。見失ったROIは黄色い破線で警告表示され、直前の位置を保持したままなので、その場で位置を直して新しいキーフレームとして確定できる。
- 新機能: メニューバーへ「動画」メニューを追加（プレビュー再生／キーフレーム操作／追跡確認）。
- 内部: キーフレームのROIは動画本体を再エンコードせずに保存される（書き出しはV4で対応）。

■更新履歴

- キャンバスを`canvasContainer`（キャンバス＋タイムライン）へ包み、メイン分割へはコンテナを入れる。静止画ではタイムラインを`isHidden`にするため従来のレイアウトと見た目は同一。分割の最小/最大幅判定もコンテナ基準へ更新。
- 動画編集状態（`currentVideoItem`/`currentVideoInfo`/`currentVideoEditState`/`currentVideoTimeSeconds`）と、`openVideoForEditing`/`exitVideoEditingMode`/`seekVideo(to:reason:)`/`addVideoKeyframe`/`removeVideoKeyframe`/`jumpTo*Keyframe`/`runTrackingPreview`を追加。
- ショートカット: `addVideoKeyframe`（⌘K）・`runTrackingPreview`（⌘T）ほかを`AppShortcut`レジストリへ登録し、タイムラインのボタン・メニュー・ヘルプ一覧へ自動反映。
- 追跡プレビューは`VideoROITracker`をバックグラウンドで直前キーフレーム→現在時刻まで進め、`readFrames`のハンドラから内部シグナル（`TrackingPreviewStop`）を投げて目標フレームで走査を打ち切る。
- `ImageCanvasView.trackingLostROIIDs`と`drawTrackingLostWarning`を追加（黄色破線の警告枠）。
- フレーム取得・動画情報取得は全てバックグラウンド実行し、UI更新のみメインへ戻す。

## v0.0.00087 - 2026-07-27（動画対応 V2: ライブラリ統合）

■更新履歴（Readme / ChangeLog 用）

- 新機能: 動画をライブラリへ登録できるようにした（ファイル＞動画を開く…／⇧⌘O）。動画本体はコピーせずリンク登録するため、ライブラリ容量が増えない。
- 新機能: ライブラリ一覧に動画が静止画と並んで表示される。動画は🎬バッジ・代表フレームのサムネイル・尺（分:秒）付きで表示され、一目で区別できる。
- 新機能: 動画のプレビュー再生ウィンドウを追加した（再生/一時停止・シークバー・時刻表示）。ライブラリの「動画をプレビュー再生」ボタン、または動画項目を開く操作で表示される。
- 内部: 動画のキーフレームROIはライブラリ配下の別ファイルへ保存する方式とし、既存の静止画ライブラリのデータ形式は一切変更していない（既存ライブラリはそのまま読み込める）。

■更新履歴

- `MosaicLibraryItem`へ`kind`（image/video）・`videoDurationSeconds`を追加。カスタム`init(from:)`で既存JSON（両フィールドなし）を`.image`として読む後方互換を担保（回帰テスト2件追加）。
- `LibraryEngine.importLinkedVideo(url:pixelWidth:pixelHeight:durationSeconds:)`／`attachProcessedVideo(relativePath:for:)`を追加。
- `NewMosaicApp`が`MosaicVideoKit`へ依存（Package.swift）。`openVideo`（⇧⌘O）・`previewSelectedVideo`をショートカットレジストリへ登録し、メニュー／ライブラリ操作行へ追加。
- `VideoPreviewView`（`AVPlayerLayer`＋自前コントロール）を新設。AVKitの`AVPlayerView`はSwiftPM実行ファイルからリンクできない（SwiftUICore依存）ため使用しない。
- 動画情報の取得・ライブラリ登録はバックグラウンドで実行し、UI更新のみメインへ戻す。サムネイルは`VideoThumbnailProvider`経由。ライブラリ削除時は`VideoEditStore`のサイドカーJSONも破棄する。

## v0.0.00086 - 2026-07-27

■更新履歴（Readme / ChangeLog 用）

- バグ修正: ウィンドウの大きさ・位置が、ズーム（緑ボタン／タイトルバーのダブルクリック）で変更した場合に保存されなかった問題を修正した。終了直前にも必ず保存する。
- バグ修正: 外部ディスプレイを外した後などに、保存された位置のままウィンドウが画面外に開いて操作できなくなる問題を修正した（現在の画面の可視領域へ自動的に収める）。
- バグ修正: 「自動候補生成」「自動保存」の設定が再起動で既定へ戻ってしまう問題を修正した（他の設定と同様に保存されるようにした）。
- バグ修正: サイドパネルを広げると「候補カテゴリ」のチェックボックスの間隔だけ不自然に広がる問題を修正した。
- バグ修正: レイヤ／ライブラリパネルのタイトルの左端が、下の項目の左端と4ptずれていた問題を修正した。
- バグ修正: インスペクタを含むサイドパネルを狭めすぎるとモザイクパターンのタイルがはみ出す問題を、そのペインの最小幅を広げて解消した。
- 改善: 「候補カテゴリ」「表示レイヤ生成」の見出しが他の見出しと同じ体裁になり、テキストサイズ変更にも追従するようにした。「並行揺れ」「形状しきい値」の数値もテキストサイズ変更へ追従する。
- 改善: 数値スライダー全てにホバー時の説明を追加し、設定行ごとのヘルプの有無のばらつきを解消した。VoiceOverでも各スライダーの名称が読み上げられるようにした。
- 追加（動画対応V2の基盤）: 動画のキーフレームROIを保存する仕組みと、動画サムネイル生成を追加した（既存の静止画ライブラリのデータ形式は一切変更していない）。

■更新履歴

- コードレビュー2件（状態保存・復元／サイドパネルUI統一性）を各3パスで並列実施し、計13件を検出。P1×3・P2×7を修正、3件は理由を明記して見送り（詳細は `Docs/QC/CodeReview/QC_CodeReview_v0.0.00086.md`）。
- `AppDelegate`: `windowDidResize`追加、`applicationShouldTerminate`でウィンドウ枠を明示保存、`frameClampedToVisibleScreen`で復元時に可視領域へクランプ。
- `Workflow.autoGenerate`/`Workflow.autoSave`を新設（`workflowOptionChanged`/`loadWorkflowOptions`）。
- `categories`グリッドへ幅上限、Layer/Libraryパネルのタイトル左右マージンを8ptへ統一、`minimumWidth(forPane:)`でインスペクタを含むペインの最小幅を340ptへ。
- `inspectorHeading`経由へ統一（候補カテゴリ/表示レイヤ生成）、`textSizeChanged`の値ラベル配列へ2件追加、数値スライダーへtoolTip+`setAccessibilityLabel`、`inspectorRow`が行ラベルをVoiceOverラベルへ自動反映。
- `MosaicVideoKit`へ`VideoEditStore`（キーフレームROIのサイドカーJSON保存。index.jsonは無変更）と`VideoThumbnailProvider`（LRUキャッシュ付きサムネイル）を追加。テスト5件追加（計84件）。

## v0.0.00085 - 2026-07-27

■更新履歴（Readme / ChangeLog 用）

- 追加: 動画対応プラグイン基盤「MosaicVideoKit」を統合した。既存機能へ影響を与えない独立モジュールで、動画のフレーム読出し（AVFoundation）、ROIの移動追随（Vision物体追跡）、モザイク適用付き再エンコード（H.264、初期段階は音声なし）、キーフレーム検出+追跡パイプラインを提供する。UI統合（ライブラリでの再生・動画のROI認識操作）は次段階で行う。
- バグ修正: 上記モジュールの全テストスイート並列実行時のハング（無期限セマフォ待機によるスレッドプール枯渇）を、タイムアウト付き待機と動画テストの直列実行化で修正した。

■更新履歴

- `Sources/MosaicVideoKit/`（VideoFrameReader/VideoROITracker/VideoMosaicExporter/VideoDetectionPipeline）+ `Tests/MosaicVideoKitTests`（合成動画による5テスト）を追加（Sonnetサブエージェント作、worktreeで検証後にマージ）。`Package.swift`へターゲット追加。
- `VideoMosaicExporter`: `finishWriting`待機を60秒タイムアウト付きへ、`isReadyForMoreMediaData`スピンへ30秒デッドライン+writer状態チェックを追加（`writingFailed`エラー新設）。テスト側も同様のタイムアウト+`@Suite(.serialized)`化。

## v0.0.00084 - 2026-07-27

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 「対象形状」のモザイクが楕円ROIの外（回転矩形の四隅）まで広がる不具合を修正した。マスクの最終制限をROI形状（矩形/楕円+回転）へ戻した（Visionが塊しか返せない画像でのスピル防止を優先。輪郭が取れている場合は形状内の輪郭がそのまま残る）。なお範囲選択の回転対応自体は非回転ROIの認識に影響しない（回転外接矩形クロップは回転ROIのみ適用）。
- 追加: インスペクタ＞検出に補助設定「形状しきい値」を追加した（既定=自動）。自動一発の結果が広すぎる画像で、しきい値を上げるとマスクが締まる（対象形状のみ）。
- バグ修正: 複数人物のクロップ個別推論で、人物が矩形の大半を占める場合に背景まで巻き込んだ全面マスクになる問題を修正した。ほぼ全面のマスクは分離失敗として全体画像推論へ自動フォールバックする。
- バグ修正: サイドツールパネルの状態が終了時に保存されない・初期幅が画面全体を覆う問題を修正した。終了直前に分割位置を明示的に保存し、起動時にサイドペインがウィンドウの半分を超える場合は既定幅へ強制する。
- 開発中: 動画対応プラグイン基盤（MosaicVideoKit: フレーム読出し・ROI追跡・モザイク付き再エンコード）を既存機能から独立したモジュールとして並行開発中（Sonnetサブエージェントへ委任。次版で統合予定）。

■更新履歴

- `RegionForegroundSegmentEngine`: 最終制限を`rectangleMask`から`ShapeSegmentEngine.restrict`（ROI形状）へ変更。`maskThreshold`（0=無効、CIColorThresholdによる任意二値化）をイニシャライザ引数として追加し、UIの「形状しきい値」スライダー（AppSettings永続化）から供給。
- `AnimeSegmenter.personMaskByCrop`: クロップマスクの被覆率>0.92を分離失敗としてnilを返す`coverageRatio`判定を追加。アプリ側は全体画像1回推論（遅延計算）+`personMask`切り出しへフォールバック。
- `MosaicWindowController.saveSplitPositionsNow()`を新設し`applicationShouldTerminate`から呼び出し。`applyInitialLayoutIfNeeded`へサイドペイン幅がウィンドウ50%超の場合の既定幅強制クランプを追加。

## v0.0.00083 - 2026-07-27

■更新履歴（Readme / ChangeLog 用）

- バグ修正: マスク生成「対象形状」の精度低下を、実装当初（Build 41）のコードとの差分レビューで特定した2件の実装変更（v82の384px拡大入力・顕著領域マスクの二値化）を取り消して修正した。クロップ計算・顕著領域処理とも当初と同一の処理へ復元し、回転ROIのみ回転外接矩形を使う（v79の回転バグ修正は維持）。
- バグ修正: 複数人物が認識された場合に2人目以降（人物3等）のマスクが作成されない問題を修正した。全体画像1回の推論から、人物範囲毎にクロップして個別推論する方式へ変更（小さく写る人物も実効解像度が上がり検出されやすくなる）。
- バグ修正: 人物レイヤを「モザイク対象に追加」してモザイク処理すると人物矩形全体が塗られてしまう問題を修正した。人物由来のROIは選択中のマスク生成方式に関係なく、候補生成時の人物シルエットに沿ってマスクされる（レイヤ移動にも追従）。
- バグ修正: 範囲選択モードで人物・骨格レイヤをドラッグできない問題を修正した。範囲選択モードでは矩形内側のどこでも掴んで移動できる（編集モードは従来通り枠の帯のみ。内側はROI新規作成用）。

■更新履歴

- `RegionForegroundSegmentEngine`: クロップ範囲計算をBuild 41当初と一致させ（非回転ROIは`roi.rect.expanded(1.15)`直接）、v82の`upscaled`前処理と`CIColorThreshold`二値化を撤去。
- `AnimeSegmenter.personMaskByCrop(in:bounds:)`を新設（人物矩形を1.08倍でクロップ→個別推論→全体フレームへ配置→bounds制限）。候補生成のアニメ経路を全体1回推論から人物毎推論へ変更。
- `PersonLayerSegmentEngine`（Segmentingラッパー）を新設。source "person-layer-N" のROIへ未着色人物シルエット（`personMaskImages`。画像切替の退避/復元・レイヤ移動の平行移動にも対応）をROI矩形制限付きで適用し、他のROIは基エンジンへ委譲。`ShapeSegmentEngine.rectangleMask`をpublic化。
- `ImageCanvasView.mouseDown`: 実効モードが範囲選択のときは`detectionLayerHit`（全面）、編集のときは`detectionLayerEdgeHit`（枠帯）で人物/骨格レイヤを掴む。

## v0.0.00082 - 2026-07-26

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 人物マスクの範囲が鏡映位置になる・矩形外までマスクが表示される不具合を修正した。合成マスクによる実測テストでマスク合成ロジック自体は正しいことを証明し、真因は表示側（`NSImage.draw(in:from:...)`がflippedビューで上下反転を補正しない既知の挙動）と特定。表示側へ`respectFlipped: true`を明示し、v0.0.00081で行ったモデル出力の反転読み出しは誤修正だったため元へ戻した（回帰テストを恒久追加）。
- 追加: 人物/骨格レイヤを画像上で選択・移動できるようにした。枠線付近（±8px）をクリックすると選択（レイヤ一覧とも同期）され、そのままドラッグで移動できる。移動完了時にマスク・骨格ボーンも追従する（枠の内側はROI新規作成のため掴まない）。
- 改善: マスク生成「対象形状」の認識精度を向上した。小さい選択範囲はVisionの実効解像度が不足するため短辺384px以上へ拡大してから推論し、顕著領域マスクはしきい値で二値化して対象物の形がはっきり出るようにした。

■更新履歴

- `AnimeSegmenter.characterMask`の読み出し行方向を元（行0=上）へ復元。`Tests/MosaicCoreTests/AnimeSegmenterPlacementTests.swift`（`personMask`の配置・制限の回帰テスト）を新規追加（74件目）。
- `ImageCanvasView.drawDetectionLayers`のマスク描画を`draw(in:from:operation:fraction:respectFlipped:hints:)`へ変更。
- `ImageCanvasView`に`DetectionMoveState`/`detectionLayerEdgeHit`/`onDetectionLayerSelected`/`onDetectionLayerMoved`を追加。コントローラ側で`selectDetectionLayerInList`/`applyDetectionLayerMove`（`translatedMask`によるマスク平行移動、ボーン・関節点の平行移動）を実装。
- `RegionForegroundSegmentEngine`: クロップ短辺が384px未満なら整数倍拡大（`upscaled`）してからVisionへ入力。`saliencyMask`に`CIColorThreshold`（0.35）による二値化を追加。

## v0.0.00081 - 2026-07-26

■更新履歴（Readme / ChangeLog 用）

- バグ修正: レイヤ一覧のレイヤ名が右寄せ表示になる問題を再々修正した。テキストの寄せ設定に依存する方式を全て捨て、ラベルの幅自体を文字幅にしてチェックボックス直後（左側）へ配置する方式に変更（寄せ設定が環境要因で無効化されても見た目が左寄せになる）。
- バグ修正: 人物認識範囲（シルエット）が上下反転して表示されるデグレを修正した。アニメ用セグメンテーションモデルの出力行方向が想定と逆（行0=下）であることが今回の実測で確定したため、読み出しを上下反転した（コードレビューで「実測未検証」と指摘されていた項目）。
- バグ修正: マスク生成「対象形状」の精度低下（対象物の輪郭でマスクされない）を修正した。楕円ROIで対象物の輪郭マスクが楕円形に切り取られていたのが原因で、制限をROIの（回転含む）矩形範囲のみに変更し、対象物の実形状どおりのマスクへ戻した。
- 追加: フラッシュの種別に「ウニフラッシュ」（両端が尖った紡錘形の線をリング状に描く）を追加した（集中線/ベタフラッシュ/ウニフラッシュの3択）。
- 追加: 人物レイヤを右クリック→「モザイク対象に追加」で人物にもモザイク処理ができるようにした（マスク生成を「人物の輪郭」にするとシルエットに沿ってマスクされる）。
- 改善: レイヤ一覧の表示順をモザイク対象→人物→骨格→画像の順に変更した。
- バグ修正: モザイク詳細設定のスライダーと設定値を固定8ptマージンで隣接させた（鼠径部位置と同じ方式）。ボーダーのランダム/トーンも横並びにした。
- 改善: ツールバーを再配置した。リンク切れ修正・画像出力・Finderで表示はライブラリパネルの操作行へ移動（選択画像を削除は行末尾）、一括処理は自動候補生成の左、モード切替はやり直すの右のセパレータ後へ。「候補を生成」→「自動候補生成」、「リンク切れを修正」→「リンク切れ修正」に改名。

■更新履歴

- `LayerRowView`のラベルtrailing制約を`equalTo`→`lessThanOrEqualTo`に変更（フレーム幅=内容幅で左詰め配置）。
- `AnimeSegmenter.characterMask`のモデル出力読み出しを上下反転（`sourceRow = (size - 1 - (y + padY)) * size + padX`）。
- `RegionForegroundSegmentEngine.regionMask`の最終制限を`ShapeSegmentEngine.restrict`（ROI形状）から回転対応の`rectangleMask`（矩形範囲のみ）へ変更。
- `MosaicFlashKind`（line/beta/uni）を新設し`flashBeta: Bool`を置換（`flashBeta`保存済みJSON/UserDefaultsからの読み込み互換あり）。`flashLayer`にウニフラッシュ（ROIサイズ基準の半径帯に紡錘形を描画）を追加。
- 候補生成のレイヤ一覧ルート順を`rootLayerItems()`（ROI→グループ→人物→骨格→画像）で構築。
- レイヤ右クリックメニューへ「モザイク対象に追加」（人物レイヤ選択時のみ表示、`addPersonLayerAsROI`）。
- `styleGrid`の各スライダー行を`sliderValueRow`（スライダー+値のスタック、spacing 8）へ、ランダム/トーンを`borderOptionsRow`（spacing 12）へ変更。
- ツールバーから`repairLinksButton`/`saveButton`/`revealButton`を除去しライブラリパネルの操作行（openOriginal→openProcessed→repairLinks→save→reveal→exportDataset→delete）へ移設。`batchButton`を最初のセパレータ直後へ、`canvasModeControl`をundo/redo後のセパレータ右へ移動。

## v0.0.00080 - 2026-07-26

■更新履歴（Readme / ChangeLog 用）

- バグ修正: レイヤ一覧のレイヤ名が右寄せ表示になるデグレを再修正した（AppKitの設定順の癖でalignment指定が無効化されていたため、表示のたびに左寄せを強制する方式へ変更）。
- バグ修正: 乳首検出時に斜めの誤ROIが2箇所残る不具合を再修正した。部位検出モデルが乳首を1つでも検出できた場合、精度の低い骨格由来の推定乳首ROIを全て取り除くようにした（検出できなかった画像でのみフォールバックとして残す）。
- バグ修正: ツールバー「レイヤ削除」で人物・骨格レイヤが削除されない不具合を修正し、全レイヤを削除するようにした。
- バグ修正: 編集/範囲選択モードのツールバーアイコンが逆だったのを修正した（編集=矩形破線、範囲選択=カーソル）。画像上のカーソルもモードに応じて切替わる（編集=＋、範囲選択=矢印。Option(⌥)キーの一時切替にも即座に追従）。
- バグ修正: 編集モード時、画像上の人物・骨格レイヤをダブルクリックしてもレイヤ削除できない問題を修正した（モザイク範囲のダブルクリック削除と操作を統一）。
- 改善: ボーダーの「種別」を「方向」に改名。ランダムON時は斜めではなく「方向」設定（縦/横）に従い、帯の太さ・間隔のみをランダム化するようにした。「並行揺れ」スライダーを追加し、各線を線の中央を軸にランダムで傾けられるようにした。
- 改善: フラッシュに「種別」（集中線=黒線 / ベタフラッシュ=黒地に白）と「密度」（放射線の本数）を追加し、「トーン」ONで網点（漫画トーン風）処理が効くようにした。集中線の形はレイヤ毎に異なるランダム形状になる（同一レイヤの再表示では同じ形を保つ）。
- バグ修正: モザイクパターンの詳細設定は、パターン切替時に関連する項目のみ表示するようにした（「トーン」等の同名設定が複数並んで見える問題も解消）。
- バグ修正: インスペクタのスライダーと設定値の間の不要なマージンを解消した（設定値を左寄せに変更）。

■更新履歴

- `LayerRowView.configure`でNSAttributedString（段落スタイルで左寄せ・末尾省略を明示）を使用し、`lineBreakMode`設定によるalignmentリセットの影響を排除。
- 候補生成に`dropPoseChestPriors(from:ifDetectorFound:)`を追加し、検出器の乳首検出があれば`source == "pose-chest"`のROIを除去（アニメ/実写両経路）。
- `clearROIs()`が人物/骨格レイヤ（canvasの表示配列・レイヤ一覧）も含めて削除するよう拡張。
- `canvasModeControl`のアイコンを入替。`ImageCanvasView.applyHoverCursor`でモード（Option一時反転含む）に応じたカーソル（crosshair/arrow）を設定し、`flagsChanged`のローカルモニタで即時追従。
- `ImageCanvasView.detectionLayerHit(at:imageRect:)`と`onDetectionLayerDeleteRequest`を追加し、ダブルクリックで`deleteDetectionLayer(_:)`（レイヤ一覧からの除去+表示オフ）を実行。
- `MosaicStyle`/`MosaicROIStyle`へ`stripeWobble`/`flashBeta`を追加（Codableは`decodeIfPresent`で後方互換）。`randomStripeMask`をCGContext直接描画へ書き換え（方向対応・帯毎の傾き）、`flashLayer`をROI IDから導出したシード+先細り三角形+ベタフラッシュ+密度+CIDotScreenトーンへ書き換え。
- `styleGridView`参照と`updateStyleGridRowVisibility(pattern:)`でNSGridViewの行をパターン毎に表示/非表示。スライダー値ラベルを右寄せ→左寄せへ変更。

## v0.0.00079 - 2026-07-26

■更新履歴（Readme / ChangeLog 用）

- バグ修正: ライブラリのサムネイル拡大縮小アイコンが、画像側の拡大縮小アイコンとサイズ・見た目が異なっていた不具合を修正した（同じ描画方式に統一）。
- バグ修正: 乳首検出時、斜めに回転した誤ったROIが2箇所検出されることがある不具合を修正した。体の傾きに応じたROI自動回転（v0.0.00076）の際、中心位置まで傾き方向へずらしていたことで、直接検出結果との重複除去が効かなくなっていたのが真因。
- バグ修正: 選択範囲を回転した状態でマスク生成方式「対象形状」を使うと検知処理されない不具合を修正した（他のマスク生成方式は元々問題なし）。
- バグ修正: 初期化時にサイドパネルの既定配置により右パネルがアプリの大半を占めてしまう不具合を修正し、右＝「ライブラリ」＋「レイヤ」、左＝「インスペクタ」を既定配置にした。
- 新機能: モザイクパターンに「フラッシュ」（集中線）を追加した。選択範囲内で放射の中心位置を画像上のハンドルドラッグで指定できる。
- 改善: 「ボーダー（縦）」「ボーダー（横）」「ボーダーランダム」を1つの「ボーダー」パターンへ統合し、種別（縦/横）・ランダムON/OFF・トーンON/OFFの設定で切替えられるようにした。
- 改善: パターン表示名を変更した（「雲」→「トーン」、「トーン化（漫画トーン）」→「トーン」）。

■更新履歴

- `thumbSmallerButton`/`thumbLargerButton`を`configureToolbarButton`経路（`SquareIconButton`）で構築し直し、キャンバスズームボタンとサイズ・詳細設定「アイコンサイズ」への追従を統一。
- `SensitiveROIGenerator.chestROIs`のROI中心位置計算から、肩ラインの傾き方向への投影オフセット（`offsetUnit.x/y`）を削除し、水平オフセットのみに戻した（回転は`rotation`のみへ反映）。未使用になった`unitVector`ヘルパーを削除。
- `RegionForegroundSegmentEngine.regionMask`のクロップ範囲を、ROIの無回転矩形ではなく回転後の外接矩形（`rotatedBoundingBox(of:rotationDegrees:)`）基準に変更。
- `MosaicWindowController.defaultPanelSide(for:)`を新設し、`.inspector`のみ既定サイドを「left」に変更（他は従来通り「right」）。パネル配置の参照箇所（`movePanel`/`applyPanelAssignments`）を`panelSide(for:)`へ統一。
- `MosaicFillPattern`から`.stripesVertical`/`.stripesHorizontal`/`.stripesRandom`を削除し`.border`へ統合、`.flash`を追加。`MosaicStyle`/`MosaicROIStyle`へ`stripeVertical`/`stripeRandom`/`stripeTone`/`flashCenter`を追加。`MosaicROIStyle`に旧パターン名からの移行ロジックを持つカスタム`init(from decoder:)`を追加し、既存ライブラリ/プロジェクトJSONの読み込み互換性を維持。
- `MosaicEngine.flashLayer(style:roi:extent:)`を新設（放射状の集中線をCGContextで描画、固定シードで再現性を確保）。フラッシュはROIごとに中心位置が異なるため`layerCache`の対象から除外。
- `ImageCanvasView`にフラッシュ中心ハンドルのドラッグ操作（`FlashCenterDragState`）と描画（`drawFlashCenterHandle`）を追加。

## v0.0.00078 - 2026-07-26

■更新履歴（Readme / ChangeLog 用）

- 新機能: ツールバーに「編集モード/範囲選択モード」の切替を追加した。範囲選択モードでは画像上の空白部分をドラッグすると、重なる複数ROIを一括選択できる（レイヤ一覧の選択とも連動）。Option(⌥)キーを押しながらのドラッグで、そのドラッグ限定で一時的にモードを入れ替えられる。
- バグ修正: レイヤパネルのレイヤ名等が右寄せ表示になっていた不具合を修正し、左寄せに戻した。
- バグ修正: グループ化解除後に再度グループ化すると、グループ名の連番が過去使用した番号から増え続けてしまう不具合を修正した（使われていない最小の番号から採番するようにした）。
- 改善: グループ内の個別レイヤ/ROIだけを選択して「グループ解除」を押した場合、そのレイヤ/ROIだけをグループから除外するようにした（グループ名自体を選択した場合の全体解除は従来通り）。
- 改善: レイヤパネル「表示:」のラベルを短縮した（「人物検出」→「人物」、「骨格検出」→「骨格」、「モザイク表示」→「モザイク」）。表示順もROI・モザイク・人物・骨格の順へ変更した。
- 改善: インスペクタ＞検出のマスク生成方式の既定選択を「対象形状」に変更した。
- バグ修正: モザイクのノイズパターンがカラーノイズに戻ってしまう不具合を修正した。パターン切替時に色付け等の詳細設定が別パターンから引き継がれていたのが真因で、パターンごとに最後に使った詳細設定を記憶・復元するようにした。
- 改善: 「ライブラリを更新」ボタンをツールバーから、ライブラリパネルの拡大縮小アイコンの右側へ移動した。
- 改善: 「選択範囲をすべて消去」を「レイヤ削除」に改名し、ライブラリの「選択画像を削除」と区別できるよう別アイコンにした。

■更新履歴

- `ImageCanvasView`に`interactionMode`（`.edit`/`.marqueeSelect`）を追加。空白ドラッグ時、Option修飾で一時反転する`dragIsMarqueeSelect`を判定し、`mouseUp`でラバーバンド矩形と交差する既存ROIを`selectedROIGroupIDs`へ格納。`onROIGroupSelectionByMarquee`でレイヤ一覧選択（`syncROIListSelectionFromCanvasGroup`）へ同期。ツールバーに`canvasModeControl`（2segment）を新設。
- `LayerRowView.label.alignment`を明示的に`.left`へ設定。
- `groupSelectedLayers()`のグループ命名を、使用中の`roiGroupCounter`/`layerGroupCounter`（増え続けるカウンタ）から、既存グループ名を避けた最小連番を採番する`nextAvailableGroupName(excluding:)`へ変更。
- `ungroupSelectedGroup()`を拡張し、`LayerLeaf`/`ROIListEntry`単体選択時はそのレイヤ/ROIのみをグループから除外するよう分岐（`updateLayerContextMenu`の「グループ解除」表示条件も同様に拡張）。
- `LayerKind.title`の"人物検出N"/"骨格検出N"を"人物N"/"骨格N"へ変更（画像上のレイヤ名表示・チェックボックスラベルも統一）。
- `segmentEngineControl`の初期選択インデックスを`SegmentEngineKind.regionForeground`へ変更。
- モザイクスタイルの詳細設定（透明度・色付け・粒度等）をパターン毎にUserDefaultsへ名前空間分けして保存する方式へ変更（`mosaicStyleKeyPrefix(for:)`/`loadMosaicStyleDetails(for:)`）。パターン切替時（`patternTileClicked`）に該当パターンの記憶済み設定を復元してから適用する。
- `reloadLibraryFromButton`のツールバーボタンをライブラリパネルの`modeRow`（サムネイル拡縮アイコンの隣）へ移設。
- `clearROIs`のショートカット表示名を「レイヤ削除」に変更し、アイコンを`trash`から`rectangle.badge.minus`へ変更。

## v0.0.00077 - 2026-07-25

■更新履歴（Readme / ChangeLog 用）

- 品質改善: コードベース全体（MosaicCore・アプリ本体）のコードレビューを実施し、複数のバグ・潜在的な不具合を修正した。詳細は `Docs/QC/CodeReview/QC_CodeReview_v0.0.00077.md` を参照。
- バグ修正: 画像上でROIをドラッグ操作中に別の操作へ割り込まれた場合、次のドラッグで別のROIを意図せず移動・変形させてしまうことがある不具合を修正した。
- バグ修正: ライブラリの一括処理とROI保存等が同時に走った場合に、一方の更新が消えてしまう可能性があった競合状態を修正した。
- バグ修正: 「線・エッジぼかし」「アンシャープ（エッジ強調）」パターンで、画像の外周付近にエッジ検出の誤りが生じうる不具合を修正した。
- バグ修正: 人物セグメンテーション方式で、Vision処理が失敗した場合にモザイク処理全体が失敗してしまう不具合を修正し、他の検出方式と同様に図形ベースへ自動的に切り替わるようにした。
- 改善: パターン画像のキャッシュが際限なく増え続けないよう上限を設けた。
- 改善: 設定の保存に失敗した場合、これまでは無言で失敗していたが、デバッグログへ記録するようにした。

■更新履歴

- コードレビュー（5並列エージェントによる初回スキャン→実コード検証→修正→再検証の4パス）で計19件の指摘を検出、14件を修正、5件は設計変更を伴うため理由を明記して見送った。詳細一覧は `Docs/QC/CodeReview/QC_CodeReview_v0.0.00077.md`。
- `LibraryEngine`に直列`DispatchQueue`を導入し、全mutatingメソッド（`importOriginal`/`saveProcessedImage`/`deleteItems`/`importLinked`/`relink`/`repairBrokenLinks`）のread-modify-writeを排他制御。`HistoryEngine.append`/`LearningEngine.record`も同様に排他制御を追加。
- `MosaicEngine.makeFillLayer`の`.edgeBlur`/`.unsharpEdges`で`CIEdges`適用前のクランプ順序を修正。`SegmentEngine.VisionPersonSegmentEngine`のVision実行を`try?`へ統一。
- `MosaicStyle`へ`@unchecked Sendable`を付与、`NormalizedRect.init(_:imageSize:)`にゼロ除算ガードを追加。
- `main.swift`: `ImageCanvasView.mouseDown()`でジェスチャー状態を明示クリア、`patternImageCache`のLRU化、`AppSettings.persistNow()`のエラーログ追加、`outlineView(child:ofItem:)`のフォールバック安全化、`setWorkingImage`のforce-unwrap除去。
- `AnimePoseEstimator`にUnified Loggingを追加し、モデル出力名の不一致時に警告を記録。

## v0.0.00076 - 2026-07-25

■更新履歴（Readme / ChangeLog 用）

- バグ修正: デバッグログウィンドウを開く・更新するたびにアプリが固まる不具合を修正した（バックグラウンド実行に変更）。
- バグ修正: ツールバーアイコンの選択枠がアイコン形状に沿った正方形にならない不具合を、ネイティブのベゼル/フォーカスリングに依存しない自前描画方式へ変更して解消した。
- 新機能: レイヤ一覧でROIグループ（またはROI選択リストの複数選択）を選ぶと、画像上でもまとめて強調表示され、いずれか1つをドラッグするとグループ全体を一括移動できるようにした。
- バグ修正: レイヤパネルの幅が十分でもレイヤ名が省略表示される不具合を修正した。
- 改善: インスペクタ上部にあった「選択レイヤ情報」を下部ステータスバーへ移動し、「(既定設定を継承)」→「<継承>」、「(個別設定)」→「<個別>」と簡潔な表記にした。レイヤパネルのROI行にも、レイヤ名とモザイク種別の間に同じフラグを表示する。
- 改善: モザイクパターン名を変更した（「任意パターン画像」→「パターン画像」、「かぶせ画像」→「マスク画像」）。ボタン名も短縮した（「パターン画像を選択…」→「画像選択…」、「全レイヤへ適用」→「全レイヤ適用」）。
- 改善: 「モザイク表示」のON/OFFと、レイヤの「輪郭」「タグ」表示を、レイヤパネルの「表示:」設定へ統合した。輪郭/タグはレイヤ行毎の個別設定から全レイヤ一括設定へ変更した。
- 改善: 候補生成時、体や顔が傾いている場合にROIの形状（目元・眼窩下〜あご・乳首・鼠径部）を認識範囲の向きに合わせて自動回転するようにした。
- バグ修正: モザイク詳細設定（透明度等）の左マージンとスライダー-数値間の間隔が、サイドパネル幅に応じて広がる不具合を修正した（`styleGrid`の最大幅制約が不足していたのが真因）。
- 改善: ライブラリのテキスト/サムネイル表示選択時、意味を持たない表示拡大縮小アイコンをグレーアウトするようにした。

■更新履歴

- `refreshDebugLog()`/`exportDebugLog()` を `Task.detached` によるバックグラウンド実行へ変更し、`OSLogStore` の走査（システム全体スキャン）をメインスレッドから排除。あわせて取得範囲を1時間→10分、最大500件へ縮小した。
- `SquareIconButton`（`NSButton`サブクラス）を新設し、`isBordered = false` + `draw(_:)`内で`isHighlighted`時のみ`bounds`いっぱいに矩形ハイライトを自前描画する方式へ変更。`.texturedRounded`/`.shadowlessSquare`ベゼルやフォーカスリングはいずれも大きな正方形フレームへ追従しなかったことが繰り返しの不具合の真因だった。
- `ImageCanvasView.selectedROIGroupIDs: Set<UUID>` と `GroupMoveState` を追加し、`mouseDown`/`mouseDragged`/`mouseUp` へグループ一括移動を実装。`outlineViewSelectionDidChange` がROIグループ選択（複数の`ROIListEntry`選択も含む）を検知して同期する。
- `layerOutlineView.sizeLastColumnToFit()` を `splitViewDidResizeSubviews` と `reloadLayerList()` の両方で呼ぶよう変更。単一列の`resizingMask = .autoresizingMask`だけではサイドパネルの分割ドラッグに追従しないことがあった。
- `selectedLayerStyleLabel`（NSTextField）を廃止し、`selectedLayerStatusSummary: String?` + `updateStatsBar()`へ統合。`rebuildROIListEntries()`のROI行タイトルへ `<継承>`/`<個別>` フラグを追加。
- `MosaicEngine`の`.customImage`/`.overlayImage`表示名、`applyStyleToAllButton`/`stylePatternImageButton`のタイトルを変更。
- `mosaicPreviewCheckbox`をレイヤパネルの`togglesRow`へ移動。`layerOutlineAllCheckbox`/`layerTagAllCheckbox`（全レイヤ一括の輪郭/タグ表示）を新設し、`LayerRowView`から行毎の`outlineCheckbox`/`tagCheckbox`を削除（`LayerLeaf.showsOutline`/`showsTag`のモデル自体とcanvas描画側は変更なし、UIのみ一括操作化）。
- `SensitiveROIGenerator`（胸部/鼠径部ROI）に`tiltAngleDegrees`/`unitVector`ヘルパーを追加し、肩ライン/腰ラインの傾きをROIの`rotation`と横方向オフセットへ反映。`FaceRegionBuilder`にも`tiltAngleDegrees(leftEyePoints:rightEyePoints:)`を追加し、Vision顔検出（`FaceRegionDetector`）の目元/眼窩下〜あごROIへ適用（アニメ向けDWPose経由の`AnimePoseEstimator.faceRegionROIs`は左右の目キーポイントが分離されていないため未対応）。
- `styleGrid`（NSGridView）に`widthAnchor.constraint(lessThanOrEqualToConstant: 362)`を追加。NSGridViewは他のinspectorRow系の行と異なり内容から一意な幅が決まらず、親スタック内で伸縮してしまっていたのが左マージン不一致の真因だった。
- `thumbSmallerButton`/`thumbLargerButton`をインスタンスプロパティへ変更し、`updateLibraryModeVisibility()`で`libraryViewMode == .thumbnailGrid`のときのみ有効化。

## v0.0.00075 - 2026-07-25

■更新履歴（Readme / ChangeLog 用）

- 改善: バージョン表示方式を変更した。従来の `v0.0.00001 (beta Build N)` 表示を廃止し、以後は `v0.0.00075` のようにバージョン番号の末尾だけをリリース毎にインクリメントして管理する。
- 新機能: ヘルプ＞デバッグ＞デバッグログを追加した。アプリ内で発生したイベント（起動・エラー・一括処理結果・画像出力・プロジェクト読込等）とVision検出診断ログを、その場で一覧表示・書き出しできる。
- バグ修正: ツールバーのアイコン枠が、フォーカスリングの影響でアイコン形状に沿った正方形にならない不具合を修正した。
- バグ修正: サイドパネル幅を広げると、鼠径部位置スライダーやモザイクパターン詳細設定のスライダー・数値表示が際限なく広がる不具合を修正した。
- バグ修正: モザイク詳細設定の各項目の左マージンが、他の設定項目と異なりサイドパネル幅に応じてズレる不具合を修正した。
- 改善: モザイクパターンの選択を、プルダウンメニューからプレビューアイコンのタイル表示へ変更した。各タイルにマウスを乗せるとフローティングヘルプ（ツールチップ）が表示される。
- 改善: マスク生成の「対象の形状（ROI内前景）」を「対象形状」に短縮し、他の項目名もより分かりやすい一般名称へ変更した（図形／人物の輪郭（AI自動認識）／物体の輪郭（自動抽出）／対象形状）。
- バグ修正: レイヤ一覧でROI選択リストの行（目元・乳首等）を複数選択して「グループ化」しても常に無反応だった不具合を修正した。ROIのグループ化に対応し、右クリックメニューからも操作できるようにした。
- 改善: レイヤ一覧で人物検出/骨格検出レイヤの行を選択すると、画像上の対応する領域に強調枠を表示するようにした。
- 新機能: レイヤ一覧の右クリックメニューに「グループ化」（対象に応じて「再グループ化」表示）・「グループ解除」を追加した。
- 新機能: 画像上のROI（レイヤ）をCommandキーを押しながらドラッグすると、コピーを作成しながら移動できるようにした。
- バグ修正: 「かぶせ画像」パターンを選択した際、画像未選択のROIが1件あるだけで、他の設定対象でないレイヤのモザイク表示まで巻き添えで解除されてしまう不具合を修正した。
- 改善: パターン画像（かぶせ画像）の選択候補を、編集中レイヤのカテゴリに応じて優先的に表示するようにした。
- バグ修正: モザイク＞ノイズパターンがカラーノイズになっていた不具合を修正し、モノクロノイズへ変更した（プレビューアイコンも連動して修正）。

■更新履歴

- `CLAUDE.md` の運用ルールを更新し、`MARKETING_VERSION` 末尾を単一のrunning版数として管理する方式へ変更（`CFBundleVersion` は内部的に同期するがUI非表示）。`AppDelegate.windowTitle()` から `(beta Build N)` 表示を削除。
- `enum AppLog`（`Sources/NewMosaicApp/main.swift`）を新設し、`os.Logger`（subsystem `com.yoshikawa.newMosaic`、MosaicCore側のVision検出診断と統一）でUI/Library/Export/Projectの主要イベントを記録。`showDebugLogWindow()` が `OSLogStore(scope: .currentProcessIdentifier)` で直近1時間分を取得し一覧表示・テキスト書き出しする。
- `configureToolbarButton` に `focusRingType = .none` を追加。フォーカスリングが`.shadowlessSquare`の実フレームに追従せず横長に見える不具合の真因だった。
- モザイク詳細設定のスライダー（`styleOpacitySlider`等7種）に `lessThanOrEqualToConstant: 220` の必須最大幅制約を追加。`styleRowLabel(_:)` を新設し、`styleGrid`（NSGridView）のラベル列を `inspectorRow` と同じ最小幅78ptへ統一。
- `MosaicROI` に `roiGroupName: String?` を追加（Codable、後方互換）。`ROIListGroup` クラスを新設し、レイヤ一覧のROI選択リストをグループ表示できるように`NSOutlineViewDataSource`を拡張。`groupSelectedLayers()`/`ungroupSelectedGroup()` がROI選択リストの選択も処理するよう修正。
- `layerOutlineView.menu` に`NSMenuDelegate`ベースの右クリックメニューを追加（`attachLayerContextMenu()`/`updateLayerContextMenu(_:)`）。既存の「プロジェクト」最近項目用`menuNeedsUpdate`実装へ分岐を追加する形で統合。
- `ImageCanvasView.selectedDetectionLayer`（`fileprivate`、`LayerKind?`）を追加し、`outlineViewSelectionDidChange`で人物/骨格レイヤの選択を同期。`drawSelectedLayerHighlight(_:)` で強調枠を描画。
- `ImageCanvasView.mouseDown`のROIヒット時、`event.modifierFlags.contains(.command)`でROIを複製してから`moveState`を新IDに切り替える方式でCommand+ドラッグコピーを実装。
- `MosaicEngine.applyMosaic` に `skipIncompletePatterns: Bool = false` パラメータを追加。`true`時は画像未選択のROIをエラーにせずスキップする（プレビュー2箇所のみ有効化。「モザイクを適用」の最終書き出しは従来通り厳格にエラーとする）。
- `choosePatternImage()` を、選択中ROIのカテゴリに一致する素材を先頭にまとめる方式へ変更。
- `MosaicEngine.makeFillLayer`の`.noise`ケースに`CIColorControls(saturation: 0)`を追加しモノクロ化。
- `stylePatternPopUp`（NSPopUpButton）を状態保持用に非表示化し、`makePatternTileGrid()`によるプレビューアイコンタイル（`patternTileButtons`）をUIとして使用。選択状態は`updateMosaicStyleControlAvailability()`内の`refreshPatternTileSelectionHighlight()`で同期。

## v0.0.00001 - Build 74 - 2026-07-25

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 「初期化」後に右サイドパネル幅が既定値（ライブラリ2列表示相当）へ反映されない不具合の真因を、Build 73で追加した診断表示の実測値から特定し修正した。

■更新履歴

- Build 73で追加した診断表示（`main=958 leftHidden=true leftW=1 canvasW=200 rightW=756`）から、キャンバス幅が`constrainMinCoordinate`delegateで定義した最小幅の定数（200）ちょうどにクランプされていることが判明した。`NSSplitView.setPosition(_:ofDividerAt:)`はプログラムからの呼び出しでも`constrainMinCoordinate`/`constrainMaxCoordinate`delegateを経由して結果をクランプする仕様だが、この2つのdelegateはインタラクティブなドラッグ操作を想定して隣接ビューの「現在のフレーム値」を基準に計算するよう書かれていたため、初期化・復元などプログラムによる一括レイアウト設定中（隣接ビューのフレームがまだ更新途中）に適用されると、意図しない位置へクランプされていた。これがBuild 69〜72で右ペイン幅の既定化が繰り返し失敗していた真因だった。
- 既存の`isRestoringSplitPositions`フラグ（`splitViewDidResizeSubviews`の保存処理を抑制するために導入済み）を、`constrainMinCoordinate`/`constrainMaxCoordinate`delegateでも参照し、フラグが立っている間（プログラムによる一括設定中）はこの制約を適用しないようにした。あわせて、右ペイン幅を扱う全関数（`restoreSplitPositions()`・`setMainSplitRightPaneWidth()`・`applyPaneWidth()`・`applyPanelAssignments()`・`applyInitialLayoutIfNeeded()`）でこのフラグを設定・復元する際、ネストした呼び出し（例: `applyInitialLayoutIfNeeded()`が内部で`restoreSplitPositions()`を呼ぶ）で内側のdeferが外側のフラグを誤って`false`に戻してしまわないよう、「呼び出し前の値へ復元する」方式（`let wasRestoring = isRestoringSplitPositions; ...; defer { isRestoringSplitPositions = wasRestoring }`）へ統一した。

## v0.0.00001 - Build 73 - 2026-07-25

■更新履歴（Readme / ChangeLog 用）

- 調査用: Build 72でも右サイドパネル幅の不具合が解消しなかったため、推測での修正を一旦停止し、実測値を得るための一時的な診断表示を追加した（機能変更なし）。

■更新履歴

- 「初期化」実行後のステータスバー表示に、実測値（mainSplit幅・左ペイン非表示状態と幅・キャンバス幅・右ペイン幅・divider太さ・arrangedSubviews数）を一時的に付加した（`layoutDiagnosticsSummary()`）。原因特定後に削除する。
- `setMainSplitRightPaneWidth()` の各候補divider index試行前後の幅をNSLog（Console.appで確認可能）へ出力するようにした。

## v0.0.00001 - Build 72 - 2026-07-25

■更新履歴（Readme / ChangeLog 用）

- バグ修正: Build 71の修正でも「初期化」後に右サイドパネル幅が既定値へ反映されない不具合が再発していたため、根本的なアプローチへ変更して修正した。あわせて、レイアウト変更直後に終了して再起動しても変更が復元されない不具合も同じ原因だったことが判明し、解消した。

■更新履歴

- `mainSplitView`（左ペイン/キャンバス/右ペイン）で右ペイン幅を設定する処理は、これまで`arrangedSubviews.count - 2`という固定indexで右ペインの境界divider を特定していた。しかし左ペインが非表示（既定状態）の場合、AppKitが非表示ペインに対応するdividerを詰めて数え直す可能性があり、狙ったdividerに反映されないことがあった（Build 69・71と2回にわたり異なるindexへ修正を試みたが、いずれも理論上の推測にとどまり、実機確認ができず解消できていなかった）。
- 対策として `setMainSplitRightPaneWidth(_:mainSplit:rightPane:)` を新設。候補のdivider index（`count - 2`および`0`）を順に試し、実際に反映された幅を測定して一致すれば確定するという、AppKitの非公開の内部挙動に依存しない方式へ変更した。`restoreSplitPositions()`（保存値からの復元）・`applyPaneWidth()`（パネル移動時の幅引き継ぎ）・`applyPanelAssignments()`（非表示→表示切替時）・`applyInitialLayoutIfNeeded()`（既定レイアウト・保険チェック）の右ペイン幅を扱う全箇所をこの関数へ統一。
- 右ペイン幅の既定化処理が、右ペイン内部（ライブラリ/レイヤ/インスペクタ）の高さ配分用の`guard total > 520`ガードに巻き込まれて未実行になっていた導線も分離し、独立して確実に実行されるようにした。
- 「レイアウト変更直後に終了すると復元されない」件は、実際には保存自体は機能しており（Build 71のpersistNow()флush修正は妥当）、復元時に上記と同じdivider index不一致で正しく反映されていなかったことが真因だったため、今回の修正で解消される。

## v0.0.00001 - Build 71 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 「初期化」を実行しても右サイドパネルの幅（ライブラリ2列表示相当への既定化）が実際には反映されない不具合を修正した。
- バグ修正: アプリを終了すると、直前の設定変更（ウィンドウ枠・分割位置など）が保存されないまま失われることがある不具合を修正した。

■更新履歴

- `applyInitialLayoutIfNeeded()` の「保存値が無い場合の既定レイアウト」処理で、右ペイン幅の反映に使う `NSSplitView.setPosition(_:ofDividerAt:)` のdivider indexが常に`0`固定になっていた。左ペインが既定で非表示の状態では、divider 0は「（非表示の）左ペイン/キャンバス」の境界であり、右ペイン幅には一切影響しない（無効な操作）ため、実質的に右ペイン幅の既定化が機能していなかった。`mainSplit.arrangedSubviews.count - 2`（＝最後のdivider、キャンバス/右ペインの境界）へ修正。他のsetPosition呼び出し箇所（`restoreSplitPositions()`・`applyPaneWidth()`・`applyPanelAssignments()`のBuild 65対策）は元々この規約で実装済みだったため、影響はこの1箇所のみ。
- `AppDelegate.applicationShouldTerminate` で `AppSettings.shared.persistNow()` を明示的に呼び出すよう変更。`AppSettings` は連続書き込み対策として0.3秒デバウンスして保存するため、変更直後にアプリを終了すると、保存タイマーが発火する前にプロセスが終了し、直前の変更（ウィンドウ枠・分割位置等）がディスクへ書き込まれないまま失われることがあった。

## v0.0.00001 - Build 70 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- バグ修正: ファイル＞設定＞「初期化」を実行しても、サイドパネルの分割位置（幅）がリセット前のまま残り、確認ダイアログの「レイアウトも既定値に戻ります」という説明と食い違っていた不具合を修正した。プロジェクト読込時も同様に、読み込んだファイルにレイアウト情報が含まれない場合は工場既定レイアウトへ戻るようにした。

■更新履歴

- `refreshAllUIFromSettings()`（初期化・プロジェクト読込の両方から呼ばれる）内の `_ = restoreSplitPositions()` を `applyInitialLayoutIfNeeded()` へ置き換え。保存済みの分割位置が無い場合に工場既定レイアウト（右パネル=`libraryTwoColumnPaneWidth()` 等、Build 69で導入）へ確実にフォールバックするようにした。

## v0.0.00001 - Build 69 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- バグ修正: ツールバーのアクティブなアイコンの選択背景が、アイコンサイズより短い横長の帯として表示される不具合を修正した。
- バグ修正: 左サイドバーパネルを有効にしたままアプリを再起動すると、左サイドバーが表示されなくなる不具合を修正した。
- 改善: 左右どちらかにサイドバーパネルがない状態でパネルを移動すると、移動先の幅は移動元パネルの幅を引き継ぐようにした。
- 改善: 右サイドバーパネルの工場初期値の幅を、ライブラリのグリッド表示がちょうど画像2列になる幅へ変更した。
- 改善: 「テキストサイズ」設定を、現在のデフォルトサイズ（中）を基準としたまま、小・大の倍率を再調整した（小=0.8倍、大=1.25倍）。
- バグ修正: 「テキストサイズ」設定が、レイヤー内容と画像内タグにしか反映されない不具合を修正した。インスペクタ・レイヤ・ライブラリ両パネル、下部ステータス/ヘルプバーなど、アプリ内の主要なテキストを対象とし、変更が即座に反映されるようにした（従来あった「次回起動時に反映」という制限を撤廃）。なお、OSネイティブのツールチップはmacOSの公開APIにフォント変更手段がないため対象外（アプリ独自のホバーヘルプ表示は既に対象内）。

■更新履歴

- 静的コントロール向けに `applyScaledFont(_:size:weight:)` を新設し、生成時にフォント適用とレジストリ登録（`scaledTextControls`）を同時に行う方式へ変更。`textSizeChanged()` はこのレジストリを走査して全登録コントロールへ即時にフォントを再適用する。動的に再生成されるライブラリテーブル/コレクションビューのセルは、`NSTableView`/`NSCollectionView` によるビュー再利用時にもフォントが再適用されるよう、フォント設定をセル生成時ではなく `configure` 相当の毎回のセットアップへ移動。
- `configureToolbarButton` の `bezelStyle` を `.texturedRounded` から `.shadowlessSquare` へ変更。旧スタイルは20〜24pt想定の内蔵ハイライト画像を用いており、Build 66のアイコン拡大後の正方形フレーム（40〜58pt）へ引き伸ばすとハイライトが横長に潰れていたため。
- `applyInitialLayoutIfNeeded()` に、表示状態のペインが実際には幅ほぼ0のまま残っていないかを確認する保険チェックを追加。`restoreSplitPositions()` が他ペインの復元成功により`true`を返しても、当該ペイン自体の保存幅（`Layout.leftPaneWidth`等）が一度も保存されていない場合は幅0のまま見過ごされていたため。
- `movePanel(from:to:)` に、移動先サイドが空の場合に移動元ペインの現在幅を引き継ぐロジックを追加（`applyPaneWidth(_:side:)` で反映・保存）。
- `libraryTwoColumnPaneWidth()` を新設し、ライブラリグリッドのアイテム幅(120pt)・アイテム間隔・セクション余白・スクロールバー・パネル内パディングから、2列表示に必要な幅を算出。右サイドパネルの各種既定幅箇所（初回起動時・非表示→表示復帰時）で使用。

## v0.0.00001 - Build 68 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- 改善: ライブラリの「テキスト」「サムネイル」表示を、項目を横一列に並べた列テーブル形式（サムネイル/ファイル名/状態/解像度/ROI数/更新日時）へ変更した。列見出しをクリックするとその列の内容でリストをソートできる（もう一度クリックで昇順/降順を切替）。
- 新機能: 処理済みフラグフィルタ（すべて/未処理/処理済）とファイル名のテキスト検索フィルタを、表示モード切替（グリッド/テキスト/サムネイル）の下に追加した。

■更新履歴

- ライブラリの単一列テーブルを、サムネイル/ファイル名/状態/解像度/ROI/更新日時の6列（サムネイル列はサムネイル表示モードのみ表示）へ再構成し、`NSTableColumn.sortDescriptorPrototype` + `tableView(_:sortDescriptorsDidChange:)` で列ソートに対応。
- `displayedLibraryItems`（フィルタ・検索・ソート適用後の表示専用配列）を新設し、一覧表示・選択・カーソルキー移動などの表示に関わる箇所を `libraryItems`（全件・フィルタの影響を受けない原本）からこちらへ切替。一括処理・学習データ書き出し等、常に全件を対象とすべき処理は `libraryItems` のまま維持。
- フィルタ・検索・ソートは現バージョンではセッション内のみ保持（再起動で既定=すべて/検索なし/更新日時降順に戻る）。

## v0.0.00001 - Build 67 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- 新機能: 詳細設定に「テンキー割当」を追加した。全ショートカット対応操作の一覧から、テンキー（0〜9・.・*・+・/・Enter・-・=）を割り当てられる。トップ行の数字キーとは区別され、テンキーだけで反応する。
- 新機能: ヘルプメニューに「ショートカット一覧」を追加した。先頭に「よく使うおすすめショートカット」、続けて機能分類（ファイル/編集/処理/表示/ライブラリ/一括処理）ごとに一覧表示する。テンキー割当がある場合は併記される。
- バグ修正: コードレビューの結果、「モザイクを適用」がツールチップに表示されないまま裸のReturnキー（修飾キーなし）に割り当てられていたことが判明した。ライブラリへの保存を伴う操作が誤操作で実行されうる状態だったため、⌘Return（Command+Return）へ変更し、ツールバーのツールチップにも表示されるようにした。

■更新履歴

- `AppShortcut` レジストリを新設（id/分類/タイトル/キー/修飾キー/おすすめフラグ/action）。メニュー構築（`shortcutMenuItem`）とツールバーのツールチップ（`shortcutToolbarButton`）の両方がこのレジストリを直接参照するため、表示文言とキー等価文字を手入力で重複させず、表示と実際の動作が食い違うバグを構造的に防ぐ。
- テンキー割当はNSMenuItem.keyEquivalent（文字ベースの一致でトップ行の数字キーと区別できない）ではなく、`NSEvent.addLocalMonitorForEvents(matching: .keyDown)` によるキーコード判定（`NumpadKey` 列挙、macOS仮想キーコード82〜92, 65, 67, 69, 75, 76, 78, 81）で実現。

## v0.0.00001 - Build 66 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 詳細設定ウィンドウで「アイコンサイズ」ラベルと設定値ボタンが重なって表示される不具合を修正した。
- 改善: 設定ファイルの保存場所の表示を、アイコンサイズ等のラベルと左寄せ位置・テキストサイズを揃えた。
- 改善: アイコンサイズの既定値が小さすぎるとの指摘を受け、全体を一段階拡大した（旧「大」を新「小」の基準とし、中・大はそこから拡大）。既定値も「大」から「中」へ変更。
- 新機能: 「テキストサイズ」設定（小/中/大）を追加した。アイコンサイズと同様に画面内のラベル・キャンバス描画テキストのサイズを変更できる。レイヤ一覧・ライブラリ一覧・キャンバス描画・ステータスバーは即座に反映され、起動時に組み立てる一部の見出し等は次回起動時に反映される。
- 改善: プロジェクトファイルの拡張子を `.newmosaicproj` から `.nmcf`（newMosaic Config の略）へ短縮した。Windows/Macとも扱いやすい4文字拡張子。

■更新履歴

- `inspectorRow` のラベル幅を `equalToConstant` から `greaterThanOrEqualToConstant` へ変更（固定幅より長い文言が隣接コントロールへはみ出して重なる不具合を解消）。
- 詳細設定ウィンドウの「設定ファイル」表示を独立ラベルから `inspectorRow` 使用へ統一し、他の設定行と左端・フォントサイズを揃えた。
- ツールバーアイコンのサイズテーブルを更新（小=18pt/40pt枠, 中=23pt/48pt枠, 大=28pt/58pt枠。既定値インデックスも2→1へ）。
- `MosaicWindowController.textScale()`/`scaledFont(_:weight:)`/`scaledMonospacedDigitFont(_:weight:)` を新設し、`.systemFont`/`.monospacedDigitSystemFont` を使う画面内テキスト24箇所（パターンプレビューアイコンの絵文字描画を除く）を差し替え。
- `projectFileExtension` を `"newmosaicproj"` から `"nmcf"` へ変更。

## v0.0.00001 - Build 65 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- バグ修正: サイドパネルの幅は編集可能になったが、パネルを左サイドへ移動すると表示されなくなる不具合を修正した。非表示→表示への切替時にNSSplitViewが幅を自動確保しないことが原因で、移動時に既定幅（280pt）を明示的に与えるようにした。
- バグ修正: レイヤのグループ化・グループ解除が機能しない不具合を修正した。グループ化は「未グループの最上位レイヤ」しか対象にしておらず、実際に選択できるレイヤ（人物検出/骨格検出など）の大半が既存グループの子要素だったため、2件選択してもグループ化できなかった。既存グループの内側にあるレイヤも選択して直接グループ化できるよう修正した（元のグループから取り除き、空になったグループは自動削除）。

■更新履歴

- `applyPanelAssignments()` で移動前後の`isHidden`状態を比較し、非表示→表示に転じたペインへ`NSSplitView.setPosition`で既定幅280ptを明示的に割り当てるよう変更。
- `groupSelectedLayers()` の対象を `ungroupedLayers` 限定から、既存グループの子レイヤも含む全選択レイヤへ拡張。グループ化のステータス表示に件数を追加。

## v0.0.00001 - Build 64 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- 新機能: 「画像出力」ウィンドウを刷新した。専用のリサイズ可能なウィンドウ（初期幅は旧保存パネルの約2倍）で、ファイル名・保存先・出力形式・形式ごとの詳細設定を1画面で確認しながら出力できる。
- 対応形式を jpg/png/bmp/gif/tif/heic/pdf/psd/ai/eps へ拡張した。jpg/heicは品質（圧縮率）スライダー、psdは「元画像レイヤを含める」（非表示レイヤとして追加。Photoshopで表示切替による比較・追加レタッチが可能）、全形式共通で出力解像度（DPI）と透明保持のON/OFFを設定できる。
- 出力前に全体プレビューと中央部の拡大プレビューを表示する。jpg/heicは実際の圧縮設定でエンコードした結果をプレビューするため、保存前に圧縮アーティファクトを確認できる。
- CLIP/SAI/MDP（CLIP STUDIO PAINT/SAI/MediBang Paint）は仕様非公開の独自バイナリ形式のため非対応とした（ユーザー確認済み）。

■更新履歴

- `MosaicCore` に `ImageExporter`/`ImageExportFormat`/`ImageExportOptions` を新設。jpg/png/bmp/gif/tiff/heicはCGImageDestination（ImageIO）、pdf/aiはCGContextのPDFコンテキストへ画像を配置して書き出す（aiはIllustrator互換のPDFコンテナとして出力し、ベクターパスとしての編集はできない旨をUIに明記）。
- `PSDWriter` を新設。Adobe公開仕様「Photoshop File Formats Specification」準拠の自前ライター（RGB 8bit・非圧縮RAWエンコード）。`original` 画像を渡すと「元画像」（非表示）＋「モザイク適用」（表示）の2レイヤ構成で書き出す。macOS標準ImageIOのPSDリーダーで読み戻し、統合画像のピクセルが正しいことをテストで確認済み。
- `EPSWriter` を新設。JPEG圧縮した画像をPostScript Level 2のDCTDecodeフィルタへ自前実装のASCII85エンコードで埋め込む、標準的なラスターEPSを生成する。
- テスト6件追加（ImageIO各形式の読み戻し検証、PDF/AIのページ数検証、PSDのレイヤ構成読み戻し検証、ASCII85エンコード検証、EPSヘッダ検証）で計73テスト。

## v0.0.00001 - Build 63 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- バグ修正: サイドパネル（左右）の幅がまったく調整できない不具合を修正した。Auto Layoutの必須幅制約とNSSplitViewの独自ドラッグ処理が競合し、ドラッグ後すぐに元の幅へ戻されていたのが原因。NSSplitViewDelegateのconstrainMinCoordinate/constrainMaxCoordinateで最小・最大幅を与える方式へ変更した。
- 新機能: ファイルメニューに「設定」を追加した。詳細設定（アイコンサイズを小/中/大で変更可能）、プロジェクト（最近保存したプロジェクトを最大10件まで表示し選択で読込）、保存（現在のモザイクスタイル・レイアウト・検出設定などのアプリ設定状態をプロジェクトファイルへ書き出し）、読込（プロジェクトファイルから設定状態を復元）、初期化（確認ダイアログの上ですべてのアプリ設定を既定値へ戻す。ライブラリの画像・ROIは対象外）。
- 改善: 「画像を書き出す」を「画像出力」に名称統一した（メニュー・ツールバーとも）。ウィンドウ本体のリニューアルは別途対応予定。

■更新履歴

- `NSSplitView` の境界ドラッグ範囲をAuto Layoutの`greaterThanOrEqualToConstant`ではなく`NSSplitViewDelegate`の`constrainMinCoordinate(ofSubviewAt:)`/`constrainMaxCoordinate(ofSubviewAt:)`で算出する方式へ変更（隣接ビューの現在フレーム＋最小幅から計算。非表示ペインはスキップ）。
- `AppSettings` へ `exportSnapshot()`/`importSnapshot(_:)`/`resetAll()` を追加し、全設定値のJSONスナップショットをプロジェクトファイル（`.newmosaicproj`）として保存/読込できるようにした。
- 「最近のプロジェクト」は`NSMenuDelegate.menuNeedsUpdate`でメニューを開く直前に再構築（最大10件、同一パスは重複排除）。
- 詳細設定ウィンドウを新設し、アイコンサイズ変更を実装。SF Symbolsの`SymbolConfiguration(pointSize:)`とボタンの幅/高さ制約を動的に差し替えることで、ツールバーアイコンの見た目サイズを即座に変更できる。

## v0.0.00001 - Build 62 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 「目元」「眼窩下〜あご」のモザイク判定処理枠で、モザイクが均一に適用されず色調が赤っぽくなったりまだらになったりする不具合を修正した。マスク生成方式が「Vision人物セグメンテーション」「前景オブジェクト」「対象の形状」のいずれかに設定されている場合、これらのVisionベースのマスクは前髪など細部が重なる領域で完全不透明にならず部分的な半透明値を持つことがあり、モザイクフィルと元画像の色が混ざって色調異常に見えていた。目元・眼窩下〜あごはメガネ等のアクセサリを重ねる/隠す用途の領域であり、被写体の実形状ではなく指定した矩形/楕円/多角形どおりに覆うのが正しい挙動のため、この2カテゴリのみ選択中のマスク生成方式に関わらず常に図形ベースのマスクを使うよう修正した（他カテゴリの挙動は変更なし）。

■更新履歴

- `MosaicEngine` に `createMasks(for:in:extent:segmentEngine:)` を新設。ROIをカテゴリで振り分け、`.eyes`/`.lowerFace` は常に `ShapeSegmentEngine`、それ以外は従来どおり渡された `segmentEngine` でマスクを生成し、元の順序へマージする。`applyMosaic` はこの新関数経由でマスクを取得するよう変更。
- テストを追加: 意図的に40%半透明のマスクを返すスタブセグメントエンジンを使い、目元カテゴリでは強制的に完全不透明（図形ベース）のマスクへ差し替わること、他カテゴリではスタブの半透明マスクがそのまま使われる（回帰なし）ことを検証（計68テスト）。

## v0.0.00001 - Build 61 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- 新機能: 画像プレビュー画面の操作方法を追加した。
  - マウスホイールの上下回転: 画像の拡大縮小（カーソル位置基準の倍率変更。トラックパッドの2本指スクロールは従来どおり拡大中のパン）
  - 右ボタンをホールドしたままドラッグ: 画像全体の移動（ドラッグせず右クリックした場合は従来どおりカテゴリ変更メニュー）
  - カーソル表示の変更: 画像上=十字、モザイク範囲の上=開いた手、範囲移動中・全体移動中=握った手、画像外=矢印

■更新履歴

- `ImageCanvasView.scrollWheel` をホイール（非精密デルタ）=ズーム10%刻み、トラックパッド（精密デルタ）=パンへ分岐。
- `rightMouseDown/Dragged/Up` を実装し、右ドラッグでpanOffsetを直接移動。非ドラッグの右クリックは `NSMenu.popUpContextMenu` で従来メニューを表示。
- `NSTrackingArea`（mouseMoved）でホバー位置に応じたカーソル切替を実装。


## v0.0.00001 - Build 60 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- 新機能: アプリ下部にステータス兼ヘルプバーを追加した。左側にはツールバーのアイコンへマウスを乗せたときのヘルプ説明・作業ステータス・一括処理などの処理状態を表示し、右側にはメジャーな画像処理アプリのステータスバーを参考に「画像 選択数/全画像数 ｜ 解像度 色ビット数 ｜ ROI数」を常時表示する。
- 改善: ライブラリのサムネイル拡大縮小をスライダーから虫めがねボタン（縮小/拡大）へ変更した。

■更新履歴

- ステータスバー（高さ26pt・上端セパレータ）をルートレイアウト最下部へ追加し、ステータスラベルをツールバーから移設。ツールバーボタンへ `NSTrackingArea`+中継レスポンダを付与し、ホバー時にヘルプ文を表示・離脱時に直前のステータスへ復帰。
- 統計表示は等幅数字・右寄せで、ROI変更・ライブラリ再読込・選択変更・画像読込のたびに更新。
- サムネイルサイズは±24px（64〜220）の段階調整（設定保存は従来どおり）。


## v0.0.00001 - Build 59 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- バグ修正: サイドパネルの幅が任意に伸縮できない問題を修正した（スライダーの固定幅が原因。希望幅+最小幅の圧縮可能な制約へ変更し、狭い幅でも内容の左右マージンが保たれるようにした）。
- バグ修正: サイドパネルのウィンドウを移動した際に内容が下寄せ表示され不要な空間ができる問題を修正した（スクロール内容を上寄せ表示へ）。
- バグ修正: モザイクパターン詳細設定のスライダーが操作中に左右へずれる問題を修正した（設定値ラベルを固定幅・等幅数字・右寄せへ）。
- 改善: ◀▶ボタンをスクロールバーと重ならない位置へ移動。「色を付ける」チェックを塗りつぶし色（カラーウェル）の左へ移動（行ラベル「塗りつぶし色」）。設定値の数値と単位の間へ半角スペースを追加（例: 50 %、12 px）。候補カテゴリを3列表示へ変更（1行目=目元/眼窩下〜あご、2行目=乳首/性器（女性）/性器（男性）、3行目=その他）。
- 改善: パターンプレビューアイコンを左右トリミングして半分の幅にし、「画」がアイコン全面に表示されるようにした。モザイク等の加工粒度も実際の適用イメージが分かる値へ調整。

■更新履歴

- インスペクタのスクロールを `FlippedDocumentView`（isFlipped）へ変更（NSScrollViewの非flippedドキュメントは内容が短いと下寄せになる）。
- スライダー幅=希望160（優先度400）+最小80、設定値ラベル=固定幅48・monospacedDigit・右寄せ。候補カテゴリはNSGridView 3列。プレビューアイコンは32x24pt（Retina 2倍描画・文字サイズ0.95倍・blockScale 6）。


## v0.0.00001 - Build 58 - 2026-07-24

■更新履歴（Readme / ChangeLog 用）

- 新機能: SNS（TikTok/Instagram等）向けのかぶせ画像素材を6種同梱した。「パターン画像を選択...」ボタンがメニュー化され、同梱素材（プレビュー付き）またはファイル選択を選べる。
  - 目元向け: サングラス（黒）／ハートサングラス／パーティーマスク（マスカレード）
  - 眼窩下向け: 医療マスク／ガスマスク／犬の鼻口
- 素材はすべて透過PNG（ベクター描画による自前生成）で、かぶせ画像パターンでROIへ引き伸ばして重ねられる。

■更新履歴

- `OverlayAssetCatalog` を新設（識別子 `builtin:<名前>`・表示名・想定カテゴリ付き）。素材は `scripts/generate_overlay_assets.swift`（CoreGraphicsベクター描画）で生成し `Resources/Overlays/` へ同梱（外部素材の取り込みなし=ライセンス問題なし）。
- リムーバブルボリューム許可ダイアログ対策として、モデルと同方式で初回のみApplication Supportへキャッシュ。
- `builtin:` 識別子はROIスタイルへそのまま保存され、一括処理のパターン解決（同梱素材+ライブラリパターン）にも対応。
- テスト1件追加（全素材の読み込み検証）で計67テスト。


## v0.0.00001 - Build 57 - 2026-07-23

■更新履歴（Readme / ChangeLog 用）

- 新機能: 塗りつぶしパターンへ「かぶせ画像（マスク・メガネ等）」を追加した。タイル敷き詰めの「任意パターン画像」と異なり、選択した画像をROI矩形へ引き伸ばして**1枚だけ重ねる**。PNGの透明部分はそのまま透過するため、目元ROIへパーティーマスク・メガネ・グラサン、眼窩下ROIへ医療マスク・ガスマスク・犬の鼻口などのアクセサリ画像をかぶせて隠せる。ROIの回転にも追従し、透明度調整も可能。ROIごとのモザイク設定と組み合わせて部位別に別画像を指定できる。

■更新履歴

- `MosaicFillPattern.overlayImage` を追加。`MosaicEngine.compositeOverlay` はマスク合成ではなくROI矩形へのスケーリング+回転変換+アルファ合成で重ねる（画像必須。未指定はエラー通知）。
- パターン画像選択ボタンは「任意パターン画像」「かぶせ画像」の両方で有効化。プレビューアイコンはサングラス風の黒帯プレースホルダで表示。
- テスト2件追加（かぶせ合成の画素検証・画像未指定エラー）で計66テスト。


## v0.0.00001 - Build 56 - 2026-07-23

■更新履歴（Readme / ChangeLog 用）

- 新機能: 顔領域の自動認識を追加した。検出対象カテゴリへ「目元」（両目+眼窩上部。パーティーマスク・メガネ・グラサン等で覆う想定）と「眼窩下〜あご」（医療マスク・ガスマスク・犬の鼻口等で覆う想定）が加わり、候補生成時に顔ごとの領域ROIが自動生成される。対象カテゴリのチェックで生成ON/OFFを切替でき、モザイク等の既存パターンでの隠蔽も従来どおり可能。

■更新履歴

- `MosaicTargetCategory` へ `eyes`（目元）/`lowerFace`（眼窩下〜あご）を追加（対象カテゴリのチェック行・ROIリスト・右クリックカテゴリメニューへ自動反映）。
- `FaceRegionBuilder` を新設: 目・眉・顔輪郭の点群から目元帯（両目+眉、パディング付き）と眼窩下領域（目の下端〜あご先、輪郭幅）を構築する共通ロジック（実写/アニメで共用）。
- 実写: `FaceRegionDetector`（`VNDetectFaceLandmarksRequest`）で顔ごとにランドマークを取得しROI生成。
- アニメ: `AnimePoseEstimator.faceRegionROIs` を追加。DWPoseのCOCO-WholeBody顔68点（輪郭・眉・目）から同じロジックでROI生成（追加モデル不要）。
- テスト2件追加（領域構築の検証・顔なし画像で0件）で計64テスト。


## v0.0.00001 - Build 55 - 2026-07-23

■更新履歴（Readme / ChangeLog 用）

- 新機能: 大量画像の一括処理を追加した。ツールバーの「一括処理」で、未加工（加工後画像なし）かつリンク有効な全画像を候補生成→モザイク適用→ライブラリ保存で連続処理する。処理中はシートに「処理中件数/全件数・画像名・進捗バー」をリアルタイム表示し、キャンセルボタンで途中中断できる（処理済み分は保存されたまま残る）。

■更新履歴

- 一括処理はバックグラウンドタスクで実行（`CandidateGenerationWorker` 再利用・画像ごとに候補生成→対象カテゴリフィルタ→形状適用→`MosaicEngine.applyMosaic`→保存）。実行パラメータ（画像種別・鼠径部位置・対象カテゴリ・形状・既定スタイル・マスク生成方式）は開始時点のUI設定で確定する。
- 進捗パネルはウィンドウのシートとして表示し、1件ごとにメインスレッドでラベル・進捗バー・ステータスを更新。キャンセルフラグは各件の処理前に確認する。


## v0.0.00001 - Build 54 - 2026-07-23

■更新履歴（Readme / ChangeLog 用）

- 新機能: フォルダの一括登録（リンク方式）を追加した。ツールバーの「フォルダを一括登録」で選択フォルダ内の画像をコピーせず**リンク**としてライブラリへ登録する（大量画像の管理向け。ROI・レイヤ情報は従来どおりアプリ側で管理）。リンク登録アイテムには🔗マークが付き、削除しても参照先の元ファイルは消えない。
- 新機能: リンク切れの表示と修正UIを追加した。参照先が見つからないアイテムは一覧へ「⚠️リンク切れ」を赤字表示。ツールバーの「リンク切れを修正」で、リンク切れアイテム選択中はファイル指定の画像単位修正、未選択時はフォルダ指定のファイル名一致による一括修正ができる。

■更新履歴

- `MosaicLibraryItem.linkedOriginalPath` を追加（旧データはnil=コピー取り込み）。`LibraryEngine` へ `importLinked`（重複パスはスキップ）/`isLinkBroken`/`brokenLinkedItems`/`relink`/`repairBrokenLinks`（フォルダ再帰探索+ファイル名一致）を追加。`deleteItems` はリンク先の外部ファイルを削除しない。
- 一括登録は拡張子（png/jpg/jpeg/tiff/tif/heic）でフィルタしファイル名の自然順で登録。画像サイズはCGImageSourceのメタデータから取得（フルデコードなし）。
- テスト1件追加（リンク登録→切れ判定→一括修正→削除の往復）で計62テスト。


## v0.0.00001 - Build 53 - 2026-07-23

■更新履歴（Readme / ChangeLog 用）

- 改善: モザイク塗りつぶしパターン選択のプレビューアイコンを刷新した。白地に太字の「画」を描いた横長アイコン（56x24・Retina描画）の右半分だけへ実際のパターン処理（モザイク/ノイズ/ボケ/エッジ系/ボーダー各種/雲/任意パターン）を適用し、元画像と加工結果の違いが一目で分かるようにした（全10パターン共通の方式）。

■更新履歴

- `makePatternPreviewImage` を「グラデーション全面加工」方式から「太字『画』+右半分のみ実エンジン適用」方式へ変更。プレビューは `MosaicEngine.applyMosaic` の実処理結果のため、選択時の見た目と一致する。


## v0.0.00001 - Build 52 - 2026-07-23

■更新履歴（Readme / ChangeLog 用）

- 新機能: サイドパネル内の各ウィンドウ（ライブラリ/レイヤ/モザイク設定）の右上に「◀」「▶」ボタンを追加した。押した方向のサイドパネル（左/右）へウィンドウを移動でき、配置は設定へ保存され次回起動時も復元される。片側が空になった場合はそのサイドパネルを自動的に畳む。

■更新履歴

- メイン分割を「左ペイン/キャンバス/右ペイン」の3分割へ拡張し、各パネルの配置（`Layout.panelSide.*`）・左右ペイン幅・ペイン内の高さ配列（`Layout.leftPaneHeights` 等）をポータブル設定へ保存/復元。
- 空ペインは非表示+最小幅制約の無効化で幅0へ畳む。移動ボタンは各パネル右上へオーバーレイ配置。


## v0.0.00001 - Build 51 - 2026-07-23

■更新履歴（Readme / ChangeLog 用）

- バグ修正: サイドパネルの幅・各ウィンドウ（ライブラリ/レイヤ/モザイク設定）の分割位置が自由に変更できず、再起動後も記憶されない問題を修正した。最小サイズ制約を緩和し、分割位置・サイドパネル幅・ウィンドウ枠を設定へ保存して次回起動時に復元する。
- 新機能: アプリ設定のポータブル保存に対応した。全設定（Windowsの*.ini相当）をアプリ本体と同じフォルダの `newMosaic_Settings/settings.json` へ保存し、アプリと設定フォルダをまとめて移動・コピーすれば他のPC環境でも同じ設定で動作する。保存先が書き込めない場合はApplication Supportへ自動フォールバック。既存のUserDefaults設定は初回起動時に自動移行される。

■更新履歴

- `AppSettings` を新設（UserDefaults互換API・JSON保存・0.3秒デバウンス書き込み・書き込み可否のプローブ判定・既知キーの自動移行）。main.swift内の設定用途のUserDefaults参照13箇所を置換（モデルキャッシュのマーカーは機種ローカル情報のためUserDefaultsのまま）。
- 分割ビューのautosaveName（UserDefaults依存）を廃止し、`NSSplitViewDelegate.splitViewDidResizeSubviews` で分割位置を `Layout.*` キーへ手動保存・起動時復元。ウィンドウ枠も `windowDidEndLiveResize`/`windowDidMove` で保存。
- 最小サイズ制約を緩和（サイドパネル最小幅300→220、各ウィンドウ最小高さ140/150/220→80/80/120、ウィンドウ最小760x560）し、キャンバス最小幅200を追加。


## v0.0.00001 - Build 50 - 2026-07-23

■更新履歴（Readme / ChangeLog 用）

- 新機能: イラスト/漫画でも骨格検出が動作するようになった。DWPose（アニメ画像でも頑健に動作する姿勢推定モデル）を同梱し、骨格レイヤ（矩形+関節点+ボーン線）の表示と、骨格ベースの候補ROI（乳首・鼠径部の位置推定）がアニメでも生成される（残件「アニメ骨格検出」の対応）。
- 残件整理: 実写NudeNet 640m版は配布元がAGPL-3.0ライセンスと判明したため同梱を見送り（現行のApache-2.0版320n+人物クロップ方式を維持）。学習データを使った自前ファインチューニングの手順書（Docs/FINETUNE_GUIDE.md）を追加。

■更新履歴

- `AnimePoseEstimator` を新設: yzd-v/DWPose dw-ll_ucoco_384.onnx（Apache-2.0, RTMPose系SimCC, 入力288x384, COCO-WholeBody 133点, 約134MB）を同梱。人物ごとの1.25倍拡張クロップ→mmpose標準前処理（レターボックス+ImageNet正規化）→SimCC argmax復号（しきい値0.3）で体幹13点を取得し、neck/rootを肩・腰の中点から合成。
- Build 49のキャラクターマスクがある場合は関節をマスク内へ限定（実写経路と同じ「検出していないものは表示しない」方針。マスク外のみなら骨格なし扱い）。
- イラスト経路でも `SensitiveROIGenerator` による骨格ベース候補ROI（乳首・鼠径部）を生成し、アニメ部位検出（内容ベース）とIoU統合する。鼠径部位置スライダーもアニメへ適用される。
- SimCC復号の単体テストとモデルロード+推論スモークを追加（計61テスト）。


## v0.0.00001 - Build 49 - 2026-07-23

■更新履歴（Readme / ChangeLog 用）

- 新機能: イラスト/漫画でも人物シルエット（青半透明の形状表示）が出るようになった。アニメ用キャラクターセグメンテーションモデルを同梱し、人物検出レイヤが矩形のみだった制約を解消（残件「アニメセグメンテーション」の対応）。

■更新履歴

- `AnimeSegmenter` を新設: skytnt/anime-seg isnetis.onnx（Apache-2.0, ISNet系, 入力1024x1024, 約176MB）を同梱し、前処理はSkyTNT公式実装と同条件（アスペクト維持+中央ゼロパディング+/255）。全体キャラクターマスクを人物矩形ごとに分配し、`PersonDetection.maskImage` として実写のVisionシルエットと同経路で表示する。
- 非同期候補生成ワーカーへ統合（イラスト経路で人物検出後にマスク付与。失敗時は矩形のみの従来表示）。
- 既存のモデルキャッシュ機構を共用（初回のみ内蔵ディスクへコピー）。
- テスト2件追加（モデルロード+全体マスクのサイズ検証、per-personマスクの矩形制限検証）で計59テスト。


## v0.0.00001 - Build 48 - 2026-07-23

■更新履歴（Readme / ChangeLog 用）

- UIをプロ向け編集アプリ構成へ再編した。処理コマンドをSF Symbolsの単一ツールバーと標準メニューへ整理し、全アイコンにフローティングヘルプを追加。ズーム/パン、ウィンドウ寸法保存、調整可能な右側3分割（ライブラリ/レイヤ/インスペクタ）を実装した。
- モザイク設定を右インスペクタへ常設し、選択ROIごとにパターン、透明度、色、細かさ、ぼかし、帯、雲、任意パターン画像を独立保存できるようにした。パターン一覧には実処理に対応する縮小プレビューを表示する。
- 人物・骨格検出を改善した。人物マスクと人物矩形の座標系を一致させ、5人以上の矩形補完、横臥姿勢の回転推論、低解像度拡大、顔起点探索、人物矩形/マスク/中心/信頼度による候補評価を追加。骨格レイヤ領域は人物検出領域と一致させた。
- 候補生成をバックグラウンド実行へ変更し、重いVision/ONNX推論中もUI操作を継続可能にした。古い画像の解析結果は破棄し、切替先の保留要求を再実行する。
- ライブラリのサムネイル縦間隔を圧縮し、表示モードとサムネイル寸法を保存。開く、貼り付け、画像切替、削除、終了の全経路で未保存確認を行い、「保存しない」を選んだ編集状態が後で復元される問題も修正した。

■品質確認

- `swift test`: 57/57 PASS。
- 6回のコードレビューを実施し、P0/P1は全件修正。最終回はP0〜P3指摘なし。レビュー表は `Docs/QC/CodeReview/QC_CodeReview_v0.0.00001_Build48.md` を参照。

## v0.0.00001 - Build 47 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 新機能: レイヤパネルの各レイヤ行に「輪郭」「タグ」チェックを追加した。レイヤごとに輪郭（枠線）とタグ（名称・カテゴリラベル）の表示を個別にON/OFFできる。モザイク対象レイヤは全ROIの枠線・カテゴリラベル、人物検出レイヤは破線枠・名称（シルエットは表示チェックに連動）、骨格検出レイヤは枠・名称（ボーンは表示チェックに連動）を制御する。画像レイヤは対象外。

■更新履歴

- `LayerLeaf` へ `showsOutline` / `showsTag` を追加し、`LayerRowView` にミニサイズの「輪郭」「タグ」チェックを右寄せ配置（画像レイヤ・グループ行・ROI選択リスト行では非表示）。
- `applyLayerVisibility` がレイヤ毎の輪郭/タグ状態をキャンバスへ反映（`showROIOutlines`/`showROITags`/`personLayerOutlineVisibility` 等）。
- キャンバス描画で枠線とラベルを個別制御（ROIの選択ハンドル・回転ハンドルは輪郭OFFでも編集可能なよう表示維持）。


## v0.0.00001 - Build 46 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 新機能: 選択範囲（ROI）の自由多角形に対応した。「追加形状」に「多角形」が加わり、ドラッグ追加すると六角形で配置される。頂点をドラッグして自由に変形でき、Option+クリックで辺上へ頂点追加・頂点上で頂点削除（最少3頂点）ができる。移動・四隅リサイズ（全体拡縮）・自由回転・ダブルクリック削除・カテゴリ変更も他形状と同様に使用可能。モザイクマスクは多角形の形どおりに生成され、頂点はライブラリへ保存される。

■更新履歴

- `ROIShape` へ `polygon` を追加し、`MosaicROI.polygonPoints`（rectローカル正規化座標の頂点列。nil=既定六角形）を追加（既存データはデコード時nil）。
- `ShapeSegmentEngine.polygonMask`: 頂点をCI画素座標へ変換してグレースケール描画（回転はCTM変換で反映）。各エンジンのROI制限（形状マスク乗算）でも多角形形状が使われる。
- キャンバス: 頂点ハンドル描画/ドラッグ変形（回転中はローカル座標へ逆回転）、Option+クリックの頂点追加（最寄り辺への射影点挿入）/削除、多角形の点内包判定（レイキャスティング）、ドラッグ終了時の外接矩形再正規化（無回転時のみ）を実装。
- 回転ROIの塗りパッチ範囲を外接円を覆う正方形へ拡張（回転で軸平行矩形からはみ出す分の描画欠けを防止）。
- 多角形マスクの画素検証・Codable往復テストを追加（計44テスト）。


## v0.0.00001 - Build 45 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 新機能: 選択範囲（ROI）の自由回転に対応した。ROIを選択すると上部に回転ハンドル（丸）が表示され、ドラッグで任意角度へ回転できる（45度の倍数付近は自動スナップ）。回転はモザイクのマスク形状にも反映され、回転中のROIも移動・四隅リサイズ・ダブルクリック削除・右クリックカテゴリ変更が可能。回転角はライブラリへ保存される。

■更新履歴

- `MosaicROI` へ `rotation`（度・時計回り・矩形中心基準）を追加（既存データはデコード時0度）。
- `ShapeSegmentEngine` のマスク生成を回転対応（ビュー座標の時計回り→CI座標の逆回転変換）。
- 各SegmentEngineのROI範囲制限を矩形クロップから**形状マスク乗算**（`ShapeSegmentEngine.restrict`）へ統一。楕円ROI・回転ROIでも形状どおりにマスクが制限されるようになった（従来は楕円ROIでも矩形制限だった）。
- キャンバス: 回転ハンドル描画・回転ドラッグ（45度スナップ±3度）・回転を考慮した当たり判定（逆回転ローカル座標判定）・回転中の四隅リサイズ（ローカル座標系マッピング）・選択ハンドルの回転追従を実装。
- 回転マスクの画素単位検証テストを追加（計42テスト）。


## v0.0.00001 - Build 44 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 改善: カーソルキーで画像を連続移動しながら自動保存すると、ライブラリ一覧の並び順がその都度変わる問題（既知の注意点）を解消した。アイテムの増減がない再読込では現在の表示順を維持する。「ライブラリ更新」ボタンを押した場合と画像の追加・削除時は従来どおり最新の並び（更新日時降順）で読み直す。

■更新履歴

- `reloadLibrary` に `preserveOrder` を導入。アイテムID集合が不変の再読込では表示順を維持し、集合が変わった場合と「ライブラリ更新」ボタン（明示操作）では更新日時降順で並び直す。


## v0.0.00001 - Build 43 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 改善: 画像種別（実写/イラスト・漫画）の自動判定をモデルベースへ強化した。専用の分類モデルを同梱し、白黒漫画・実写風イラスト等の誤判定を削減。判定の確信度もステータスに表示する（例:「自動判定 97%」）。従来の統計判定はモデルが読み込めない場合のフォールバックとして維持。

■更新履歴

- `DomainModelClassifier` を新設: deepghs/anime_real_cls mobilenetv3_v1.4_dist（OpenRAILライセンス, MobileNetV3系ONNX, 入力384x384, 2クラス[anime, real], 約16.8MB）を同梱し、softmax確率で判定。前処理はimgutils標準（単純リサイズ・(v/255-0.5)/0.5正規化）。
- 既存のモデルキャッシュ機構（Application Support/newMosaic/Models）を共用し、リムーバブル許可ダイアログは発生しない。
- 「画像種別」の手動指定は従来どおり最優先。テスト1件追加で計41テスト。


## v0.0.00001 - Build 42 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 新機能: モザイク設定へ塗りつぶしパターンを3種追加した（計10種）。いずれも既存の透明度・色付け・細かさ・輪郭ぼかし等の基本パラメータを踏襲する。
  - アンシャープ（エッジ強調）: 範囲内の輪郭・内部模様のエッジのみをアンシャープ処理し、エッジ以外は元画像を保つ。
  - ボーダーランダム: 帯幅・間隔を±40%揺らした帯を斜めに配置し、ノイズ変位で間隔・角度に揺れを出す（間隔は透明。対象の形状マスクに沿って配置される）。
  - 雲: Photoshopの雲フィルタ風のテクスチャで塗りつぶす。密度スライダーで塗り面積を調整でき、「トーン化」ONで漫画のトーンパターン（網点）へ変換する。

■更新履歴

- `MosaicFillPattern` へ `unsharpEdges` / `stripesRandom` / `clouds` を追加し、`MosaicStyle` へ `cloudDensity` / `cloudTone` を追加。
- アンシャープ=CIUnsharpMask+エッジマスク合成（線・エッジぼかしの強調版）、ボーダーランダム=シード固定乱数の帯パターン（512px）を斜め回転タイル+CIDisplacementDistortionで揺らし、雲=白ノイズ拡大+ぼかしの2オクターブ合成+密度ガンマ補正+CIDotScreen網点変換。
- ボーダーランダムはシード固定（SplitMix64）のため、再レンダリングしても同じ模様になる（プレビュー安定）。
- 設定パネルへ「雲の密度」スライダーと「トーン化（漫画トーン）」チェックを追加（雲選択時のみ有効。UserDefaults永続化）。帯設定はボーダー3種で有効。
- 全パターン×共通パラメータのテストループが新パターンを自動的に検証（計40テスト）。


## v0.0.00001 - Build 41 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- バグ修正: マスク生成「対象の形状（ROI内前景）」で、マスク形状が上下反転して適用される問題を修正した。
- バグ修正: 同方式で複数のROIに同じような対象マスクが適用される問題を修正した。ROI周辺が人物で埋まっていて前景抽出が対象物を分離できない場合、顕著領域（オブジェクトネス）マスクへ自動的に切替え、ROIごとの対象物の形状を取る。

■更新履歴

- 上下反転の原因: クロップ経路の前景マスクにも全体画像用の上下反転補正を適用していたが、クロップ経路ではバッファ行方向が画像と一致しており補正が不要だった（補正を除去）。
- 同一マスクの原因: クロップ（旧1.3倍）内が人物でほぼ埋まる場合、前景抽出が「人物全体」を返し、隣接ROIのマスクが同じ見た目になっていた。クロップを1.15倍へ縮小し、前景マスクの被覆率が85%超の場合は `VNGenerateObjectnessBasedSaliencyImageRequest` の顕著領域マスク（コントラスト強調+軽いぼかしで軟マスク化）へ切替える方式にした。
- 被覆率判定 `coverageRatio`（CIAreaAverage）を追加しテストで検証（計40テスト）。


## v0.0.00001 - Build 40 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 改善: 実写で骨格が検出できない場合、画像内の顔を先に検出し、顔位置から全身範囲を推定して骨格検出をやり直すフォールバックを追加した（ユーザー提案の方式）。人物矩形に背景が多い場合や人物シルエットが部分的な場合の骨格検出率が向上する。

■更新履歴

- `VisionPoseEstimator` に顔起点フォールバックを追加: 人物クロップでの骨格検出が失敗した場合、`VNDetectFaceRectanglesRequest`（横顔対応）で最も信頼度の高い顔を検出し、顔幅4.5倍×顔高さ8.5倍の全身領域を組み立ててクロップし直し、骨格検出を再実行する。
- 低解像度クロップ（最大辺480px未満）は2倍へ拡大してから骨格検出する（Visionは低解像度で精度が落ちるため。正規化座標のため座標マッピングには影響しない）。
- 再検出した関節も従来どおり人物マスク内フィルタ（緩衝0.03）を通し、マスク外の関節は除外する。
- 既存の骨格検出ロジックを `detectPose(in:region:person:)` として共通化（通常経路・顔起点経路の両方で使用）。
- テスト1件追加（顔起点経路のスモーク）で計39テスト。


## v0.0.00001 - Build 39 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- バグ修正: Build 38導入の実写NSFW部位検出が実際の写真でほぼ検出できなかった問題を修正した。全体画像に加えて人物領域ごとのクロップでも検出を実行し、全身写真でも性器・乳首を検出できるようにした。

■更新履歴

- 原因: モデル入力が320x320のため、全身写真では対象部位が数ピクセルに縮小され検出不能だった（デコード・前処理はNudeNet公式実装と同一であることをモデル入出力形状・公式コードの照合で確認済み）。
- `PhotoCensorDetector.detect(in:personBounds:)` を新設: 全体画像+人物クロップ（1.15倍拡張）ごとに推論し、対象部位の実効解像度を向上。人物未検出時は20%重複の2x2タイルで代替。重複検出は同カテゴリ内IoU 0.45で信頼度の高い方へ統合（`dedupe`）。
- 検出しきい値をNudeNet公式実装と同じ0.2へ変更（従来0.25）。
- テスト2件追加（人物クロップ/タイル経路のスモーク、重複統合の検証）で計38テスト。


## v0.0.00001 - Build 38 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- バグ修正: イラスト/漫画では性器を認識するのに実写では認識できない問題を修正した。実写用のNSFW部位検出モデル（NudeNet）を同梱し、実写でも乳首・性器・肛門を画像内容から直接検出してカテゴリ付き候補を生成する。

■更新履歴

- 原因: 実写経路には内容ベースの部位検出が無く、骨格関節からの位置推定（乳首=肩基準、鼠径部=腰基準）のみだった。骨格が取れない画像・構図では性器候補が一切生成されなかった。
- `PhotoCensorDetector` を新設: deepghs/nudenet_onnx 320n（Apache-2.0, NudeNet v3, YOLOv8n系, 入力320x320, 約12.2MB）を同梱し、18クラスのうち乳首/性器（女・男）/肛門の4クラスのみ採用（顔・足・着衣クラスは無視。信頼度0.25）。
- `YOLOONNXModel` の入力解像度を可変化（アニメ系=640, 実写=320。レターボックス前処理・座標逆変換は共通）。モデルキャッシュ（Application Support/newMosaic/Models）にも同機構で対応。
- 候補生成の実写経路で骨格ベース候補とIoU 0.5統合（内容ベース優先）。ステータスに「実写部位検出: N件」を表示。
- テスト2件追加（モデルロード+推論スモーク、採用クラス対応表の検証）で計36テスト。`Mosaic/THIRD_PARTY_NOTICES.md` へライセンス表記を追加。


## v0.0.00001 - Build 37 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 新機能: レイヤパネルにROI選択リストを追加した。「モザイク対象」配下に現在のROIをカテゴリ名で一覧表示し（同一カテゴリ名は連番付き。例: 乳首 1 / 乳首 2）、行クリックでキャンバス上のROIを選択できる。
- 改善: レイヤ表示トグル（人物検出/骨格検出/ROI）をツールバーからレイヤパネル先頭へ移設した。
- 改善: ツールバーの「対象カテゴリ」を複数チェック式の生成フィルタへ変更した。乳首/性器（女性）/性器（男性）/その他に加えて「人物」「骨格」もチェック対象になり、候補生成時にチェックされたものだけを生成する（設定は保存される）。
- 変更: 既存ROIのカテゴリ変更は、ROIの右クリックメニューから行う方式へ移動した（新規手動ROIは「その他」で追加）。

■更新履歴

- レイヤパネル: `ROIListEntry` によるROI選択リストを「モザイク対象」レイヤの子として `NSOutlineView` に表示。同一カテゴリ名の連番付与、リスト↔キャンバスの双方向選択同期、ドラッグ移動中の毎フレーム再読込回避（件数・カテゴリ構成のシグネチャ比較）を実装。
- レイヤパネル先頭に表示トグル行（人物検出/骨格検出/ROI）を追加し、ツールバーの同項目を削除。ROIレイヤの一括ON/OFFも追加。
- 対象カテゴリ行を新設（4カテゴリ+人物+骨格のチェックボックス。`GenerateFilter.*` キーで永続化）。候補生成でカテゴリ外ROIを除外し、除外件数をステータスへ表示。人物・骨格チェックOFF時は該当レイヤを生成しない（内部検出はROI位置推定に必要なため実行する）。
- `ImageCanvasView` にROI右クリックのカテゴリ変更メニューを追加（旧カテゴリポップアップの置き換え。アンドゥ対応）。


## v0.0.00001 - Build 36 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 新機能: モザイクの描画パターンと適用パラメータを設定できるようにした。ツールバーの「モザイク設定」から、塗りつぶしパターン（モザイク/ノイズ/ボケ/線・エッジぼかし/ボーダー縦・横/任意パターン画像）と、共通パラメータ（透明度・色付け・パターン細かさ・範囲輪郭ぼかし・ボーダーの帯太さ/間隔）を組み合わせて指定できる。設定は保存され、モザイク表示中は変更が即時反映される。

■更新履歴

- `MosaicCore` に `MosaicFillPattern`（7種）と `MosaicStyle`（透明度/色/細かさ/輪郭ぼかし/帯太さ/帯間隔/パターン画像）を追加し、`MosaicEngine.applyMosaic(to:rois:style:segmentEngine:)` を新設（従来の `scale` 指定APIは後方互換として維持）。
- パターン実装: モザイク=CIPixellate、ノイズ=CIRandomGenerator、ボケ=CIGaussianBlur、線・エッジぼかし=CIEdges+CIMorphologyMaximumで抽出したエッジ部のみをぼかす、ボーダー=縞アルファマスク（帯=塗り、間隔=透明）をROIマスクへ乗算、任意パターン=インポート画像をCIAffineTileで敷き詰め。
- 共通パラメータ実装: 透明度=マスク輝度への乗算、色付け=CIColorMonochrome（ボーダーは帯色として適用）、輪郭ぼかし=マスクへのCIGaussianBlur（塗りパッチをフェザー分拡張して境界の途切れを防止）。
- UI: ツールバーへ「モザイク設定…」ボタンとポップオーバー設定パネルを追加。パターンに応じて関係ないスライダーを自動で無効化。任意パターン画像は `Application Support/newMosaic/Patterns/` へコピーして次回起動時も復元。設定はUserDefaults（`MosaicStyle.*`）で永続化。
- テスト: 全パターン×共通パラメータ適用で出力サイズが保たれることの検証と、縞マスク生成の検証を追加（計34テスト）。



■更新履歴（Readme / ChangeLog 用）

- 改善: アニメ部位検出の位置ずれを修正した。前処理をレターボックス方式（アスペクト比維持+パディング。モデル学習時と同条件）へ変更し、縦長の漫画ページ等での検出位置精度を向上。
- 新機能: マスク生成方式に「対象の形状（ROI内前景）」を追加した。矩形・楕円ではなく、検出範囲内の対象物（性器等）の実形状に沿ったモザイクを生成できる（前景が取れないROIは図形へフォールバック）。

■更新履歴

- `YOLOONNXModel.preprocess` をレターボックス方式（中央配置・グレー114パディング）へ変更し、`LetterboxTransform` による座標逆変換を追加（単体テスト付き）。従来のアスペクト無視リサイズは学習条件と不一致で位置ずれの原因だった。
- `RegionForegroundSegmentEngine` を新設。ROIごとに周辺1.3倍をクロップして `VNGenerateForegroundInstanceMaskRequest` を実行し、対象物の実形状マスクをROI範囲に制限して生成（クロップにより対象が主要被写体として大きく写るため形状が取りやすい）。

■補足（仕様説明）

- 「対象の形状」はモザイクマスクの形が対象に沿う方式であり、ROI枠の表示は従来通り矩形/楕円（枠は選択範囲、マスクが実形状）。


## v0.0.00001 - Build 34 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 候補生成時に「リムーバブルボリューム上のファイルへのアクセス権」ダイアログが表示される問題を修正した。同梱の検出モデルを初回のみ内蔵ディスク（Application Support/newMosaic/Models）へキャッシュし、以後はアプリバンドルへ一切触れない方式に変更。

■更新履歴

- 原因: アプリ本体がリムーバブル扱いのボリューム上にあり、候補生成時のONNXモデル初回読み込みがmacOSのリムーバブルボリューム保護に該当。さらにadhoc署名はビルドごとに別アプリ扱いになるため、ビルド更新のたびにダイアログが再表示されていた。
- `YOLOONNXModel` にモデルキャッシュ機構を追加（コピー済みマーカー+キャッシュ存在確認。マーカーがあればバンドル未アクセス）。モデル差し替え時はマーカーキーのバージョンで再コピーを制御。
- Info.plistへ `NSRemovableVolumesUsageDescription` を追加（初回コピー時のダイアログに用途説明を表示）。
- `Mosaic/DEBUG_LOG_INVENTORY.md` へモデルキャッシュの保存先・削除方法を追記。


## v0.0.00001 - Build 33 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- バグ修正: ROIのダブルクリック削除・範囲追加・候補生成などで「モザイク表示」チェックが勝手に解除される問題を修正した。チェック状態はユーザー操作でのみ変わり、編集中は一時的に元画像を表示し、編集完了時にチェックONなら自動でモザイクを再適用する。
- 改善: イラスト/漫画では人物検出をアニメ専用モデルへ切替えた。実写学習のVisionシルエットがイラストで部分的にしか反応しない問題（人物検知が中途半端になる報告）への対応。

■更新履歴

- キャンバスへ編集完了フック `onManualEditDidEnd` を追加（追加・移動・リサイズ・削除の完了時）。`dismissMosaicPreview`（チェックごと解除）を廃止し、`suspendMosaicPreview`（表示のみ一時停止）/`resumeMosaicPreviewIfNeeded`（チェックONなら再レンダリング）方式へ。Undo/Redo・候補生成・ROIクリアもチェック状態を維持して表示を復元する。
- deepghs/anime_person_detection person_detect_v1.3_s（MITライセンス, YOLOv8系ONNX, 約44.6MB, クラス: person）を同梱し、`AnimePersonDetector` を新設。イラスト/漫画判定時の人物検出は本モデル（矩形）を使用し、Visionシルエットは使用しない（シルエットマスク表示はアニメでは無し。将来のアニメセグメンテーションモデルで対応）。
- ONNX推論の共通ヘルパー `YOLOONNXModel` を新設し、部位検出・人物検出で共用。

■既知の問題（未修正・継続観測）

- イラスト/漫画の人物レイヤは矩形表示のみ（シルエットなし）。骨格もアニメ用モデル未導入のため骨格レイヤは生成されない。


## v0.0.00001 - Build 32 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 骨格の関節が1つも検出できていない人物に対して、固定比率のフォールバック矩形が「骨格検出N」レイヤとして表示されていた問題を修正した（アニメ等で偽の骨格枠が出るとの報告に対応。「検出していないものは表示しない」方針の徹底）。
- 候補生成のステータスへ骨格の実検出数を明示するようにした（例: 人物1名（骨格検出 0名））。

■更新履歴

- `rebuildDetectionLayers` を人物ごとの骨格有無（`poseAvailability`）ベースへ変更。骨格が検出できた人物のみ「人物N」グループへ骨格検出レイヤを追加し、検出できない人物は人物検出レイヤのみとする。編集状態の復元時も関節点の有無から骨格レイヤ有無を再現。

■補足（仕様説明）

- アニメ・イラストでは実写学習の骨格検出（VNDetectHumanBodyPoseRequest）はほぼ機能しない一方、人物シルエット検出はある程度汎化して動作する。アニメ対応の骨格検出（DWPose等）は計画書§6.1 Dで対応予定。


## v0.0.00001 - Build 31 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 白黒漫画が「実写」と誤判定されアニメ部位検出が実行されない問題を修正した（判定ロジックを彩度・紙白率・テクスチャ帯を使う複合方式へ強化）。
- ツールバーに「画像種別」（自動判定/実写/イラスト・漫画）の手動指定を追加した。判定が誤る場合でも「イラスト・漫画」固定にすればアニメ部位検出を必ず実行できる（設定は再起動後も保持）。

■更新履歴

- `DomainClassifier` を複合特徴（平坦率・中間差分率・紙白率・彩度平均）へ刷新。白黒漫画は「彩度ほぼゼロ+紙白15%以上+テクスチャ帯35%以下」で判定し、カラーは平坦率0.6+中間差分0.25で判定（従来の平坦率0.75単独では描き込みの多い漫画が漏れていた）。漫画風合成画像の判定テストを追加。
- 候補生成のステータスへ判定結果と判定方法（自動判定/手動指定）を常時表示するようにした。


## v0.0.00001 - Build 30 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 漫画・アニメ・イラスト向けの内容ベースNSFW部位検出を追加した。イラスト/漫画と判定した画像では、アニメ特化検出モデルが乳首・性器（男女）を画像内容から直接検出し、カテゴリ付き候補ROIとして自動生成する（従来の骨格ベース位置推定では不可能だった機能）。

■更新履歴

- `AnimeCensorDetector` を新設。deepghs/anime_censor_detection（censor_detect_v1.0_s, MITライセンス, YOLOv8系ONNX, 約44.6MB）を同梱し、ONNX Runtime（1.24.2, SwiftPM）で完全ローカル実行。クラス nipple_f/penis/pussy をアプリ内カテゴリ（乳首/性器（男性）/性器（女性））へ自動対応付け。
- `YOLODecoder` を新設（YOLOv8出力の属性メジャー配列デコード+クラス別NMS。単体テスト付き）。
- 候補生成時、`DomainClassifier` がイラスト/漫画と判定した場合にアニメ部位検出を実行し、既存の骨格ベース候補とIoU統合（内容ベース検出を優先）。ステータスへ「アニメ部位検出: N件」を表示。
- パッケージスクリプトへSwiftPMリソースバンドル（モデル同梱）のコピーを追加。
- `Mosaic/THIRD_PARTY_NOTICES.md` を新設（モデル・ライブラリのライセンス記録）。
- `NormalizedRect.iou(with:)` を追加。

■既知の問題（未修正・継続観測）

- アニメ部位検出はイラスト/漫画と判定された画像でのみ実行される（ドメイン判定は軽量統計方式のため誤判定があり得る。誤判定時は候補生成をやり直すか手動ROIで対応）。
- 実写画像の内容ベース検出（nudenet_onnx, Apache 2.0）は未導入（次段候補）。
- アニメ人物検出・骨格のアニメ対応（計画書§6.1 D）は未導入のため、イラストでは人物レイヤ・骨格レイヤは通常空となる。


## v0.0.00001 - Build 29 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 実写／イラスト・漫画のドメイン自動判定を追加した。イラスト・漫画と判定した場合、候補生成時に「アニメ用検出モデル未導入のため自動検出は限定的」である旨をステータスへ明示する。

■更新履歴

- `DomainClassifier` を新設（64x64縮小の隣接画素差の平坦率で判定する軽量統計方式。単体テスト付き）。将来アニメ判定モデルへ置き換え/併用できる独立構造で、アニメ検出モデル導入時の経路自動切替（計画書§6.1 C）の土台。


## v0.0.00001 - Build 28 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- ライブラリのアノテーション（元画像+保存済みROI）をYOLO形式の学習データセットとしてエクスポートする機能を追加した（「学習用エクスポート（YOLO形式）」ボタン）。以後のモザイク作業がそのまま漫画・アニメ・実写対応モデルの教師データになる。

■更新履歴

- `YOLODatasetExporter` を新設（images/・labels/（class cx cy w h 正規化座標）・classes.txt・dataset.yaml を出力。完全ローカル処理、単体テスト付き）。
- DETECTION_IMPROVEMENT_PLAN.md §6.1 B（自前ファインチューニング）の先行実装。


## v0.0.00001 - Build 27 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 骨格検出を「人物検知（シルエット）領域内で実行」する方式へ変更した。人物検知の方が安定しているというフィードバックを受け、人物ごとに領域をクロップして骨格検出を行い、検出関節をその人物のマスク内（+輪郭緩衝）に限定する。

■更新履歴

- `VisionPoseEstimator` を人物領域クロップ方式へ刷新: 人物矩形+15%マージンをクロップして `VNDetectHumanBodyPoseRequest` を人物ごとに実行。クロップ内で複数骨格が出た場合はマスク内関節数が最多のものを採用し、マスク外（緩衝0.03含む）の関節は隣接人物の写り込みとみなし除外。
- クロップにより人物が画面内で大きく写るため、関節検出の精度自体も向上する（小さく写る人物で特に有効）。
- `PersonMaskSampler.containsNear(x:y:radius:)`（緩衝付き内包判定）を追加（テスト追加）。
- Build 26の全体一括検出+貪欲マッチングは本方式に置き換え（対応付け誤りの余地自体がなくなる）。

## v0.0.00001 - Build 26 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 2人の人物矩形が大きく重なる構図で、骨格と人物の対応付け（人物Nグループへの割り振り）が誤る問題を修正した。シルエットマスク内に入る関節数を主基準とする貪欲マッチングへ変更。
- バグ修正: 保持している骨格関節がボーン描画閾値の不整合により描画されないことがある問題を修正した（保存閾値0.1に描画閾値を統一）。

■更新履歴

- `PersonMaskSampler` を新設。人物シルエットマスクを128x128へ縮小サンプリングし、関節の内包判定を高速に行う（単体テスト追加）。
- `VisionPoseEstimator` の対応付けを刷新: 全（人物×骨格）ペアについて「マスク内関節数（主基準）+中心距離（タイブレーク）」でスコアリングし、良いペアから貪欲に割当。マスクを持つ人物に関節が1つも入らない骨格は誤対応とみなし割り当てない。
- `NormalizedRect.contains(x:y:)` を追加。

■補足（仕様説明）

- シルエット（セグメンテーション）と骨格（姿勢推定）は別のVision APIのため、マスクが取れても骨格関節が取れないことがある（四つん這い・密着などの姿勢では関節信頼度が低く、検出できた関節のみ描画する正直表示によりボーンが少なくなる）。骨格検出率の根本改善はPhase 2本命（RTMPose等の高精度姿勢推定への差し替え検討を含む）で対応。

## v0.0.00001 - Build 25 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- レイヤパネルのデフォルト縦幅を「人物4人分が表示できる高さ」（約430pt=グループ4+子8+固定2の14行相当）に設定した。右ペインの高さが足りない場合はライブラリと半々の割合になる。

■更新履歴

- ウィンドウ表示後に一度だけ右ペインのディバイダ初期位置を適用する `applyInitialLayoutIfNeeded()` を追加（最小高さ制約: ライブラリ200pt/レイヤ160ptとの整合を考慮）。
- 適用済みフラグを永続化し、以後はユーザーのドラッグ調整（autosave位置）を尊重する。既存環境でも初回起動時に一度だけ新デフォルトが適用される。

## v0.0.00001 - Build 24 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 乳首ROIの生成基準を実画像フィードバックに基づき調整した（位置: 胴体の32%→42%へ下方修正、サイズ: 肩幅24%→14%へ縮小、横位置: 肩幅27%→22%へ内側修正）。
- 鼠径部ROIは左右の腰関節が両方検出できた場合のみ生成するようにした。腰・膝が取れない場合の低精度な推定（肩付近へ巨大な「その他」ROIが出る原因）は廃止。

■更新履歴

- `SensitiveROIGenerator` に `chestPositionRatio`（既定0.42）を追加し、乳首ROIの縦位置をコードから補正可能にした。
- 鼠径部ROIの生成条件を「左右腰関節の両方検出」へ厳格化し、root関節のみ・関節なしの固定比率フォールバック（`heuristic-lower-body`）を廃止。confidence も腰関節の実信頼度を使用。
- テストを新仕様へ更新（関節なしヒントは候補0件、乳首位置42%）。

■既知の問題（未修正・継続観測）

- 乳首・鼠径部とも位置ベース推定であり画像内容（何が写っているか）は見ていないため、姿勢によっては手・他部位への誤配置が残る。内容ベースの判定はDETECTION_IMPROVEMENT_PLAN.md Phase 2本命（部位検出モデル）で対応予定。
- 骨格の肩・腰が検出できない人物はROI候補0件となる（手動追加でカバー。検出率の根本改善もPhase 2本命）。

## v0.0.00001 - Build 23 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 鼠径部（性器相当）ROIのデフォルト位置を下方へ調整した（腰→膝の30%位置 → 45%位置。実画像でのフィードバック反映）。
- 鼠径部位置の基準をツールバーのスライダーで事前補正できるようにした（腰0%〜膝100%の20〜80%、数値表示付き。設定は再起動後も保持され、次回の候補生成から適用）。
- 骨格レイヤのボーン線を枠線より2段階太い線（4px・丸端）、枠線より暗いオレンジで描画するようにした（関節点も同色・拡大）。

■更新履歴

- `SensitiveROIGenerator.groinPositionRatio`（既定0.45、0.05〜0.95へクランプ）を追加し、膝なしフォールバックも比率連動化。コントローラから同一インスタンスを注入し、スライダー変更を即反映（UserDefaultsで永続化）。
- 位置比率の反映を検証する単体テストを追加。

■補足（仕様説明）

- 鼠径部ROIの範囲形状（矩形/楕円）は、Build 18以降すべての自動候補にツールバー「追加形状」の選択が適用される（鼠径部含む）。
- 「グラフィカルに腰・膝間の位置を指定」する方式（骨格レイヤ上のドラッグ指定）は将来拡張として検討中。まず数値スライダーで基準補正を提供する。

## v0.0.00001 - Build 22 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- バグ修正: Build 20の修正でも解消していなかったツールバーのレイアウト崩れを修正した（3度目の対処。今回はAPI選定誤りの是正）。

■更新履歴

- 原因: Build 20で使用した `setContentHuggingPriority`（NSView汎用API）は内在サイズを持つビュー向けで、内在サイズを持たない `NSStackView` には効かない。
- 修正: スタック境界を内容へフィットさせる `NSStackView` 専用API `setHuggingPriority(.required, for: .vertical)` へ置き換え、ツールバー3段を内容ぴったりの高さに固定。縦余白は常にキャンバス側（splitView）が吸収する。

## v0.0.00001 - Build 21 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- ライブラリからの選択画像削除機能を追加した（「選択画像を削除」ボタンまたはDeleteキー。確認ダイアログ付きで元画像・加工後画像とも完全削除）。
- ライブラリの複数選択に対応した。Shift+クリックで範囲選択、Cmd+クリックで個別追加選択ができ、一括削除が可能（グリッド/テキスト/サムネイルの全表示モード対応）。

■更新履歴

- `LibraryEngine.deleteItems(ids:)` を追加（索引・元画像PNG・加工後PNGを削除。単体テスト追加）。
- `NSTableView` / `NSCollectionView` の `allowsMultipleSelection` を有効化し、Deleteキー（backspace/forward delete）でも削除確認を起動。
- 表示中の画像を削除した場合はキャンバスをプレースホルダへ戻し、編集状態キャッシュ・アンドゥ履歴も破棄。

■補足

- macOSの標準操作に合わせ、個別追加選択は **Cmd+クリック**（WindowsのCtrl+クリック相当）。Ctrl+クリックはmacOSでは右クリック扱いのため使用しない。

## v0.0.00001 - Build 20 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- バグ修正: Build 18でも残っていたツールバーのレイアウト崩れ（チェックボックス行が画面中央へ浮き、キャンバスが下へ押し込まれる）を修正した。

■更新履歴

- 原因: ツールバー3段（操作／形状・カテゴリ・マスク生成／チェックボックス群）に縦方向の content hugging が未設定で、Auto Layout が縦の余白をチェックボックス行のスタックへ割り当てて引き伸ばしていた。
- 修正: 3段すべてに `setContentHuggingPriority(.required, for: .vertical)` を設定し、余白は常にキャンバス側（splitView）が吸収するようにした。

## v0.0.00001 - Build 19 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 候補生成の実行後、レイヤパネル内のすべてのレイヤ（グループ内含む）を自動的に表示状態にするようにした。
- キャンバス上の検出レイヤ（人物検出N・骨格検出N）の範囲右下にレイヤ名を表示するようにした。

■更新履歴

- `showAllLayers()` を追加し、候補生成後にレイヤの全チェックON+ツールバーチェック同期+全展開再読込を行うようにした。
- `ImageCanvasView.drawLayerName` を追加し、人物検出=青／骨格検出=橙の色付き背景でレイヤ名を範囲内右下に描画。

## v0.0.00001 - Build 18 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- バグ修正: 人物検出レイヤのシルエットマスクが上下反転して表示される問題を修正した（Vision人物セグメンテーション／前景オブジェクトのモザイクマスクにも同じ反転があったため併せて修正）。
- バグ修正: UIツールバーの項目過多によるレイアウト崩れを修正した（編集ツールバーを2行へ分割、ステータス文の長文時は末尾省略、ウィンドウ最小サイズを設定）。
- 改善: 「追加形状」の選択（矩形/楕円）を自動候補生成のROIにも適用するようにした（従来は楕円固定）。
- 改善: ROIにカテゴリ名ラベルを表示し、「対象カテゴリ」の変更が画面上で確認できるようにした（変更時はステータスにも表示）。

■更新履歴

- `CIImage(cvPixelBuffer:)` 由来のマスクへ垂直反転補正 `verticallyFlippedForRaster()` を導入し、表示用シルエット（`VisionPersonDetector`）・`VisionPersonSegmentEngine`・`ForegroundSegmentEngine` の3経路に適用。CVPixelBufferとCGImageラスタの座標規約差が原因。
- ツールバーを3段構成（操作ボタン+ステータス／形状・カテゴリ・マスク生成／レイヤ表示・自動化チェック）へ再編。`contentMinSize` 1100x700 を設定。
- 候補生成後のROIへ `canvas.currentShape` を一括適用（学習提案 `learned-prior` 含む）。
- `ImageCanvasView.drawCategoryLabel` を追加（ROI左上にカテゴリ名を色付き背景で表示）。

■補足（仕様説明）

- 「対象カテゴリ」は新規手動ROIへ付与するラベルと、選択中ROIのラベル変更に作用する（自動候補の生成部位を絞り込むフィルタではない）。カテゴリ別の実部位検出はDETECTION_IMPROVEMENT_PLAN.md Phase 2本命で対応予定。
- 骨格ボーン・モザイク対象ROIの座標系は上下反転していない（骨格座標は変換済み）。見え方に引き続き違和感がある場合はスクリーンショットで再報告いただきたい。

## v0.0.00001 - Build 17 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- レイヤリストをポップオーバーからアプリウィンドウ右下（ライブラリの下）の常設パネルへ変更した。
- キャンバスと右ペイン（ライブラリ+レイヤ）の左端境界を左右ドラッグで幅変更、ライブラリとレイヤの境界を上下ドラッグで比率変更できるようにした（位置はアプリ再起動後も記憶）。
- レイヤ一覧は通常時、すべての階層（グループ内含む）を展開した状態で表示するようにした。

■更新履歴

- 右ペインを縦 `NSSplitView`（上=ライブラリ / 下=レイヤ）で構成し、メイン分割・右ペイン分割とも `autosaveName` でディバイダ位置を永続化。
- 「レイヤ...」ボタンと `NSPopover` を廃止し、`makeLayerPanel()` による常設パネルへ移行（グループ化/グループ解除ボタンはパネル下部に維持）。
- `reloadLayerList()`（reloadData+全展開）を導入し、レイヤ一覧の全更新経路で常に全階層展開を維持。

■確認事項

- ユーザーより、実写画像での検出表示（人物1グループ=人物検出1+骨格検出1の自動グループ化、青シルエット、骨格矩形、ROI）が実装意図通りである旨の確認を受領。

## v0.0.00001 - Build 16 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 仕様確定: 人物を検出できない画像では偽の固定矩形を表示せず、候補0件として「手動追加してください」と案内するようにした（Build 14で一時復活させた互換フォールバックを廃止）。

■更新履歴

- 経緯: Phase 1以前の「常に1件返る人物矩形」は実際には検出結果ではなく固定比率の想定枠だったことをユーザーへ報告し、「検出していない枠線を表示しない＝正確な検知内容の表示を優先」との方針指示を受けて確定。
- `VisionPersonDetector` の検出0件時フォールバック矩形を削除（インスタンスマスク→人物矩形の2段検出のみ。0件は0件として返す）。
- 候補生成で人物0件・候補0件のとき、ステータスへ「人物を検出できませんでした（候補0件）。ドラッグで手動追加してください」を表示。
- テストを新方針へ更新（人物なし画像で検出0件・候補0件を検証）。

■既知の問題（未修正・継続観測）

- 漫画・イラストは実写学習のVisionでは検出率が低く、候補0件になりやすい。手動ROI+ローカル学習（Phase 4）で補いつつ、根本改善はPhase 2本命（漫画+実写データでのファインチューニング）で対応予定。

## v0.0.00001 - Build 15 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 画像ごとに編集状態（ROI・検出レイヤ・アンドゥ履歴・モザイク表示状態）をセッション内で保持し、ライブラリで画像を切り替えて戻ってきても編集を継続できるようにした。
- 「モザイク表示」チェックボックスを追加し、モザイクの解除/再適用をワンクリックで切替できるようにした。解除中はROIが保持され再編集可能。

■更新履歴

- `PerImageEditState` によるセッション内キャッシュ（直近8画像、LRU方式でメモリ抑制）を追加。画像切替時に自動退避し、戻ってきたときに復元する。
- 「モザイク表示」トグル: ONで現在のROIとマスク生成方式による未保存プレビューを表示、OFFで元画像+ROI表示。編集開始・候補生成・ROIクリアで自動解除され、アンドゥ/リドゥとも状態同期する。
- ライブラリ画像を開く際、作業画像を常に元画像とし、加工後画像はモザイク表示側へ読み込む方式に変更。加工済み画像でも解除→再編集→再適用が可能になった。
- 仕様変更: 「元画像を開く」でも保存済みROIを復元するようにした（再編集の起点。まっさらに戻す場合は「ROIクリア」を使用）。

■既知の問題（未修正・継続観測）

- 編集状態のセッション内キャッシュは直近8画像まで（超過分は古い順に破棄。保存済みデータはライブラリに残るため失われない）。
- アプリ再起動でセッション内キャッシュは消える（保存済みROI・加工後画像はライブラリから復元される）。

## v0.0.00001 - Build 14 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- バグ修正: Phase 1実装後、漫画・イラスト等でVisionが人物を検出できない画像において自動認識が全く動作しなくなっていた問題を修正した（マスク生成方式の選択に関わらず候補ROIが0件になる症状）。

■更新履歴

- 原因: Phase 1以前は検出成否に関わらず固定比率の人物想定矩形を必ず1件返していたが、Phase 1で「検出0件なら空配列」へ変更したため、実写学習のVision（インスタンスマスク・人物矩形とも）が反応しない漫画画像で候補が全滅していた。
- 修正: `VisionPersonDetector.detectPersons` に検出0件時の従来互換フォールバック（中央の人物想定矩形1件）を復活。下流のヒューリスティックROI生成と手動調整の起点を常に確保する。
- 併せて `VNDetectHumanRectanglesRequest` に revision 2 を明示指定（`upperBodyOnly=false` の全身矩形はrevision 2でのみ有効なため）。
- 回帰防止テストを2件追加（人物なし画像でのフォールバック矩形、パイプライン全体で候補が必ず1件以上）。

## v0.0.00001 - Build 13 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 自動認識Phase 4: モザイク範囲選択のローカル学習を追加した。採用/削除したROIを収集し、選択位置の頻度・サイズ・パッチの近似画像参照（知覚ハッシュ）で候補生成の精度を継続的に改善する。学習データはすべてローカル保存で外部送信しない。

■更新履歴

- `LearningEngine` を新設（`Application Support/newMosaic/Learning/`）。保存時に採用ROI=正例、削除された自動候補=負例として記録し、パッチPNG（最大256px）も収集（将来の部位検出モデル学習データを兼ねる）。
- カテゴリ別に人物相対8x8位置頻度グリッド・平均サイズ・dHash知覚ハッシュを蓄積。候補生成時に位置頻度ブースト（最大+0.3）、正例近似で+0.2/負例近似で−0.25の信頼度調整、高頻度位置への `learned-prior` ROI自動提案（最大8件）を行う。
- 近似画像参照は「Google画像検索型の類似検索」をローカル完結のdHashハミング距離近傍探索で代替（外部サービス不使用・プライバシー準拠）。
- 処理負荷: 集計は保存時のみ、推論時はグリッド参照+線形ハッシュ走査で数千サンプルまで1ms級。
- `Mosaic/DEBUG_LOG_INVENTORY.md` に学習ストアの保存先・個人情報の有無・削除方法を追記。

■既知の問題（未修正・継続観測）

- 学習効果はサンプル5件以上から発現する設計（それ未満は従来動作と同一）。
- パッチ収集はモザイク対象範囲の画像断片を含むため、`Learning/` フォルダの取り扱いに注意（削除すれば完全消去）。

## v0.0.00001 - Build 12 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 自動認識Phase 3: マスク生成方式に「前景オブジェクト」を追加した。被写体の画素単位マスクに沿ったモザイクを生成できる。

■更新履歴

- `ForegroundSegmentEngine` を新設。`VNGenerateForegroundInstanceMaskRequest` で前景（人物・物体）の画素マスクを取得し、ROIごとに切り出してモザイクマスクに使用。前景が得られない場合は図形ベースへフォールバック。
- ツールバー「マスク生成」ポップアップに「前景オブジェクト」を追加（`SegmentEngineKind.foregroundObjects`）。
- モデル選定: SAM/MobileSAM等の外部モデル同梱（数百MB+CoreML変換工数）を避け、macOS標準の前景インスタンスマスクAPIを採用（追加コスト0・完全ローカル）。`Segmenting` 境界は維持しているため、将来SAM系CoreMLへの差し替えも可能。

## v0.0.00001 - Build 11 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 自動認識Phase 2（暫定）: 自動候補ROIをVisionの顕著領域検出で精密化する `SaliencyCandidateDetector` を追加し、候補生成パイプラインの既定にした。

■更新履歴

- `SaliencyCandidateDetector` を新設。自動候補（pose-chest / pose-groin / heuristic-lower-body）の周辺1.8倍領域をクロップし `VNGenerateObjectnessBasedSaliencyImageRequest` を実行、元ROIとの重なり率0.2以上・面積4倍以内の顕著領域があればROIをそこへ精密化する。整合しない場合は元ROIを維持（再現率優先）。
- `NormalizedRect` に `area` / `expanded(scale:)` / `intersection(_:)` を追加。
- モデル選定: 学習済み部位検出モデル（Phase 2本命）はデータセットとライセンス確認が必要なため、追加コスト0のVisionサリエンシーを暫定採用。プロトコル境界は維持しており、部位検出モデル取得後に `CandidateDetecting` 実装を差し替えるだけで移行できる。

■既知の問題（未修正・継続観測）

- サリエンシーは「目立つ物体」への吸着であり部位の意味分類ではない。カテゴリ別の真の部位検出はデータセット整備後に対応。

## v0.0.00001 - Build 10 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 自動認識Phase 1: Vision標準APIによる複数人物の実位置検出・骨格検出に対応した。固定比率ヒューリスティックを廃止。
- 骨格検出レイヤに関節点とボーン線、人物検出レイヤに破線矩形+人物シルエット（青半透明）を表示するようにした（従来の保留2件を解除）。
- 乳首カテゴリのROI（肩幅・胴体比から左右2点）と鼠径部ROI（腰・膝関節から算出）を人物ごとに自動生成するようにした。

■更新履歴

- `VisionPersonDetector` を `VNGeneratePersonInstanceMaskRequest`（最大4人、インスタンス別マスク）ベースに書き換え。ラベルマップから人物ごとの外接矩形を算出し、シルエットマスクCGImageも取得。失敗時は `VNDetectHumanRectanglesRequest` へフォールバック。
- `VisionPoseEstimator` を新設。`VNDetectHumanBodyPoseRequest`（複数人物）の15関節を `PoseJoint`（正準名enum）として `PoseHint.joints` に保持し、胴体中心距離で人物へ対応付け。
- `SensitiveROIGenerator` を関節ベースの解剖学的プライア方式に書き換え（関節なしは従来比率へフォールバック、既存動作維持）。
- モデル選定: Apple Vision標準API（追加コスト0・完全ローカル・macOS 14前提で可用性100%）を採用。

■既知の問題（未修正・継続観測）

- 鼠径部ROIのカテゴリは性別分類器が未導入のため `.other`（男女判別はPhase 2で対応予定）。
- 人物インスタンスマスクは最大4人（Vision制約）。5人以上は矩形検出のみに縮退。
- 検証画像が漫画の場合、Vision（実写学習）の検出率は実写より低い（ドメインギャップ、Phase 2で対応検討）。

## v0.0.00001 - Build 9 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- 認識範囲（ROI）を左ドラッグで移動できるようにした（従来はクリック選択のみで移動不可だった）。
- 複数人物の乳首・性器の自動認識改善計画を技術サーベイに基づき策定し、`Mosaic/DETECTION_IMPROVEMENT_PLAN.md` として追加した。

■更新履歴

- `ImageCanvasView` に `MoveState` を追加。ROI内側の左ドラッグで位置移動（サイズ維持・画像範囲内クランプ）。ドラッグ閾値超過時に一度だけアンドゥスナップショットを記録し、閾値未満の単純クリックは従来通り選択のみ。
- リサイズハンドル優先・ダブルクリック削除・空き領域のドラッグ新規作成など既存操作との優先順位は変更なし。

■更新履歴（画面・機能別）

- macOS: ROIの内側をドラッグすると移動。四隅ハンドルはリサイズ、ダブルクリックは削除（従来通り）。

■既知の問題（未修正・継続観測）

- 自動検出は固定比率ヒューリスティックのまま（改善はDETECTION_IMPROVEMENT_PLAN.mdのPhase 1以降で対応予定）。
- 骨格ボーン表示・人物シルエット破線表示は保留継続（Phase 1のVision標準API接続で実データが揃い次第解除）。

## v0.0.00001 - Build 8 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- `SegmentEngine` を実際にモザイク処理から呼び出すようにし、Visionの人物セグメンテーション結果を使ったマスク生成を選べるようにした。既存の図形ベースマスク生成と機能が重複するため、ツールバーの設定で切替可能にした。

■更新履歴

- `Segmenting` プロトコルを `createMasks(for:in:extent:) throws -> [CIImage]` に刷新（従来の `MosaicMask`/`EllipseSegmentEngine` は置き換え）。
- `ShapeSegmentEngine` を追加。従来 `MosaicEngine` に内蔵されていた矩形/楕円マスク生成ロジックをこちらへ移設。
- `VisionPersonSegmentEngine` を追加。`VNGeneratePersonSegmentationRequest` を画像全体に対し1回実行し、結果マスクをROIごとに切り出す。Vision結果が得られない場合は `ShapeSegmentEngine` にフォールバックする。
- `MosaicEngine.applyMosaic` に `segmentEngine` 引数を追加（既定値 `ShapeSegmentEngine()` で後方互換）。
- ツールバーに「マスク生成」ポップアップ（図形ベース／Vision人物セグメンテーション）を追加し、「モザイク適用」時に選択方式を使用するようにした。

■更新履歴（画面・機能別）

- macOS: ツールバーに「マスク生成」ポップアップを追加。「図形ベース（矩形/楕円）」「Vision人物セグメンテーション」を切替可能。

■既知の問題（未修正・継続観測）

- `VisionPersonSegmentEngine` はマスク生成にVisionの実セグメンテーション結果を使うが、`PersonDetector`（ROI自体の位置決定）と`PoseEstimator`は依然固定比率ヒューリスティックのまま。マスクの「形」はVision準拠でも、「どこにROIを置くか」は実検出ではない。
- カテゴリ（乳首・性器等）ごとの実形状認識は未実装（ユーザー提供の参照画像待ち）。
- 骨格検出レイヤのボーン表示、人物検出レイヤの実シルエット破線表示は保留中。

## v0.0.00001 - Build 7 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- モザイク範囲をダブルクリックで削除できるようにした。
- 画像切替時の「自動候補生成」「自動保存」ON/OFF設定を追加した。
- ライブラリ一覧でカーソルキー（矢印キー）による画像切替に対応した。自動保存OFF時は未保存内容の保存確認ダイアログを表示する。
- 複数人物・複数骨格の検出に対応し、検出のたびに人物ごとレイヤを自動グループ化するようにした。

■更新履歴

- `ImageCanvasView.mouseDown` にダブルクリック判定を追加し、既存ROIをヒットテストして削除する処理を実装。
- ツールバーに「自動候補生成」「自動保存」チェックボックスを追加。画像を開く/貼り付け/ライブラリ切替のたびに自動候補生成を、カーソルキーでのライブラリ切替時に自動保存または保存確認ダイアログを行う。
- `NavigableTableView` / `NavigableCollectionView`（矢印キーをハンドリングするカスタムビュー）を追加し、ライブラリ一覧でのカーソルキー画像切替を実装。
- `LayerKind` を `person(Int)` / `pose(Int)` の連番対応に変更し、候補生成のたびに検出数に応じてレイヤを再構築。人物・骨格ペアが揃う場合は「人物N」グループへ自動集約し、揃わない場合はグループ外に追加。

■更新履歴（画面・機能別）

- macOS: モザイク範囲のダブルクリック削除に対応。
- macOS: ツールバーに「自動候補生成」「自動保存」チェックボックスを追加。
- macOS: ライブラリ一覧にフォーカスした状態で矢印キーを押すと前後の画像へ切替（自動保存/保存確認付き）。
- macOS: 複数人物検出時、レイヤパネルに「人物1」「人物2」...の自動グループが生成される。

■保留（未実装・今回は見送り）

- 骨格検出レイヤのボーン（関節線）表示: `HeuristicPoseEstimator` が関節・骨格情報を一切保持していないため、描画対象となる実データが存在しない。骨格検出APIへの接続後に対応する。
- 人物検出レイヤの実シルエット破線表示: `VNGeneratePersonSegmentationRequest` の画素マスク結果を現状破棄しており、輪郭抽出（ベクトル化）が未実装のため保留。

■既知の問題（未修正・継続観測）

- 自動検出（`PersonDetector` / `PoseEstimator` / `ROIGenerator` / `CandidateDetector`）は固定比率の算術ヒューリスティックのままで、構造上は複数人物対応済みだが実際には常に高々1件しか検出しない。カテゴリ（乳首・性器等）ごとの実形状認識も未実装。
- `SegmentEngine`（`EllipseSegmentEngine`）はパイプラインから未接続。
- ROIの位置移動（ドラッグでの平行移動）は未対応。
- カーソルキーでの画像切替時の保存確認は、マウスクリックによる「元画像を開く」「加工後を開く」には適用されない（意図的なスコープ限定）。
- 自動保存はアイテムの更新日時を更新するため、`updatedAt` 降順のライブラリ一覧の並び順がブラウズ中に変化する。
- 形状ごとの記憶サイズ、レイヤのグループ構成・表示状態はセッション内のみでアプリ再起動後は保持されない。

## v0.0.00001 - Build 6 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- モザイク対象ROIに検出カテゴリ（乳首／性器（女性）／性器（男性）／その他）のラベル付けを追加した。ツールバーから新規ROI・選択中ROIへ付与できる。
- 画像・人物検出・骨格検出・モザイク対象の4レイヤを個別に表示/非表示切替でき、複数レイヤをグループ化して一括操作・グループ解除できるレイヤパネルを追加した。

■更新履歴

- `MosaicROI` に `category: MosaicTargetCategory` を追加。既存データ（categoryキー無し）は `.other` として後方互換デコードする。
- ツールバーに「対象カテゴリ」ポップアップを追加。
- 「レイヤ...」ボタンから開くレイヤパネル（`NSOutlineView`）を追加し、レイヤの表示切替・複数選択によるグループ化・グループ解除に対応。既存の「人物検出レイヤ」「骨格検出レイヤ」チェックボックスとは表示状態を双方向同期する。
- `ImageCanvasView` に画像レイヤ・ROI(モザイク対象)レイヤそれぞれの表示切替を追加。

■更新履歴（画面・機能別）

- macOS: ツールバー2段目に「対象カテゴリ」ポップアップと「レイヤ...」ボタンを追加。
- macOS: レイヤパネルで2つ以上のレイヤを選択して「グループ化」すると、1つのマスターチェックボックスで一括ON/OFFできる。「グループ解除」で個別レイヤに戻せる。

■既知の問題（未修正・継続観測）

- 自動検出（`PersonDetector` / `PoseEstimator` / `ROIGenerator` / `CandidateDetector`）は固定比率の算術ヒューリスティックのままで、カテゴリ（乳首・性器等）ごとの実形状認識は未実装。ユーザー提供の参照画像を受け取り次第、対応を検討する。
- `SegmentEngine`（`EllipseSegmentEngine`）はパイプラインから未接続で、実際のマスク生成は `MosaicEngine` が別途重複実装している。
- ROIの移動（ドラッグでの位置変更）とキーボードでの個別削除は未対応（クリア全消去のみ）。
- 形状ごとの記憶サイズ、レイヤのグループ構成・表示状態はセッション内のみでアプリ再起動後は保持されない。

## v0.0.00001 - Build 4 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- ウィンドウタイトルにリリース番号（v<MARKETING_VERSION> (beta Build <CURRENT_PROJECT_VERSION>)）を表示するようにした。
- モザイク対象ROIに矩形／楕円の形状選択を追加し、直前に使ったサイズを形状ごとに記憶してワンクリックで同サイズ追加できるようにした。既存ROIは選択後にサイズ変更・形状変更ができる。
- 人物検出・骨格検出の中間結果を画像上に半透明レイヤとして個別表示できるようにした。

■更新履歴

- `MosaicROI` に `shape: ROIShape`（矩形／楕円）を追加。既存データ（shapeキー無し）は `.ellipse` として後方互換デコードする。
- `MosaicEngine` に矩形マスク生成を追加し、ROIの形状に応じてモザイク描画を切り替える。
- `StaticImageMosaicPipeline.generateDetailedCandidates(for:)` を追加し、人物検出・骨格ヒント・ROIの中間結果を取得可能にした。
- `ImageCanvasView` にROI選択・四隅リサイズハンドル・形状別プレビュー描画・検出レイヤ描画を追加した。

■更新履歴（画面・機能別）

- macOS: ツールバー2段目に「追加形状（矩形／楕円）」「人物検出レイヤ」「骨格検出レイヤ」を追加。
- macOS: キャンバス上でROIをクリック選択→四隅ハンドルでリサイズ、形状ボタンで形状変更が可能。
- macOS: 記憶済みサイズがある形状は、空き領域をクリックするだけでそのサイズのROIを追加できる。

■既知の問題（未修正・継続観測）

- 自動検出（`PersonDetector` / `PoseEstimator` / `ROIGenerator` / `CandidateDetector`）は固定比率の算術ヒューリスティックのままで、画像内容に応じた実検出は未実装。
- `SegmentEngine`（`EllipseSegmentEngine`）はパイプラインから未接続で、実際のマスク生成は `MosaicEngine` が別途重複実装している。
- ROIの移動（ドラッグでの位置変更）とキーボードでの個別削除は未対応（クリア全消去のみ）。
- 形状ごとの記憶サイズはセッション内のみでアプリ再起動後は保持されない。

## v0.0.00001 - Build 3 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- ライブラリ画面に表示モード切替（グリッド／テキストのみ／サムネイル付きリスト）を追加した。
- モザイク編集操作（ROI手動追加・候補生成・クリア・モザイク適用）のアンドゥ・リドゥに対応した。

■更新履歴

- `NSCollectionView` によるサムネイルグリッド表示を追加し、`NSSlider` でサムネイルサイズを可変にした。
- `NSTableView` のリスト表示にテキストのみ／サムネイル付きの2種類のセルを追加した。
- 編集状態のスナップショットを保持するアンドゥ・リドゥ機構を追加し、画像切替時に履歴をクリアするようにした。

■更新履歴（画面・機能別）

- macOS: ライブラリパネルの表示モードをセグメントコントロールで切替可能。
- macOS: `command + Z` / `command + shift + Z` でROI編集・モザイク適用を戻す・やり直せる。

■既知の問題（未修正・継続観測）

- 自動検出（`PersonDetector` / `PoseEstimator` / `ROIGenerator` / `CandidateDetector`）は固定比率の算術ヒューリスティックのままで、画像内容に応じた実検出は未実装。
- `SegmentEngine`（`EllipseSegmentEngine`）はパイプラインから未接続で、実際のマスク生成は `MosaicEngine` が別途重複実装している。

## v0.0.00001 - Build 2 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- ブラウザ等でコピーした画像を、macOS ペーストボードから直接インポートできるようにした。
- 検証用ライブラリを追加し、元画像とモザイク加工後画像をローカル保存・一覧管理できるようにした。

■更新履歴

- `LibraryEngine` を追加し、`Application Support/newMosaic/Library` 配下に `Originals` / `Processed` / `index.json` を保存する構成にした。
- メイン画面に「画像を貼り付け」「ライブラリ更新」「Finder表示」とライブラリ一覧を追加した。

■更新履歴（画面・機能別）

- macOS: `command + V` または「画像を貼り付け」でクリップボード画像をインポート。
- macOS: ライブラリ一覧から元画像・加工後画像を再表示可能。
- Core: 元画像保存、加工後画像保存、ROI付き索引管理を追加。

■既知の問題（未修正・継続観測）

- ブラウザ側が画像ではなくURL文字列のみをコピーした場合は、現時点では画像として取り込まれない。

## v0.0.00001 - Build 1 - 2026-07-22

■更新履歴（Readme / ChangeLog 用）

- newMosaic macOS MVP の初期リリース。
- 静止画読み込み、ROI候補生成、手動ROI追加、モザイク適用、PNG保存の基盤を追加。

■更新履歴

- maruPlay 由来のAI共同開発、チャットログ、品質管理、リリース運用ルールを newMosaic 向けに移植。
- UI と画像処理を分離した Swift Package 構成を追加。

■更新履歴（画面・機能別）

- macOS: 画像を開く、候補生成、モザイク適用、保存。
- Core: ROI生成、マスク生成、モザイク生成、履歴保存。

■既知の問題（未修正・継続観測）

- MediaPipe、SAM2、ONNX Runtime は初期MVPでは未接続。
- 動画対応は未実装。
