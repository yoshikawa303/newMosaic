### 2026-08-04 JST - Claude Sonnet 5 - 種別: 依頼内容 - 残務の全コードレビュー

```
★残務の全コードレビューして
```

（続けて `★再開して`）

### 2026-08-04 JST - Claude Sonnet 5 - 種別: 作業結果 - v0.0.00124

- 作業AIモデル: Claude Sonnet 5（本セッションはモデル切替の仕組みを持たないため単一モデルで対応）
- 前ターン（v0.0.00123作業時）で依頼されたが未着手だった「全コードの複数回レビュー」を再開。
  前回レビュー記録（`QC_CodeReview_v0.0.00122.md`）以降の未レビュー分、つまり
  v0.0.00122（`b8777bd`）・v0.0.00123（`469e9b2`）の差分を対象に静的レビューを実施した。
- v0.0.00122で「修正済み」とされた5件（maskShapeScale再計算漏れ／モザイク表示チェックの
  非表示レイヤ無視／マスク追加ペンの2段階Undo／ツールチップ・コメントの旧ラベル残存）を、
  呼び出し元まで遡って再検証。いずれも正しく機能していることを確認（新規の機能バグは検出せず）。
- 運用文書側で4件を検出・修正:
  1. (P2) `Mosaic/ARCHITECTURE.md` の章番号「§5.65」が2つ存在（v0.0.00122分とv0.0.00123分が
     重複）→ v0.0.00123側を§5.66へ改番。
  2. (P2) 同ファイル§5.58.3が、v0.0.00119でツールバーを2モード（マスク追加ペン／マスク消しゴム）
     へ分割した後も旧仕様（単一「マスク修正モード」・3セグメント）のまま記載されていた（§5.61と
     矛盾）→ 現状に合わせて修正。
  3. (P3・情報共有) `SAMSegmentEngine.minimumUsableCoverage`（v0.0.00123で0.02→0.40へ較正）は
     クラス共通の定数で乳首・乳輪ROIにも適用されるが、v0.0.00123の実測・回帰テストは性器ROIのみ
     を対象にしていたため、乳首・乳輪への副作用は未検証であることを記録（対応は保留）。
  4. (P4) `Mosaic/QUALITY_STATS.md` のテスト件数表記（147件）がCHANGELOG記載（148件）と
     食い違っていた → 同期。
- コード（Swift）側の変更は無し。テスト148件PASS（既存のまま）、両ゲート通過。
- QC記録を `Docs/QC/CodeReview/QC_CodeReview_v0.0.00124.md` に作成。
- 作業時間: 約1.2時間

### 2026-08-06 22:30 JST - Claude Sonnet 5 - 種別: 依頼内容 - GUIバグ・改善8件の報告

```
★バグ：添付画像のツールバーが他のアイコンとサイズが異なる。編集モード変更、マスク修正アイコンが詳細設定のアイコンサイズ変更で設定が変わらない。添付画像参照
★バグ：人物生成マスクが上で反転で表示される。添付画像参照
　　　　人間骨格検知しきれていない。（F5D9FF5D画像、ログ添付）
★バグ：デバッグログに、デバッグに必要なユーザー操作内容（ボタン、メニュー等実行操作名(UI画面上の表示名)の実行記録）がない。
★バグ：デバッグログのクリア操作を行っても、過去ログがすべて削除されていない。添付画像参照
★改善：ヘルプメニュー直下に、デバッグログ表示メニューを移動し、ログウィンドウ表示しやすくする。
★バグ：乳首、乳輪誤検知。男性器、乳首・乳輪検知位置、サイズ等いちずれ。添付画像参照（7624B71B画像、ログ添付）
★バグ：人物検知。骨格検知が微妙。添付画像参照（D898DA82画像、ログ添付）
★バグ：人物検知、マスク表示微妙。漫画コマ右下男性性器が形状にそって範囲選択、モザイクマスク生成されていない。添付画像参照（8C52ADAB画像、ログ添付）
```

### 2026-08-06 22:55 JST - Claude Sonnet 5 - 種別: 作業結果 - v0.0.00125 ツールバーアイコンサイズ不一致の修正

- 作業AIモデル: Claude Sonnet 5（UI定型修正のため軽量モデルで実施。本セッションはモデル切替の仕組みを持たないため単一モデルで対応）
- 報告8件のうち1件目「ツールバーのモード切替・学習モードアイコンのサイズ不一致」を修正。
- 原因は2系統:
  1. `canvasModeControl`（`NSSegmentedControl`）が `toolbarIconButtons` の `NSButton` 経路を通らず、
     アイコンサイズ設定（SymbolConfigurationのpointSize）が一切適用されていなかった。
  2. 学習モードボタンは登録済みだったが、`refreshLearningModeButton()` がON/OFF状態の記号を
     SymbolConfiguration なしで上書きし、常に既定サイズへ戻していた。
- 共通ヘルパ `currentIconSymbolConfiguration()` を新設して3経路（通常ボタン／モード切替セグメント／
  学習モードボタン）で共用。`applyIconSizeToAllToolbarButtons()` からセグメント幅・学習モード状態も再適用。
- 残り7件（人物マスク反転・骨格検知、ユーザー操作ログ、ログクリア残存、メニュー移動、
  photo検知位置ずれ、SAM形状追従）は継続対応。

### 2026-08-06 23:05 JST - Claude Sonnet 5 - 種別: 作業結果 - v0.0.00126 デバッグログ消去の残存修正

- 作業AIモデル: Claude Sonnet 5
- 「消去…」実行後も今回起動分の過去ログが表示に残っていた問題を修正。
- 原因: 表示側は毎回OSLogStoreから今回起動分（直近10分）を再取得しており、
  旧修正（v0.0.00119の退避位置前進）は退避ファイルへの復活だけを防いでいた。
- 消去時刻（debugLogClearedAt）を記録し、読み出し側でそれ以前のエントリを除外する方式へ変更。
  markCurrentLogAsArchived()は削除。DEBUG_LOG_INVENTORY.mdも更新。

### 2026-08-06 23:15 JST - Claude Sonnet 5 - 種別: 作業結果 - v0.0.00127 デバッグログメニューをヘルプ直下へ移動

- 作業AIモデル: Claude Sonnet 5
- 中間の「デバッグ」サブメニューを廃止し、「デバッグログ…」をヘルプメニュー直下へ移動（ユーザー要望）。
- ARCHITECTURE.md / DEBUG_LOG_INVENTORY.md のメニュー経路表記を同期。

### 2026-08-06 23:30 JST - Claude Sonnet 5 - 種別: 作業結果 - v0.0.00128 ユーザー操作記録をデバッグログへ追加

- 作業AIモデル: Claude Sonnet 5
- NSApplicationサブクラス（MosaicApplication）のsendActionオーバーライドで、
  メニュー・ボタン・セグメント・ポップアップの操作をUI表示名で一元記録（category Action）。
- スライダードラッグ・テキスト入力は対象外（ログ溢れ・プライバシー配慮）。
- DEBUG_LOG_INVENTORY.mdへActionカテゴリの節を追加。

### 2026-08-07 00:00 JST - Claude Sonnet 5 - 種別: 作業結果 - v0.0.00129 実写人物マスクの上下反転を修正

- 作業AIモデル: Claude Sonnet 5（原因調査を含むが、実画像レンダリングによる実測手法が確立済みのため単一モデルで対応）
- 実写・動画フレームの人物マスク上下反転を修正。原因はVisionPersonDetector.cgImage(from:)の
  verticallyFlippedForRaster()が表示側修正（v82）後に二重反転として残っていたこと。
- 実画像レンダリングで実測: 反転を戻すと体に完全一致（SAMマスクとのIoU: 正立0.94/反転0.44）。
- 反転マスクはPersonMaskSampler（骨格対応付け）とmaskBounds（人物枠y座標）も誤らせていた。
- 回帰テストvisionPersonMaskIsNotVerticallyFlippedを追加。テスト149件PASS。

### 2026-08-07 00:40 JST - Claude Sonnet 5 - 種別: 作業結果 - v0.0.00130 SAM経路の二段フォールバック

- 作業AIモデル: Claude Sonnet 5
- 「漫画コマ右下の男性器が形状に沿ってマスク生成されない」報告（8C52ADAB）を修正。
- 実測: full経路 coverage=0.26（v123から図形フォールバック中）→ crop経路 coverage=0.41で
  ペニス輪郭に沿ったマスクを取得（PNGレンダリングで目視確認）。
- samMaskを二段チェーン化（第一経路→不合格なら反対経路→図形フォールバック）。
  しきい値0.40は不変のため検閲漏れ側の安全性は同一。
- 回帰テストlowCoverageSAMMaskFallsBackToShapeを新挙動へ更新。テスト149件PASS。

### 2026-08-07 01:00 JST - Claude Sonnet 5 - 種別: 作業結果 - 実写/イラスト解析精度の比較表

- 作業AIモデル: Claude Sonnet 5
- ユーザー依頼「実写とイラストのマスク生成、範囲選択、骨格検知など解析精度の違いを発生原因も含めて比較表にまとめて」に対応。
- 本セッションの実測値（人物マスクIoU=0.94、SAM coverage実測、骨格関節数0〜15件、部位検出の視覚検証）に基づき、
  7項目（ドメイン判定/人物検出/人物マスク/骨格/部位検出/SAMマスク生成/顔領域）の比較表と
  根本原因3系統（学習データのドメイン差・画の性質差・構図遮蔽）をチャットへ提示。
- 結論: Vision系は漫画でほぼ機能せず、アニメ系は分離能力が弱く、SAMは線画で手がかり不足。
  遮蔽・フレームアウトは両ドメイン共通の弱点でモデル差し替え無しでは限界 → 半自動方針でカバー。
- コード変更なし。乳首位置ずれ対応の方針（補正実装/現状維持/学習モード強化）はユーザー指示待ち。

### 2026-08-07 02:00 JST - Claude Sonnet 5 - 種別: 依頼内容 - ディルド範囲ずれの報告

```
★バグ：ディルド範囲ずれ。添付画像：ずれ修正／自動検知後（7624B71B、v0.0.00130ログ添付）
```

### 2026-08-07 02:00 JST - Claude Sonnet 5 - 種別: 作業結果 - v0.0.00131 samShapeマスクの安全側膨張

- 作業AIモデル: Claude Sonnet 5
- 同一画像（md5一致）・同一ROI・アプリ同一経路の再現レンダリングで実測し、
  SAMマスクは対象に正確に一致（coverage 0.596＝報告ログの0.60）、系統的な位置ズレは無いと確認。
- 報告の実体は (a) 対象輪郭ぎりぎりで止まるSAMマスクの縁スライバー露出、
  (b) ROIドラッグではSAMが同一対象へロックしマスクが追従しない仕様、の2点。
- 対策: samMaskへCIMorphologyMaximumの膨張（ROI長辺3%、2〜12px）を形状クリップ前に追加。
  安全側（覆い不足解消）で、はみ出しは形状クリップで抑制。目視で縁露出の解消を確認。
- 回帰テストsamMaskEdgeDilationCoversObjectEdge追加。テスト150件PASS。

### 2026-08-08 03:00 JST - Claude Fable 5 - 種別: 依頼内容 - ボーダー関連の改善・バグ報告と質問

```
★改善：パターン＞帯の間隔の下に「並行揺れ」を移動
★バグ：パターン＞ボーダーがマスク選択範囲の角度を基準に縦横表示されない。
★質問：コード修正不要
・UI改善。モザイク加工アプリと現状の画像処理の実装状態からして、画像種別、マスク生成は、現状のすべての選択項目必要？マスク生成は「図形〜」と「対象形状(SAM)」「学習モデル形状」のみで良い気がします。
・ツールバーやインスペクタの内容について、モザイク編集の流れやUIをUXを考慮した場合、改善した方が良い点あります？
```

### 2026-08-08 03:00 JST - Claude Fable 5 - 種別: 作業結果 - v0.0.00132 ボーダーのROI回転追従と行順変更

- 作業AIモデル: Claude Fable 5（ユーザー操作で切替済み。AI自身によるモデル切替は不可のためこのモデルで実施）
- インスペクタのボーダー行順を「帯の太さ→帯の間隔→並行揺れ→方向→ボーダー」へ変更（可視性マップは行5〜9すべてボーダー時表示のため行順変更のみで整合）。
- ボーダーの縞アルファへROI回転を適用（形状マスクと同じciRotation規約・ROI中心軸）。縞はスタイル単位キャッシュ共有のため回転はROI毎に適用。
- 回帰テストborderStripesFollowROIRotation追加。テスト151件PASS。
- 質問2件（マスク生成選択肢の絞り込み・ツールバー/インスペクタUX）はチャットで回答（コード修正なし）。
- 作業時間: 約40分

### 2026-08-08 04:00 JST - Claude Fable 5 - 種別: 依頼内容 - 3択化の実装指示と起動時パネル幅改善

```
★修正して
★起動直後のサイドツールパネルの幅が微妙。改善して。現状は添付画像参照
```

### 2026-08-08 04:00 JST - Claude Fable 5 - 種別: 作業結果 - v0.0.00133 マスク生成3択化とパネル幅安定化

- 作業AIモデル: Claude Fable 5
- マスク生成の選択肢を3択（図形/対象形状(SAM)/学習モデル形状）へ絞り込み。旧方式は内部・保存互換維持、読み戻しはSAMへ丸め。
- 起動時パネル幅の3点対策: ハードコード280の是正・最小幅未満の幅を保存しない・起動後1ターン遅延の最終補正。
- テスト151件PASS、両CIゲート通過。起動直後の見た目はGUI確認をお願いします。
- 作業時間: 約35分
