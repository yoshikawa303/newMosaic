# チャット作業履歴 2026-07-27週

### 2026-07-31 JST - Claude Opus 5 - 種別: 依頼内容 - 小さいROIのSAM形状・既定変更・学習量の質問

```
★添付のような、小さい男性性器だと「対象形状」の方が正しく形状認識できるようです。SAMでも改善可能か？それとも自動検知した場合よりよい正しい形状の検出方法あるか？
★マスク生成のデフォルトを対象形状（SAM）にして。初期実装比較様のアルゴリズムは、今のところ活用範囲ないため、削除。
★学習モデル形状の場合、どの程度学習作業が必要なんですか？画像１枚、２枚？
```

### 2026-07-31 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00104

- 小さいROIでSAMの形状が出ない原因を特定（全体を長辺1024へ縮小＋マスクは256×256生成のため、
  乳首ROI 25×27pxは入力換算17＝マスク格子4×4まで潰れる）。周辺を切り出してから推論する経路を追加。
  実測で入力換算60以下の被覆率 0.89〜0.97 → 0.72〜0.82。130以上では差が無いためしきい値96で切替。
- 検出しきい値を0.30→0.40（実測: 誤検出1件のみ消え、真の検出は減らない）。
- `dropOversizedGenitalROIs` を削除。実測で真の検出を消していた（規則自体が誤り）。
- 人物シルエットの経路を `PersonSilhouetteProvider` へ集約。塗り潰し判定がクロップ比で見ていたため
  働いていなかった不具合を修正（人物枠比0.90で判定）。被覆率 0.96→0.79、0.97→0.68。
- マスク生成の既定を `samShape` へ変更。`regionForegroundLegacy` と実装クラスを削除。
- 実画像回帰テストの期待値をGit管理下へ。サンプル3枚で再現率1.00・誤検出0件。テスト129件PASS。
- 学習モデル形状の必要データ量について回答（1〜2枚では不可能。実用には数百インスタンス規模）。
- 作業時間: 約2.0時間

### 2026-07-31 JST - Claude Opus 5 - 種別: 依頼内容 - リリースファイルとメモリ使用量

```
★リリースファイルが有りません。
★アプリの使用メモリがVS Codeより異常に大きい６GBオーバーなのは改善できませんか？
```

### 2026-07-31 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00105

- `dist/newMosaic.app` を再生成（従来はv0.0.00103時点のものが残っていた）。
- メモリを実測（`MemoryFootprintTests` を新設）。モデル常駐は約1.0〜1.5GBで、SAM初回推論の540MBは
  MobileSAMエンコーダ固有（スレッド数1/2/4/既定で不変）。6GBの主因ではないと切り分けた。
- 主因は「元に戻す」1ステップがフル解像度の描画済み画像（A4 300dpiで34.8MB）を保持し、
  スタックに上限が無く、さらに画像切替用に最大8枚分のスタックを持っていたこと（最大約5.6GB）。
  `EditorState` をROIのみへ変更し、undo時はROIから再描画する方式にした。
- 併せてサムネイルキャッシュにLRU上限300件、SAM埋め込みキャッシュを24→8件、
  ONNX Runtimeにアリーナ縮小指定を追加。
- 代償として「元に戻す」に再描画時間がかかる点をARCHITECTURE §5.49.2へ明記。
- テスト130件PASS。作業時間: 約1.5時間

### 2026-07-31 JST - Claude Opus 5 - 種別: 依頼内容 - GUI報告3件とメモリ再報告

```
★バグ：楕円マスク時、男性性器の先端、根本が範囲選択されない。添付画像中央あたり
★バグ：画像下部左女性性器範囲が大きい？画像下部右女性性器範囲は誤検知
★画像右上 あご誤検知
★使用メモリ削減されましたが、まだ多いです。保持している表示中画像のレイヤ情報は範囲情報など軽いデータです。画像処理アプリってこんなメモリ食いましたっけ。
```

### 2026-07-31 JST - Claude Opus 5 - 種別: 経過 - v0.0.00106（メモリのみ対応）

- メモリ: `PerImageEditState` から描画済み画像を削除、保持枚数を8→3。約630MB削減見込み。
- 検出バグ2件（下段の性器・右上のあご）は、アプリと同じ合成経路を再現する診断テストを追加して
  実測したが、**ライブラリ原本では再現しなかった**。ユーザーへ確認を依頼。
- 楕円マスクの先端・根本の件は、マスク範囲の変更にあたるため CLAUDE.md §2.2 に従い実装前に確認を依頼。

### 2026-07-31 JST - Claude Opus 5 - 種別: 依頼内容 - 診断ログ・楕円拡大・運用ルール追記

```
★v0.0.00105 以降のアプリ使用です。解析結果の環境を疑うのであれば、ログにソース画像ファイル名、MD5のハッシュ値、各ROIのマスク生成設定、解析結果を残してください。アプリ解析ログをこちらに添付します。
★「1.」を実装して
★リモート環境で開発も行う為、Claude.mdとCodex様のAgent.mdに書き内容が記載されていなければ、追記して
・コード修正、実装、ドキュメント更新など行った場合は、都度コミット、プッシュを必ず行い、ロールバック可能にする。
・コード修正を行う場合は、実装内容毎に、コード品質を保ちながら、最もコストパフォーマンスの良いAIモデルに切替えながら実装を行う。作業前と作業後に実装内容と作業AIモデル情報をチャットに表示し、チャット履歴ファイルにも記録する。
```

### 2026-07-31 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00107

- 作業AIモデル: Claude Opus 5（本セッションは実行中AIが自身のモデルを切り替えられないため、全工程をOpus 5で実施）
- 運用ルール追記: `CLAUDE.md` §2.0（都度コミット・都度プッシュ）、§2.0.1（実装ごとのAIモデル切替と記録）。
  `AGENTS.md` / `GEMINI.md` にも要約を記載。
- 解析診断ログ `AnalysisDiagnostics` を追加。ファイル名・MD5・画素サイズ・版数・画像種別・
  検出しきい値・マスク生成方式・候補カテゴリ＋全ROI（マスク生成設定の個別/継承を含む）を出力。
  `DEBUG_LOG_INVENTORY.md` にプライバシー方針の例外として明記。
- 性器の楕円ROIを1.15倍へ拡大（`DetectedROIRefiner.expandGenitalEllipses`）。手描き・矩形・多角形・乳首は対象外。
- テスト133件PASS、両ゲート通過。
- **プッシュ不能を検出**: ONNXモデル2件がGitHubの100MBファイル上限を超え、65コミットが未push状態だった。
  対応方針の判断をユーザーへ依頼。
- 作業時間: 約1.2時間

### 2026-07-31 JST - Claude Opus 5 - 種別: 依頼内容 - 認識結果の誤検知報告とログ不備

```
下記認識結果になりました。
誤認識：
・眼窩下〜あご　２個所
・性器（女性）　　２個所　※一箇所は女性器付近ではあるが、モザイクが不要なレベル
バグ：デバッグログに、検知ファイル情報が残っていない
```
（SegmentMaskログ添付）

### 2026-07-31 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00108

- 作業AIモデル: Claude Opus 5
- 添付ログの `boxSideInInput` を実測値と照合し、**サンプル画像と同一**と確定
  （nipple 33/11/11、maleGenital 150/57、femaleGenital 156 は、いずれも実測値×1.15＝楕円拡大後の値）。
- 差分の2件（femaleGenital 127/122）は検出器由来ではなく `LearningEngine` の学習提案と特定。
  2つの不具合を修正:
  1. 位置は人物枠相対なのに大きさだけ画像相対 → 引きのコマの小さな人物に巨大ROIが付いていた
  2. 検出器が見つけたカテゴリにも提案していた → 骨格プライアと同じくフォールバック扱いへ
- 解析ログを `.info` → `.notice` へ（取りこぼし防止）。
- テスト135件PASS。
- 作業時間: 約0.8時間

### 2026-07-31 JST - Claude Opus 5 - 種別: 依頼内容 - 巨大モデルの除外と個別インストール運用

```
★プッシュ時オープンソースのモデルなど巨大なファイルは除外し、個別インストール運用にする。
```

### 2026-07-31 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00109

- 作業AIモデル: Claude Opus 5
- モデル8点（451MB）を `/Volumes/DATA/newMosaic_Models_Backup` へ退避後、Git管理外・同梱対象外へ変更。
- `Package.swift` のリソース宣言から除外、`.gitignore` へ追加、追跡中の4点を `git rm --cached`。
- `scripts/install_models.sh` と `Docs/MODELS.md` を追加。未検出時のエラーに導入手順を追記。
- テスト135件PASS（Application Support 経由でモデルが読めることを実測）。
- **未完**: 未push範囲のコミットに含まれる巨大blobの除去（`git filter-branch`）は破壊的操作のため
  権限で拒否された。ユーザーの承認と実行が必要。バックアップ `backup-before-model-strip` を作成済み。
- 作業時間: 約0.6時間

### 2026-07-31 JST - Claude Opus 5 - 種別: 依頼内容 - 学習モード・初期化/バックアップ・パネル重なり

```
・追加：学習モードON/OFF設定とツールバーにON/OFFアイコンを追加。学習するかしないかは、ユーザーが学習モードにした場合のみ学習する様にして。
・私が手で書いた以前のROIはテスト様に書き込んだものも含まれる為、初期モデルからの学習データは一旦削除して。
・追加：ファイル＞設定＞初期化　実行時、初期化項目を個別に指定可能にする。モデルの学習内容の初期化も可能にする。
・追加：ファイル＞設定＞バックアップ　初期化メニューの下にバックアップメニューを追加。各種アプリ設定、動作状態、モデル内容などを一括バックアップ可能にする。一括バックアップ時、バックアップ項目を個別にチェックON/OFF指定可能にする。
・バグ：サイドツールパネルの縦サイズを添付の様に調整した際、アイコンなどの表示が重なってしまう。
```

### 2026-07-31 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00110

- 作業AIモデル: Claude Opus 5
- 学習モード（既定OFF）とツールバーのトグルアイコンを追加。OFF時は記録も利用も行わない。
- 既存の学習データ381サンプル・31MBを削除（`/Volumes/DATA/newMosaic_Learning_Backup_20260802-193343` へ退避）。
- ファイル＞設定＞初期化 を項目別チェック方式へ変更（アプリ設定／レイアウト／学習内容／ライブラリ）。
- ファイル＞設定＞バックアップ を追加（項目別チェック、戻し方のREADME同梱）。
- サイドパネル縦分割の最小高さ制約を追加し、行・アイコンの重なりを解消。
- テスト135件PASS、両ゲート通過。
- 作業時間: 約1.5時間

### 2026-07-31 JST - Claude Opus 5 - 種別: 依頼内容 - 認識範囲とマスクの形状一致

```
★改善：
・矩形範囲：大きめに認識される。
・楕円、多角形：性器より小さく範囲が選択される。マスクが、範囲外もマスクされる。※良い面もあるが、基本的には性器を範囲内で正しく捉え、性器部がマスクされることが正しい動作。
```

### 2026-07-31 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00111

- 作業AIモデル: Claude Opus 5
- 矩形が大きい原因: v0.0.00107の拡大が検出直後（全ROIが既定`.ellipse`の時点）に走り、
  形状ガードが素通りしていた。形状決定後に、形状ごとの倍率で拡大する方式へ変更。
  倍率は幾何から算出（矩形1.0／楕円√2／六角形1.58）。
- マスクのはみ出し原因: §5.46で「矩形だけで切る」分岐があった。拡大により形状が対象を
  包み込むようになったため、常に選択形状で切る方式へ変更。
- テスト135件PASS（形状ごとの倍率検証を追加）。
- 断続的な失敗を追跡し、原因を2つ特定: (1) `embeddingCacheAvoidsReencoding` が実行時間の比較で
  判定しており機械の負荷で揺れていた、(2) 埋め込みキャッシュがプロセス共有のため並列実行された
  他テストが対象画像を入れ替えていた。実行回数での判定＋テスト専用キャッシュへ変更し、
  全体テストの5回連続完走を確認。
- 作業時間: 約0.7時間

### 2026-08-02 JST - Claude Opus 5 - 種別: 依頼内容 - ログ永続化・UI文言・クラッシュ

```
★デバッグログ
・アプリ再起動でも前回ログが保持される様にする。ログはローテーション保存する。各ファイル１MBで最大5ファイル
★UI改善
・「レイヤ削除」→「全レイヤ削除」に変更。
★アプリが異常終了する。
（クラッシュレポート添付: EXC_BAD_ACCESS / @objc VideoPreviewView.togglePlay()）
```

### 2026-08-02 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00112

- 作業AIモデル: Claude Opus 5
- `RotatingLogFile` を新設（1MB×5世代）。起動直後・30秒ごと・終了時に退避。
  デバッグログ画面は保存済み＋今回分を表示。テスト2件追加。
- 「レイヤ削除」→「全レイヤ削除」へ改名。
- クラッシュ対応: 動画プレビューウィンドウに delegate が無く、閉じても `stop()` が呼ばれず
  コントロールの `target` が自分自身を指したまま残っていた。`windowWillClose` での後始末と
  `stop()` の target 解除を追加。**再現手順が不明なため、これが唯一の原因である確証は無い**旨を
  ARCHITECTURE §5.55.2 に明記。
- ローカルに残っていたONNXモデル8点を `Sources/` から削除（SwiftPMの未処理ファイル警告の解消）。
- テスト137件PASS、両ゲート通過。
- 作業時間: 約1.0時間

### 2026-08-02 JST - Claude Opus 5 - 種別: 依頼内容 - キー割当後のクラッシュ

```
★詳細設定でキー割当設定後、アプリが不安定になり強制終了する。キー割当後、再度キー割当設定画面が開けない。
（クラッシュレポート添付: v0.0.00112、EXC_BAD_ACCESS）
```

### 2026-08-02 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00113

- 作業AIモデル: Claude Opus 5
- 原因を特定: `NSWindow.isReleasedWhenClosed` の既定 `true` と、アプリ側の強参照が併存して
  過剰解放になっていた。閉じた補助ウィンドウを再度開く経路で解放済みへ
  `makeKeyAndOrderFront:` を送り、開けない／落ちる。
- 前回の報告で「動画プレビューのライフサイクル」と見立てたが、**スタックのシンボル名が誤帰属**で
  あったと判明（動画プレビュー未使用の操作で同一スタック。レジスタが `makeKeyAndOrderFront:` と
  `showAdvancedSettings()` を指していた）。この読み違いの経緯と判断材料を ARCHITECTURE §5.56.2 に残した。
- 補助ウィンドウ5つ＋動画プレビューに `isReleasedWhenClosed = false` を設定し、
  `windowWillClose` で参照を解放するようにした。
- テスト137件PASS、両ゲート通過。
- 作業時間: 約0.5時間

### 2026-08-02 JST - Claude Opus 5 - 種別: 依頼内容 - 形状で結果が変わる

```
★矩形では正しく判定されるが、楕円、多角形で同じ性器を範囲指定しても性器が同じ状態にマスクされない。
（v0.0.00112の解析ログ添付）
```

### 2026-08-02 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00114

- 作業AIモデル: Claude Opus 5
- 添付ログから原因を特定: v0.0.00111 で性器ROIを形状ごとに広げたが、`rect` はSAMのプロンプト枠・
  前景抽出のクロップとしても使われており、広げた枠を渡してエンジンが別の範囲を切り出していた
  （被覆率 矩形0.55 → 楕円0.28 → 多角形0.22）。**私の設計ミス**。
- `MosaicROI.analysisInsetScale` / `analysisRect` を追加し、「表示の枠」と「解析の枠」を分離。
  SAMのプロンプトと対象形状のクロップを解析枠基準へ変更。
- 実測で確認: サンプル3枚の性器ROI10件すべてで、矩形・楕円・多角形の被覆率が完全一致（差0.000）。
- テスト140件PASS、両ゲート通過。
- 作業時間: 約0.7時間

### 2026-08-02 JST - Claude Opus 5 - 種別: 依頼内容 - アプリ強制終了（再発）

```
★アプリ強制終了
（クラッシュレポート添付: v0.0.00114、EXC_BAD_ACCESS、applicationShouldTerminate内のobjc_retain）
```

### 2026-08-02 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00115

- 作業AIモデル: Claude Opus 5
- **前回 v0.0.00113 の修正漏れ**。補助ウィンドウ5つは直したが、`AppDelegate` が保持する
  主ウィンドウと動画書き出しシートを見落としていた。主ウィンドウが解放済みになり、
  「最後のウィンドウを閉じた」→終了判定→`applicationShouldTerminate` の `if let window` で落ちていた。
- 全8箇所の `NSWindow(` 生成を機械的に監査して残り2箇所を修正。
- 再発防止として `agent_governance_guard.sh` にチェックを追加（故意にFAILすることも確認）。
- テスト140件PASS、両ゲート通過。
- 作業時間: 約0.4時間

### 2026-08-02 JST - Claude Opus 5 - 種別: 依頼内容 - 実写の誤検知とペンツール

```
★人物の性器、骨格、人物誤検知する。
★マスク形状をペンツールでペイント修正可能にする。
```

### 2026-08-02 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00116

- 作業AIモデル: Claude Opus 5
- ペンによるマスク修正を実装。`ManualMaskStroke` / `ManualMaskPainter`（コア）＋
  キャンバスの `maskPaint` モード＋インスペクタの太さ設定。
  ストローク（点列）で保持する方式にした（保存が小さく、ROIの移動・リサイズに追従し、1本ずつ取り消せる）。
- 座標系はコードベースで反転事故が多いため、既存の `hardShapeMask` と同じ位置に出るかで検証。
  この検証で実装バグ（1点ストロークの塗り色が黒のまま）を1件発見・修正。
- テスト143件PASS、両ゲート通過。
- **未着手**: 実写画像の誤検知（性器・骨格・人物）。原因の特定に `analysis` ログの `domain=` が要るため、
  ユーザーへログの提供を依頼した。
- 作業時間: 約1.2時間

### 2026-08-02 JST - Claude Opus 5 - 種別: 依頼内容 - 誤検知4件とモザイク編集時のハング

```
★バグ：
・一番上漫画コマ：性器有りの手前人物認識なし
・真ん中漫画コマ：乳首、乳輪誤検知
・左下漫画コマ　：人物背中に　乳首、乳輪誤検知
・右下漫画コマ　：性器範囲誤検知、性器範囲判定できていない
★バグ：
・モザイク対象の１つのモザイクを編集する毎に、アプリが一時ハングする。継承、個別設定両方で発生。
```
（v0.0.00116の解析ログ添付）

### 2026-08-02 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00117

- 作業AIモデル: Claude Opus 5
- ハングの原因を特定して修正: `applyMosaic` が毎回 `createMasks` を実行しており、既定のSAMだと
  ROIごとにONNX推論（実測20〜30ms×15件）が主スレッドで走っていた。マスクはスタイルに依存しないので
  `maskIdentity` を鍵にキャッシュする方式へ変更。生成回数を数えるテストで検証。
- 誤検知は添付ログから**すべて `src=pose-chest`（骨格由来の推定）**と特定。検出器は乳首を1件も
  出しておらず、実際にこのページに乳首は写っていない。骨格プライアが6件を発明していた。
  対処方針（既定OFF化／削除）はマスク範囲の仕様変更にあたるため、ユーザーへ確認を依頼。
- テスト145件PASS、両ゲート通過。
- 作業時間: 約0.8時間

### 2026-08-02 JST - Claude Opus 5 - 種別: 依頼内容 - 骨格プライア廃止・グループ分け・表示ON/OFF・ログ消去・関連ウィンドウ

```
★「A」対応
★改善：自動分析後、モザイク対象内で、候補カテゴリ毎にグループ分けする。グループ名は、カテゴリ名にする。
★改善：グループ単位、モザイク対象内も含めすべてのレイヤ単位で表示ON/OFFチェックを表示する。
★追加：デバッグログウィンドウ内にログクリアボタンを追加。ローテーションファイルも含め削除可能にする。
★改善：アプリメインウィンドウがクローズされた場合は、デバッグログウィンドウ、詳細設定ウィンドウ、キー割当ウィンドウなど関連ウィンドウもすべて終了する。
```

### 2026-08-02 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00118

- 作業AIモデル: Claude Opus 5
- 案A: 骨格由来の乳首・乳輪プライアを廃止（`chestROIs` / `dropChestPriors` を削除）。
  鼠径部プライアは誤検知報告が無いため残す。
- 自動検出の結果をカテゴリごとにグループ化（`roiGroupName` にカテゴリ表示名）。
- レイヤ一覧に1件ごと・グループごとの表示ON/OFFチェックを追加。**画面表示のみで出力には影響しない**
  （隠したまま出力して検閲漏れになるのを避けるため）。
- デバッグログウィンドウに「消去…」を追加（世代ファイルごと削除、確認ダイアログ付き）。
- 主ウィンドウを閉じたら補助ウィンドウ6種をすべて閉じるようにした。
- テスト144件PASS、両ゲート通過。
- 作業時間: 約1.0時間

### 2026-08-03 JST - Claude Opus 5 - 種別: 依頼内容 - 人物マスク・ログ消去・ペン/消しゴム

```
★バグ：各人物のマスク範囲がおかしい。
★バグ：デバッグログ＞消去ボタンを押しても、すべてのデバッグログファイルが削除されない。
★変更：「マスク修正モード」→「マスク追加ペン」に変更（レイヤ未選択時は新規レイヤ追加）
★追加：追加マスクペンの右に「マスク消しゴム」を追加
```

### 2026-08-03 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00119

- 作業AIモデル: Claude Opus 5
- ログ消去の不具合を修正。削除は成功していたが、退避位置を空へ戻していたため30秒ごとの
  退避処理が全行を書き戻していた。退避位置を「今の最後の行」まで進める方式へ変更。
- 「マスク追加ペン」へ改名し、レイヤ未選択時は新規レイヤ（その他）を作って塗るようにした。
- 「マスク消しゴム」を追加。Option(⌥)で両者が一時的に入れ替わる。
- テスト144件PASS、両ゲート通過。
- **未着手**: 実写の人物マスク範囲。原因の切り分けに該当画像が必要なため、ユーザーへ提供を依頼。
  併せて、実写経路には人物マスクの品質判定（被覆率チェック＋SAMフォールバック）が無いという
  構造上の差を確認した（イラスト経路にはある）。
- 作業時間: 約0.8時間

### 2026-08-03 JST - Claude Opus 5 - 種別: 依頼内容 - トーン全パターン・検索欄・ノイズ既定・設定保持

```
＞★人物マスクの範囲 — 未着手です
動作確認で使用している画像サンプルを３枚追加しました。現象報告は女性２人の写真です。
★追加：各モザイクパターン全てにトーンON/OFF追加し、適用可能にする。
★バグ：ライブラリ＞検索テキスト入力欄が、サイドツールパネルの幅調整時、幅と同期して伸縮しない。
★改善：モザイク＞パターン＞ノイズ＞細かさ デフォルト値は4pxにする。
★改善：モザイク＞パターンの各パラメータ修正後、アプリ内に設定状態保持し、次回同じパターン使用時に前回設定値が適用される様にする。
```

### 2026-08-03 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00120

- 作業AIモデル: Claude Opus 5
- 全パターン共通のトーン（`patternTone`）を追加。ボーダー・雲・フラッシュは専用設定があるため除外。
- ライブラリ検索欄の幅追従を修正（フィルタ行の右端制約が `lessThanOrEqualTo` だった）。
- ノイズの「細かさ」既定値を4pxへ。
- パターン別の設定保持は**既に実装済み**だったことを確認（`mosaicStyleKeyPrefix` /
  `loadMosaicStyleDetails`）。動作が期待と違う場合は具体例を要確認。
- 追加サンプルで `samProducesTheSameMaskRegardlessOfROIShape` が失敗し、v0.0.00114の設計の穴
  （端のROIでクランプが逆算を壊す）を発見・修正。全10件で差0.000へ回復。
- 実写の人物マスクを実測（Vision vs SAM）。被覆率では優劣がつかず、**修正を根拠づけられない**ことを
  ARCHITECTURE §5.63 に記録。
- テスト145件PASS、両ゲート通過。
- 作業時間: 約1.5時間

### 2026-08-03 JST - Claude Opus 5 - 種別: 依頼内容 - 右下性器位置誤判定

```
★右下性器位置誤判定
```
（v0.0.00120の解析ログ添付）

### 2026-08-03 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00121

- 作業AIモデル: Claude Opus 5
- ログの座標を照合し、検出枠の中心はモデル出力のまま（拡大で中心は動いていない）ことを確認。
  見た目が大きく／ずれて見える原因は `rect` 自体を広げていたこと。
- 拡大の要否を実測（拡大なしだとマスクが1.2〜6.7%失われる）。拡大は必要と確認。
- そこで**表示の枠は検出枠のまま、マスクを切り取る形状だけを広げる**方式へ変更。
  マスクの被覆率が変更前と完全に同一であることを実測で確認。
- 副次的に `analysisInsetScale` が不要になり、§5.62の「クランプで逆算が壊れる」構造も解消。
- テスト146件PASS、両ゲート通過。
- 作業時間: 約0.8時間

