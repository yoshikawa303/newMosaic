# チャット作業履歴 2026-07-27週

### 2026-07-31 JST - Claude Opus 5 - 種別: 依頼内容 - 小さいROIのSAM形状・既定変更・学習量の質問

```
★添付のような、小さい男性性器だと「対象形状」の方が正しく形状認識できるようです。SAMでも改善可能か？それとも自動検知した場合よりよい正しい形状の検出方法あるか？
★マスク生成のデフォルトを対象形状（SAM）にして。初期実装比較様のアルゴリズムは、今のところ活用範囲ないため、削除。
★学習モデル形状の場合、どの程度学習作業が必要なんですか？画像１枚、２枚？
```

### 2026-07-31 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00104

- 小さいROIでSAMの形状が出ない原因を特定（全体を長辺1024へ縮小＋マスクは256×256生成のため、
  乳首ROI 25×27pxは入力換算17＝マスク格子4×4まで潰れる）。周辺を切り出してから推論する経路を追加。
  実測で入力換算60以下の被覆率 0.89〜0.97 → 0.72〜0.82。130以上では差が無いためしきい値96で切替。
- 検出しきい値を0.30→0.40（実測: 誤検出1件のみ消え、真の検出は減らない）。
- `dropOversizedGenitalROIs` を削除。実測で真の検出を消していた（規則自体が誤り）。
- 人物シルエットの経路を `PersonSilhouetteProvider` へ集約。塗り潰し判定がクロップ比で見ていたため
  働いていなかった不具合を修正（人物枠比0.90で判定）。被覆率 0.96→0.79、0.97→0.68。
- マスク生成の既定を `samShape` へ変更。`regionForegroundLegacy` と実装クラスを削除。
- 実画像回帰テストの期待値をGit管理下へ。サンプル3枚で再現率1.00・誤検出0件。テスト129件PASS。
- 学習モデル形状の必要データ量について回答（1〜2枚では不可能。実用には数百インスタンス規模）。
- 作業時間: 約2.0時間

### 2026-07-31 JST - Claude Opus 5 - 種別: 依頼内容 - リリースファイルとメモリ使用量

```
★リリースファイルが有りません。
★アプリの使用メモリがVS Codeより異常に大きい６GBオーバーなのは改善できませんか？
```

### 2026-07-31 JST - Claude Opus 5 - 種別: 作業結果 - v0.0.00105

- `dist/newMosaic.app` を再生成（従来はv0.0.00103時点のものが残っていた）。
- メモリを実測（`MemoryFootprintTests` を新設）。モデル常駐は約1.0〜1.5GBで、SAM初回推論の540MBは
  MobileSAMエンコーダ固有（スレッド数1/2/4/既定で不変）。6GBの主因ではないと切り分けた。
- 主因は「元に戻す」1ステップがフル解像度の描画済み画像（A4 300dpiで34.8MB）を保持し、
  スタックに上限が無く、さらに画像切替用に最大8枚分のスタックを持っていたこと（最大約5.6GB）。
  `EditorState` をROIのみへ変更し、undo時はROIから再描画する方式にした。
- 併せてサムネイルキャッシュにLRU上限300件、SAM埋め込みキャッシュを24→8件、
  ONNX Runtimeにアリーナ縮小指定を追加。
- 代償として「元に戻す」に再描画時間がかかる点をARCHITECTURE §5.49.2へ明記。
- テスト130件PASS。作業時間: 約1.5時間

### 2026-07-31 JST - Claude Opus 5 - 種別: 依頼内容 - GUI報告3件とメモリ再報告

```
★バグ：楕円マスク時、男性性器の先端、根本が範囲選択されない。添付画像中央あたり
★バグ：画像下部左女性性器範囲が大きい？画像下部右女性性器範囲は誤検知
★画像右上 あご誤検知
★使用メモリ削減されましたが、まだ多いです。保持している表示中画像のレイヤ情報は範囲情報など軽いデータです。画像処理アプリってこんなメモリ食いましたっけ。
```

### 2026-07-31 JST - Claude Opus 5 - 種別: 経過 - v0.0.00106（メモリのみ対応）

- メモリ: `PerImageEditState` から描画済み画像を削除、保持枚数を8→3。約630MB削減見込み。
- 検出バグ2件（下段の性器・右上のあご）は、アプリと同じ合成経路を再現する診断テストを追加して
  実測したが、**ライブラリ原本では再現しなかった**。ユーザーへ確認を依頼。
- 楕円マスクの先端・根本の件は、マスク範囲の変更にあたるため CLAUDE.md §2.2 に従い実装前に確認を依頼。

