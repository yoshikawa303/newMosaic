# コードレビュー v0.0.00152

- 対象: 動画の1フレーム移動ショートカット、中間フレーム修正の時刻同期・永続化
- バージョン: v0.0.00152
- ブランチ: main
- レビュー回数: 3回

| 確認回 | 優先度 | 問題点 | 修正 |
|---|---|---|---|
| 1 | P1 | キーフレーム一覧が左右キーを独自に前後キーフレーム移動へ割り当て、動画共通ショートカットより先に消費する | 一覧の左右キーを`stepVideoFrame(by:)`へ接続し、一覧外もローカルイベント監視で同じ処理へ統一 |
| 1 | P1 | シークバーの連続時刻をそのまま表示・保存し、実際に取得された動画フレームと手動キーフレーム時刻がずれる | 停止中シークを元動画fpsの最寄りフレーム境界へ整列 |
| 2 | P1 | 再生を一時停止した直後は実時間由来の端数時刻が残り、そのまま編集すると再び時刻ずれが起こる | 一時停止時と修正保存直前にも最寄りフレームへ整列し、編集開始位置を再読込 |
| 2 | P2 | 29.97fps等の時刻を600分の1秒・許容誤差ゼロで要求すると、該当サンプルを取得できない場合がある | 停止中は60,000 timescaleとフレーム間隔45%の取得許容値を使用 |
| 2 | P2 | 左右キー仕様変更後に旧キーフレーム一覧移動関数が未使用で残る | 未使用関数を削除 |
| 3 | - | 追加のP0/P1問題なし | 変更不要 |

## 残余リスク

- `nominalFrameRate`基準の整列であり、可変フレームレート動画では実サンプル時刻とわずかに差が出る可能性がある。取得許容値内の最寄りフレームを表示するため、編集単位としての重複・飛び越しは抑制する。
- AppKitのキーボード実操作、実動画上のROIドラッグ、前後フレームへの補正、再生・書き出し結果の目視確認は自動テストでは代替できない。

## 確認

- `swift test --filter VideoEditStoreTests`
- `swift test --filter VideoKitTests`
- `swift test`
- `swift build`
- `git diff --check`
- `agent_governance_guard` / `local_quality_gate`
- macOSパッケージ作成 / 署名検証 / バージョン確認 / 起動確認

上記はすべてPASS。AppKitショートカットの実操作と実動画上の手動修正目視は未実施。
