# コードレビュー v0.0.00151

- 対象: 動画フレーム修正の即時保存、前後キーフレーム補正、高fpsフレーム識別
- バージョン: v0.0.00151
- ブランチ: main
- レビュー回数: 3回

| 確認回 | 優先度 | 問題点 | 修正 |
|---|---|---|---|
| 1 | P1 | ROIドラッグ以外の形状・カテゴリ・マスクペン・削除・Undo/Redo経路では現在フレーム保存が行われず、同じ反映漏れが残る | 動画の全ROI編集経路を`persistCurrentVideoFrameCorrectionIfNeeded()`へ統一 |
| 1 | P2 | 位置差分が無いスタイル変更でも近傍キーフレームを更新済みとして扱う | 有効な中心・サイズ・回転差分がある場合だけ伝播 |
| 2 | P1 | 動画自動解析の再実行時、手動アンカーは復元されるが周辺へ伝播済みの補正が新しい自動結果へ再適用されない | 手動キーフレームを時刻順に`applyManualCorrection`で復元 |
| 2 | P2 | 前後補正した回転値が0〜360度の範囲外になり得る | 伝播後の回転を0〜360度へ正規化 |
| 3 | - | 追加のP0/P1問題なし | 変更不要 |

## 残余リスク

- 前後補正は最大1秒の幾何差分伝播であり、対象が急に向きを変える、遮蔽される、シーンカットを跨ぐ場合は追加の手動アンカーが必要。
- 実動画上のドラッグ、ペン補正、Undo/Redo、再生・書き出しの目視確認は自動テストでは代替できない。

## 確認

- `swift test --filter VideoEditStoreTests`
- `swift test`
- `swift build`
- `git diff --check`
- `agent_governance_guard` / `local_quality_gate`
- macOSパッケージ作成 / 署名検証 / バージョン確認 / 起動確認

上記はすべてPASS。実動画上の手動ドラッグと前後フレームの目視確認は未実施。
