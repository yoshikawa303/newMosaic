# newMosaic 品質統計

## アプリ基盤

### macOS MVP

- バグ報告回数: 24
- 修正回数: 20
- 状態: Build 48でプロ向け単一アイコンツールバー、標準メニュー、ズーム/パン、右側3分割インスペクタ、ウィンドウ/分割位置保存を実装。開く・貼り付け・画像切替・削除・終了の未保存保護を共通化し、候補生成をバックグラウンド化。Build 69でツールバーアイコンの選択ハイライトの表示崩れ、サイドバーパネルが再起動後に表示されない不具合、パネル移動時の幅引き継ぎ、右パネル既定幅（ライブラリ2列相当）、テキストサイズ設定の反映範囲（一部表示のみ→アプリ内主要テキスト全体・即時反映）を修正。v0.0.00075（2026-07-25）でツールバーアイコンのフォーカスリング不具合、インスペクタ幅追従、レイヤ一覧グループ化ボタンの無反応（ROI選択リスト非対応）、レイヤ一覧右クリックメニュー新設、画像上のレイヤ選択強調表示、Command+ドラッグでのROIコピー、ヘルプ＞デバッグ＞デバッグログ新設、バージョン表示方式の一本化を実施。v0.0.00077でコードベース全体のコードレビュー（5並列エージェント→実コード検証→修正→再検証の4パス）を実施し、ROIドラッグのジェスチャー状態残留バグ、`LibraryEngine`の並行アクセス競合を含む19件を検出。うち15件を修正し、4件（ORTEnv共有化・AnimeSegmenter出力行方向の実測検証・importLinkedのO(n²)・大きな画像IOのメインスレッド実行）は設計変更や実測検証を要するため理由を明記して見送った（詳細はQC記録参照）。v0.0.00078でツールバーの編集モード/範囲選択モード切替（保留していた機能）を実装し、レイヤパネルのテキスト右寄せ・グループ再作成時の連番増え続けバグを修正、グループ内個別選択時のグループ解除、表示ラベル短縮・並び順変更、マスク生成既定値変更、ツールバー項目の移設・改名を実施。v0.0.00079でライブラリのサムネイル拡大縮小アイコンサイズ不一致、サイドパネル既定配置（右パネルがアプリの大半を占める不具合）を修正し、モザイクパターンにフラッシュ（集中線）を追加、ボーダー3パターンを1つへ統合した。
- 最終更新: 2026-07-26

### 画像ライブラリ

- バグ報告回数: 2
- 修正回数: 2
- 状態: グリッド/テキスト/サムネイル表示切替対応。Build 48で右ペイン幅が戻る問題を修正し、表示モード/サムネイル寸法を永続化、グリッドの縦間隔を圧縮。
- 最終更新: 2026-07-23

## 画像処理

### ROI / モザイク生成

- バグ報告回数: 6
- 修正回数: 6
- 状態: Vision由来マスクの上下反転をBuild 18で修正。Build 48で各ROIに独立した描画スタイルと任意パターン画像IDを保存し、同一画像内で異なるモザイク設定を描画可能にした。旧ライブラリJSONはstyle欠落を既定設定として互換読込。v0.0.00075でノイズパターンのカラー化を修正（モノクロ化）、かぶせ画像未設定ROIが他レイヤのプレビューまで巻き添えで解除される不具合を修正（`skipIncompletePatterns`導入）、ROIへ`roiGroupName`を追加してレイヤ一覧でのグループ化に対応。v0.0.00077のコードレビューで、「線・エッジぼかし」「アンシャープ」パターンの画像外周での偽エッジ検出、人物セグメンテーション方式のVision実行時エラーで処理全体が失敗する不具合を修正。v0.0.00079で、ROI自動回転（v0.0.00076）が乳首検出の中心位置まで傾き方向へずらしていたことによる誤ROI2箇所の混入、回転ROIで「対象形状」マスク生成が検知できない不具合を修正。モザイクパターンにフラッシュ（集中線、中心位置を画像上でドラッグ指定可能）を追加し、ボーダー（縦/横/ランダム）を1パターンへ統合した（既存ライブラリJSONとの互換読込あり）。
- 最終更新: 2026-07-26

## AI連携

### モデル交換境界

- バグ報告回数: 3
- 修正回数: 3
- 状態: Phase 1〜4実装済み。Build 48で人物マスクの元解像度境界、5人以上の矩形補完、人物ごとの骨格候補評価、横臥姿勢の回転推論、低解像度拡大、顔起点再探索を統合。骨格表示領域を人物領域へ一致させ、Vision失敗は統合ログへ記録。
- 最終更新: 2026-07-23

## テスト状況

- Swift Testing: 73件 PASS（v0.0.00079）
- 対象: ROI別スタイル/任意パターン、旧JSON互換（ボーダー旧パターン名の移行含む）、複数人物統合、元解像度マスク境界、回転座標復元、横臥候補、人物関連付け、既存画像処理・ライブラリ回帰
- GUI: アプリ起動確認を実施。画面キャプチャはmacOSの画面収録権限がないため未取得。ツールバーのモード切替・レイヤパネル関連・フラッシュ中心ハンドルのドラッグ操作等はAppKit UIロジックのためユーザーによるGUI確認が必要。
- 最終更新: 2026-07-26
